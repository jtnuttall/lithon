-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core10.SamplerCommands (
  SamplerCreateInfo (..),
  createSampler,
  withSampler,
  destroySampler,
) where

import Control.Exception (bracket)
import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Control.Monad.IO.Unlift (MonadUnliftIO, withRunInIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.DeviceInitialization
import Lithon.Vk.Core10.Enums.BorderColor
import Lithon.Vk.Core10.Enums.CompareOp
import Lithon.Vk.Core10.Enums.Filter
import Lithon.Vk.Core10.Enums.SamplerAddressMode
import Lithon.Vk.Core10.Enums.SamplerCreateFlagBits
import Lithon.Vk.Core10.Enums.SamplerMipmapMode
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSamplerCreateInfo.html VkSamplerCreateInfo>
-- Available: VK_VERSION_1_0
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
data SamplerCreateInfo (es :: [Type]) = SamplerCreateInfo
  { next :: Chain es
  , flags :: SamplerCreateFlags
  , magFilter :: Filter
  , minFilter :: Filter
  , mipmapMode :: SamplerMipmapMode
  , addressModeU :: SamplerAddressMode
  , addressModeV :: SamplerAddressMode
  , addressModeW :: SamplerAddressMode
  , mipLodBias :: Float
  , anisotropyEnable :: Bool
  , maxAnisotropy :: Float
  , compareEnable :: Bool
  , compareOp :: CompareOp
  , minLod :: Float
  , maxLod :: Float
  , borderColor :: BorderColor
  , unnormalizedCoordinates :: Bool
  }

instance (AllExtends SamplerCreateInfo es, PokeChain es) => CStruct (SamplerCreateInfo es) where
  cSize = 80
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (31 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 20 (toWire x.magFilter))
    liftIO (pokeByteOff p 24 (toWire x.minFilter))
    liftIO (pokeByteOff p 28 (toWire x.mipmapMode))
    liftIO (pokeByteOff p 32 (toWire x.addressModeU))
    liftIO (pokeByteOff p 36 (toWire x.addressModeV))
    liftIO (pokeByteOff p 40 (toWire x.addressModeW))
    liftIO (pokeByteOff p 44 x.mipLodBias)
    liftIO (pokeByteOff p 48 (if x.anisotropyEnable then TRUE else FALSE))
    liftIO (pokeByteOff p 52 x.maxAnisotropy)
    liftIO (pokeByteOff p 56 (if x.compareEnable then TRUE else FALSE))
    liftIO (pokeByteOff p 60 (toWire x.compareOp))
    liftIO (pokeByteOff p 64 x.minLod)
    liftIO (pokeByteOff p 68 x.maxLod)
    liftIO (pokeByteOff p 72 (toWire x.borderColor))
    liftIO (pokeByteOff p 76 (if x.unnormalizedCoordinates then TRUE else FALSE))
    pure ()

instance (AllExtends SamplerCreateInfo es, PokeChain es, PeekChain es) => FromCStruct (SamplerCreateInfo es) where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_flags <- (peekByteOff p 16)
    v_magFilter <- (unsafeFromWire <$> peekByteOff p 20)
    v_minFilter <- (unsafeFromWire <$> peekByteOff p 24)
    v_mipmapMode <- (unsafeFromWire <$> peekByteOff p 28)
    v_addressModeU <- (unsafeFromWire <$> peekByteOff p 32)
    v_addressModeV <- (unsafeFromWire <$> peekByteOff p 36)
    v_addressModeW <- (unsafeFromWire <$> peekByteOff p 40)
    v_mipLodBias <- (peekByteOff p 44)
    v_anisotropyEnable <- (toBool <$> peekByteOff p 48)
    v_maxAnisotropy <- (peekByteOff p 52)
    v_compareEnable <- (toBool <$> peekByteOff p 56)
    v_compareOp <- (unsafeFromWire <$> peekByteOff p 60)
    v_minLod <- (peekByteOff p 64)
    v_maxLod <- (peekByteOff p 68)
    v_borderColor <- (unsafeFromWire <$> peekByteOff p 72)
    v_unnormalizedCoordinates <- (toBool <$> peekByteOff p 76)
    pure
      SamplerCreateInfo
        { next = v_next
        , flags = v_flags
        , magFilter = v_magFilter
        , minFilter = v_minFilter
        , mipmapMode = v_mipmapMode
        , addressModeU = v_addressModeU
        , addressModeV = v_addressModeV
        , addressModeW = v_addressModeW
        , mipLodBias = v_mipLodBias
        , anisotropyEnable = v_anisotropyEnable
        , maxAnisotropy = v_maxAnisotropy
        , compareEnable = v_compareEnable
        , compareOp = v_compareOp
        , minLod = v_minLod
        , maxLod = v_maxLod
        , borderColor = v_borderColor
        , unnormalizedCoordinates = v_unnormalizedCoordinates
        }

instance (NilChain es) => Nil (SamplerCreateInfo es) where
  nil =
    SamplerCreateInfo
      { next = nilChain
      , flags = mempty
      , magFilter = FilterNearest
      , minFilter = FilterNearest
      , mipmapMode = SamplerMipmapModeNearest
      , addressModeU = SamplerAddressModeRepeat
      , addressModeV = SamplerAddressModeRepeat
      , addressModeW = SamplerAddressModeRepeat
      , mipLodBias = 0
      , anisotropyEnable = False
      , maxAnisotropy = 0
      , compareEnable = False
      , compareOp = CompareOpNever
      , minLod = 0
      , maxLod = 0
      , borderColor = BorderColorFloatTransparentBlack
      , unnormalizedCoordinates = False
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateSampler.html vkCreateSampler>
-- Available: VK_VERSION_1_0
-- __Deprecated__.
createSampler
  :: forall m es1
   . (MonadIO m, AllExtends SamplerCreateInfo es1, PokeChain es1)
  => Device
  -> SamplerCreateInfo es1
  -> Maybe AllocationCallbacks
  -> m (Outcome Sampler)
createSampler device createInfo allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_createInfo :: Ptr () <- castPtr <$> new createInfo
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    p_o_sampler <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkCreateSampler
            (checkCommandPtr "vkCreateSampler" device.cmds.pCreateSampler)
            (castPtr device.handle)
            p_createInfo
            p_allocator
            (castPtr p_o_sampler)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_sampler <- liftIO (peekByteOff p_o_sampler 0 :: IO Word64)
      let o_sampler = Sampler w_o_sampler
      pure (Ok (openFromWire r_) o_sampler)

withSampler
  :: forall m r es1
   . (MonadUnliftIO m, AllExtends SamplerCreateInfo es1, PokeChain es1)
  => Device
  -> SamplerCreateInfo es1
  -> Maybe AllocationCallbacks
  -> (Outcome Sampler -> m r)
  -> m r
withSampler device createInfo allocator cont_ =
  withRunInIO \run_ ->
    bracket
      (run_ (createSampler device createInfo allocator))
      ( \o_ -> case o_ of
          Ok _ h_ -> run_ (destroySampler device h_ allocator)
          Err _ -> pure ()
      )
      (run_ . cont_)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkDestroySampler.html vkDestroySampler>
-- Available: VK_VERSION_1_0
-- __Deprecated__.
destroySampler
  :: forall m
   . (MonadIO m)
  => Device
  -> Sampler
  -> Maybe AllocationCallbacks
  -> m ()
destroySampler device sampler allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    liftIO
      ( mkDestroySampler
          (checkCommandPtr "vkDestroySampler" device.cmds.pDestroySampler)
          (castPtr device.handle)
          (case sampler of Sampler w_ -> w_)
          p_allocator
      )
    pure ()
