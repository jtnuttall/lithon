-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core10.ExtensionDiscoveryCommands (
  ExtensionProperties (..),
  enumerateDeviceExtensionProperties,
  enumerateInstanceExtensionProperties,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable (..), peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems, peekFixedString, pokeFixedString)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkExtensionProperties.html VkExtensionProperties>
-- Available: VK_VERSION_1_0
data ExtensionProperties = ExtensionProperties
  { extensionName :: !ByteString
  , specVersion :: !Word32
  }
  deriving stock (Eq, Show)

instance Storable ExtensionProperties where
  sizeOf _ = 260
  alignment _ = 4
  peek p =
    ExtensionProperties
      <$> (peekFixedString 256 p 0)
      <*> (peekByteOff p 256)
  poke p x = do
    pokeFixedString 256 p 0 x.extensionName
    pokeByteOff p 256 x.specVersion

instance CStruct ExtensionProperties where
  cSize = 260
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct ExtensionProperties where
  peekCStruct = peek

instance Nil ExtensionProperties where
  nil =
    ExtensionProperties
      { extensionName = ""
      , specVersion = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkEnumerateDeviceExtensionProperties.html vkEnumerateDeviceExtensionProperties>
-- Available: VK_VERSION_1_0
enumerateDeviceExtensionProperties
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> Maybe ByteString
  -> m (Outcome (V.Vector ExtensionProperties))
enumerateDeviceExtensionProperties physicalDevice layerName =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_layerName :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . newCString) layerName
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkEnumerateDeviceExtensionProperties
                  ( checkCommandPtr
                      "vkEnumerateDeviceExtensionProperties"
                      physicalDevice.cmds.pEnumerateDeviceExtensionProperties
                  )
                  (castPtr physicalDevice.handle)
                  p_layerName
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pPropertyCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_properties <- arenaBytes (260 * max 1 (fromIntegral n_pPropertyCount)) 4
            forM_
              ([0 .. fromIntegral n_pPropertyCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_properties `plusPtr` (i * 260))) (nil :: ExtensionProperties))
            r2_ <-
              liftIO
                ( mkEnumerateDeviceExtensionProperties
                    ( checkCommandPtr
                        "vkEnumerateDeviceExtensionProperties"
                        physicalDevice.cmds.pEnumerateDeviceExtensionProperties
                    )
                    (castPtr physicalDevice.handle)
                    p_layerName
                    (castPtr p_n_)
                    (castPtr p_o_properties)
                )
            n_pPropertyCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_properties <- liftIO (peekElems (fromIntegral n_pPropertyCount') (castPtr p_o_properties))
                pure (Ok (openFromWire r2_) o_properties)
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkEnumerateInstanceExtensionProperties.html vkEnumerateInstanceExtensionProperties>
-- Available: VK_VERSION_1_0
enumerateInstanceExtensionProperties
  :: forall m
   . (MonadIO m)
  => Maybe ByteString
  -> m (Outcome (V.Vector ExtensionProperties))
enumerateInstanceExtensionProperties layerName =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_layerName :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . newCString) layerName
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkEnumerateInstanceExtensionProperties
                  ( checkCommandPtr
                      "vkEnumerateInstanceExtensionProperties"
                      globalCmds.pEnumerateInstanceExtensionProperties
                  )
                  p_layerName
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pPropertyCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_properties <- arenaBytes (260 * max 1 (fromIntegral n_pPropertyCount)) 4
            forM_
              ([0 .. fromIntegral n_pPropertyCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_properties `plusPtr` (i * 260))) (nil :: ExtensionProperties))
            r2_ <-
              liftIO
                ( mkEnumerateInstanceExtensionProperties
                    ( checkCommandPtr
                        "vkEnumerateInstanceExtensionProperties"
                        globalCmds.pEnumerateInstanceExtensionProperties
                    )
                    p_layerName
                    (castPtr p_n_)
                    (castPtr p_o_properties)
                )
            n_pPropertyCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_properties <- liftIO (peekElems (fromIntegral n_pPropertyCount') (castPtr p_o_properties))
                pure (Ok (openFromWire r2_) o_properties)
    goDual_
