-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core10.PipelineLayoutCommands (
  PushConstantRange (..),
  PipelineLayoutCreateInfo (..),
  createPipelineLayout,
  withPipelineLayout,
  destroyPipelineLayout,
) where

import Control.Exception (bracket)
import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Control.Monad.IO.Unlift (MonadUnliftIO, withRunInIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable (..), peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.DeviceInitialization
import Lithon.Vk.Core10.Enums.PipelineLayoutCreateFlagBits
import Lithon.Vk.Core10.Enums.ShaderStageFlagBits
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPushConstantRange.html VkPushConstantRange>
-- Available: VK_VERSION_1_0
-- __Deprecated__.
data PushConstantRange = PushConstantRange
  { stageFlags :: !ShaderStageFlags
  , offset :: !Word32
  , size :: !Word32
  }
  deriving stock (Eq, Show)

instance Storable PushConstantRange where
  sizeOf _ = 12
  alignment _ = 4
  peek p =
    PushConstantRange
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 4)
      <*> (peekByteOff p 8)
  poke p x = do
    pokeByteOff p 0 x.stageFlags
    pokeByteOff p 4 x.offset
    pokeByteOff p 8 x.size

instance CStruct PushConstantRange where
  cSize = 12
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct PushConstantRange where
  peekCStruct = peek

instance Nil PushConstantRange where
  nil =
    PushConstantRange
      { stageFlags = mempty
      , offset = 0
      , size = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPipelineLayoutCreateInfo.html VkPipelineLayoutCreateInfo>
-- Available: VK_VERSION_1_0
-- __Deprecated__.
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @setLayoutCount@ is derived from @setLayouts@'s length.
--   * @pushConstantRangeCount@ is derived from @pushConstantRanges@'s length.
data PipelineLayoutCreateInfo = PipelineLayoutCreateInfo
  { flags :: PipelineLayoutCreateFlags
  , setLayouts :: V.Vector DescriptorSetLayout
  , pushConstantRanges :: V.Vector PushConstantRange
  }

instance CStruct PipelineLayoutCreateInfo where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (30 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    p_setLayouts <- arenaBytes (8 * max 1 (V.length x.setLayouts)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_setLayouts (i * 8) v) x.setLayouts)
    liftIO
      (pokeByteOff p 24 (if V.null x.setLayouts then nullPtr else (castPtr p_setLayouts :: Ptr ())))
    liftIO (pokeByteOff p 20 (fromIntegral (V.length x.setLayouts) :: Word32))
    p_pushConstantRanges <-
      if V.null x.pushConstantRanges then pure nullPtr else newVector x.pushConstantRanges
    liftIO (pokeByteOff p 40 p_pushConstantRanges)
    liftIO (pokeByteOff p 32 (fromIntegral (V.length x.pushConstantRanges) :: Word32))
    pure ()

instance FromCStruct PipelineLayoutCreateInfo where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_setLayouts <- do
      n <- (peekByteOff p 20 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_pushConstantRanges <- do
      n <- (peekByteOff p 32 :: IO Word32)
      q <- peekByteOff p 40
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure
      PipelineLayoutCreateInfo
        { flags = v_flags
        , setLayouts = v_setLayouts
        , pushConstantRanges = v_pushConstantRanges
        }

instance Nil PipelineLayoutCreateInfo where
  nil =
    PipelineLayoutCreateInfo
      { flags = mempty
      , setLayouts = V.empty
      , pushConstantRanges = V.empty
      }

instance Chainable PipelineLayoutCreateInfo where
  sTypeWire = 30
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreatePipelineLayout.html vkCreatePipelineLayout>
-- Available: VK_VERSION_1_0
-- __Deprecated__.
createPipelineLayout
  :: forall m
   . (MonadIO m)
  => Device
  -> PipelineLayoutCreateInfo
  -> Maybe AllocationCallbacks
  -> m (Outcome PipelineLayout)
createPipelineLayout device createInfo allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_createInfo :: Ptr () <- castPtr <$> new createInfo
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    p_o_pipelineLayout <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkCreatePipelineLayout
            (checkCommandPtr "vkCreatePipelineLayout" device.cmds.pCreatePipelineLayout)
            (castPtr device.handle)
            p_createInfo
            p_allocator
            (castPtr p_o_pipelineLayout)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_pipelineLayout <- liftIO (peekByteOff p_o_pipelineLayout 0 :: IO Word64)
      let o_pipelineLayout = PipelineLayout w_o_pipelineLayout
      pure (Ok (openFromWire r_) o_pipelineLayout)

withPipelineLayout
  :: forall m r
   . (MonadUnliftIO m)
  => Device
  -> PipelineLayoutCreateInfo
  -> Maybe AllocationCallbacks
  -> (Outcome PipelineLayout -> m r)
  -> m r
withPipelineLayout device createInfo allocator cont_ =
  withRunInIO \run_ ->
    bracket
      (run_ (createPipelineLayout device createInfo allocator))
      ( \o_ -> case o_ of
          Ok _ h_ -> run_ (destroyPipelineLayout device h_ allocator)
          Err _ -> pure ()
      )
      (run_ . cont_)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkDestroyPipelineLayout.html vkDestroyPipelineLayout>
-- Available: VK_VERSION_1_0
-- __Deprecated__.
destroyPipelineLayout
  :: forall m
   . (MonadIO m)
  => Device
  -> PipelineLayout
  -> Maybe AllocationCallbacks
  -> m ()
destroyPipelineLayout device pipelineLayout allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    liftIO
      ( mkDestroyPipelineLayout
          (checkCommandPtr "vkDestroyPipelineLayout" device.cmds.pDestroyPipelineLayout)
          (castPtr device.handle)
          (case pipelineLayout of PipelineLayout w_ -> w_)
          p_allocator
      )
    pure ()
