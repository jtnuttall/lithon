-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core10.SparseResourceMemoryManagementAPICommandsOptional (
  ImageSubresource (..),
  SparseImageFormatProperties (..),
  SparseImageMemoryBind (..),
  SparseImageMemoryRequirements (..),
  SparseMemoryBind (..),
  BindSparseInfo (..),
  SparseBufferMemoryBindInfo (..),
  SparseImageMemoryBindInfo (..),
  SparseImageOpaqueMemoryBindInfo (..),
  getImageSparseMemoryRequirements,
  queueBindSparse,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable (..), peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Enums.ImageAspectFlagBits
import Lithon.Vk.Core10.Enums.SparseImageFormatFlagBits
import Lithon.Vk.Core10.Enums.SparseMemoryBindFlagBits
import Lithon.Vk.Core10.FundamentalTypesUsedByManyCommandsAndStructures
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkImageSubresource.html VkImageSubresource>
-- Available: VK_VERSION_1_0
data ImageSubresource = ImageSubresource
  { aspectMask :: !ImageAspectFlags
  , mipLevel :: !Word32
  , arrayLayer :: !Word32
  }
  deriving stock (Eq, Show)

instance Storable ImageSubresource where
  sizeOf _ = 12
  alignment _ = 4
  peek p =
    ImageSubresource
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 4)
      <*> (peekByteOff p 8)
  poke p x = do
    pokeByteOff p 0 x.aspectMask
    pokeByteOff p 4 x.mipLevel
    pokeByteOff p 8 x.arrayLayer

instance CStruct ImageSubresource where
  cSize = 12
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct ImageSubresource where
  peekCStruct = peek

instance Nil ImageSubresource where
  nil =
    ImageSubresource
      { aspectMask = mempty
      , mipLevel = 0
      , arrayLayer = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSparseImageFormatProperties.html VkSparseImageFormatProperties>
-- Available: VK_VERSION_1_0
data SparseImageFormatProperties = SparseImageFormatProperties
  { aspectMask :: !ImageAspectFlags
  , imageGranularity :: !Extent3D
  , flags :: !SparseImageFormatFlags
  }
  deriving stock (Eq, Show)

instance Storable SparseImageFormatProperties where
  sizeOf _ = 20
  alignment _ = 4
  peek p =
    SparseImageFormatProperties
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 4)
      <*> (peekByteOff p 16)
  poke p x = do
    pokeByteOff p 0 x.aspectMask
    pokeByteOff p 4 x.imageGranularity
    pokeByteOff p 16 x.flags

instance CStruct SparseImageFormatProperties where
  cSize = 20
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct SparseImageFormatProperties where
  peekCStruct = peek

instance Nil SparseImageFormatProperties where
  nil =
    SparseImageFormatProperties
      { aspectMask = mempty
      , imageGranularity = nil
      , flags = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSparseImageMemoryBind.html VkSparseImageMemoryBind>
-- Available: VK_VERSION_1_0
data SparseImageMemoryBind = SparseImageMemoryBind
  { subresource :: !ImageSubresource
  , offset :: !Offset3D
  , extent :: !Extent3D
  , memory :: !DeviceMemory
  , memoryOffset :: !Word64
  , flags :: !SparseMemoryBindFlags
  }
  deriving stock (Eq, Show)

instance Storable SparseImageMemoryBind where
  sizeOf _ = 64
  alignment _ = 8
  peek p =
    SparseImageMemoryBind
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 12)
      <*> (peekByteOff p 24)
      <*> (peekByteOff p 40)
      <*> (peekByteOff p 48)
      <*> (peekByteOff p 56)
  poke p x = do
    pokeByteOff p 0 x.subresource
    pokeByteOff p 12 x.offset
    pokeByteOff p 24 x.extent
    pokeByteOff p 40 x.memory
    pokeByteOff p 48 x.memoryOffset
    pokeByteOff p 56 x.flags

instance CStruct SparseImageMemoryBind where
  cSize = 64
  cAlign = 8
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct SparseImageMemoryBind where
  peekCStruct = peek

instance Nil SparseImageMemoryBind where
  nil =
    SparseImageMemoryBind
      { subresource = nil
      , offset = nil
      , extent = nil
      , memory = nil
      , memoryOffset = 0
      , flags = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSparseImageMemoryRequirements.html VkSparseImageMemoryRequirements>
-- Available: VK_VERSION_1_0
data SparseImageMemoryRequirements = SparseImageMemoryRequirements
  { formatProperties :: !SparseImageFormatProperties
  , imageMipTailFirstLod :: !Word32
  , imageMipTailSize :: !Word64
  , imageMipTailOffset :: !Word64
  , imageMipTailStride :: !Word64
  }
  deriving stock (Eq, Show)

instance Storable SparseImageMemoryRequirements where
  sizeOf _ = 48
  alignment _ = 8
  peek p =
    SparseImageMemoryRequirements
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 20)
      <*> (peekByteOff p 24)
      <*> (peekByteOff p 32)
      <*> (peekByteOff p 40)
  poke p x = do
    pokeByteOff p 0 x.formatProperties
    pokeByteOff p 20 x.imageMipTailFirstLod
    pokeByteOff p 24 x.imageMipTailSize
    pokeByteOff p 32 x.imageMipTailOffset
    pokeByteOff p 40 x.imageMipTailStride

instance CStruct SparseImageMemoryRequirements where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct SparseImageMemoryRequirements where
  peekCStruct = peek

instance Nil SparseImageMemoryRequirements where
  nil =
    SparseImageMemoryRequirements
      { formatProperties = nil
      , imageMipTailFirstLod = 0
      , imageMipTailSize = 0
      , imageMipTailOffset = 0
      , imageMipTailStride = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSparseMemoryBind.html VkSparseMemoryBind>
-- Available: VK_VERSION_1_0
data SparseMemoryBind = SparseMemoryBind
  { resourceOffset :: !Word64
  , size :: !Word64
  , memory :: !DeviceMemory
  , memoryOffset :: !Word64
  , flags :: !SparseMemoryBindFlags
  }
  deriving stock (Eq, Show)

instance Storable SparseMemoryBind where
  sizeOf _ = 40
  alignment _ = 8
  peek p =
    SparseMemoryBind
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 8)
      <*> (peekByteOff p 16)
      <*> (peekByteOff p 24)
      <*> (peekByteOff p 32)
  poke p x = do
    pokeByteOff p 0 x.resourceOffset
    pokeByteOff p 8 x.size
    pokeByteOff p 16 x.memory
    pokeByteOff p 24 x.memoryOffset
    pokeByteOff p 32 x.flags

instance CStruct SparseMemoryBind where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct SparseMemoryBind where
  peekCStruct = peek

instance Nil SparseMemoryBind where
  nil =
    SparseMemoryBind
      { resourceOffset = 0
      , size = 0
      , memory = nil
      , memoryOffset = 0
      , flags = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkBindSparseInfo.html VkBindSparseInfo>
-- Available: VK_VERSION_1_0
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @waitSemaphoreCount@ is derived from @waitSemaphores@'s length.
--   * @bufferBindCount@ is derived from @bufferBinds@'s length.
--   * @imageOpaqueBindCount@ is derived from @imageOpaqueBinds@'s length.
--   * @imageBindCount@ is derived from @imageBinds@'s length.
--   * @signalSemaphoreCount@ is derived from @signalSemaphores@'s length.
data BindSparseInfo (es :: [Type]) = BindSparseInfo
  { next :: Chain es
  , waitSemaphores :: V.Vector Semaphore
  , bufferBinds :: V.Vector SparseBufferMemoryBindInfo
  , imageOpaqueBinds :: V.Vector SparseImageOpaqueMemoryBindInfo
  , imageBinds :: V.Vector SparseImageMemoryBindInfo
  , signalSemaphores :: V.Vector Semaphore
  }

instance (AllExtends BindSparseInfo es, PokeChain es) => CStruct (BindSparseInfo es) where
  cSize = 96
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (7 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    p_waitSemaphores <- arenaBytes (8 * max 1 (V.length x.waitSemaphores)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_waitSemaphores (i * 8) v) x.waitSemaphores)
    liftIO
      (pokeByteOff p 24 (if V.null x.waitSemaphores then nullPtr else (castPtr p_waitSemaphores :: Ptr ())))
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.waitSemaphores) :: Word32))
    p_bufferBinds <- if V.null x.bufferBinds then pure nullPtr else newVector x.bufferBinds
    liftIO (pokeByteOff p 40 p_bufferBinds)
    liftIO (pokeByteOff p 32 (fromIntegral (V.length x.bufferBinds) :: Word32))
    p_imageOpaqueBinds <-
      if V.null x.imageOpaqueBinds then pure nullPtr else newVector x.imageOpaqueBinds
    liftIO (pokeByteOff p 56 p_imageOpaqueBinds)
    liftIO (pokeByteOff p 48 (fromIntegral (V.length x.imageOpaqueBinds) :: Word32))
    p_imageBinds <- if V.null x.imageBinds then pure nullPtr else newVector x.imageBinds
    liftIO (pokeByteOff p 72 p_imageBinds)
    liftIO (pokeByteOff p 64 (fromIntegral (V.length x.imageBinds) :: Word32))
    p_signalSemaphores <- arenaBytes (8 * max 1 (V.length x.signalSemaphores)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_signalSemaphores (i * 8) v) x.signalSemaphores)
    liftIO
      ( pokeByteOff
          p
          88
          (if V.null x.signalSemaphores then nullPtr else (castPtr p_signalSemaphores :: Ptr ()))
      )
    liftIO (pokeByteOff p 80 (fromIntegral (V.length x.signalSemaphores) :: Word32))
    pure ()

instance (AllExtends BindSparseInfo es, PokeChain es, PeekChain es) => FromCStruct (BindSparseInfo es) where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_waitSemaphores <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_bufferBinds <- do
      n <- (peekByteOff p 32 :: IO Word32)
      q <- peekByteOff p 40
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    v_imageOpaqueBinds <- do
      n <- (peekByteOff p 48 :: IO Word32)
      q <- peekByteOff p 56
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    v_imageBinds <- do
      n <- (peekByteOff p 64 :: IO Word32)
      q <- peekByteOff p 72
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    v_signalSemaphores <- do
      n <- (peekByteOff p 80 :: IO Word32)
      q <- peekByteOff p 88 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    pure
      BindSparseInfo
        { next = v_next
        , waitSemaphores = v_waitSemaphores
        , bufferBinds = v_bufferBinds
        , imageOpaqueBinds = v_imageOpaqueBinds
        , imageBinds = v_imageBinds
        , signalSemaphores = v_signalSemaphores
        }

instance (NilChain es) => Nil (BindSparseInfo es) where
  nil =
    BindSparseInfo
      { next = nilChain
      , waitSemaphores = V.empty
      , bufferBinds = V.empty
      , imageOpaqueBinds = V.empty
      , imageBinds = V.empty
      , signalSemaphores = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSparseBufferMemoryBindInfo.html VkSparseBufferMemoryBindInfo>
-- Available: VK_VERSION_1_0
--
-- Marshalling:
--   * @bindCount@ is derived from @binds@'s length.
data SparseBufferMemoryBindInfo = SparseBufferMemoryBindInfo
  { buffer :: Buffer
  , binds :: V.Vector SparseMemoryBind
  }

instance CStruct SparseBufferMemoryBindInfo where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 x.buffer)
    p_binds <- if V.null x.binds then pure nullPtr else newVector x.binds
    liftIO (pokeByteOff p 16 p_binds)
    liftIO (pokeByteOff p 8 (fromIntegral (V.length x.binds) :: Word32))
    pure ()

instance FromCStruct SparseBufferMemoryBindInfo where
  peekCStruct p = do
    v_buffer <- (peekByteOff p 0)
    v_binds <- do
      n <- (peekByteOff p 8 :: IO Word32)
      q <- peekByteOff p 16
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure SparseBufferMemoryBindInfo{buffer = v_buffer, binds = v_binds}

instance Nil SparseBufferMemoryBindInfo where
  nil =
    SparseBufferMemoryBindInfo
      { buffer = nil
      , binds = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSparseImageMemoryBindInfo.html VkSparseImageMemoryBindInfo>
-- Available: VK_VERSION_1_0
--
-- Marshalling:
--   * @bindCount@ is derived from @binds@'s length.
data SparseImageMemoryBindInfo = SparseImageMemoryBindInfo
  { image :: Image
  , binds :: V.Vector SparseImageMemoryBind
  }

instance CStruct SparseImageMemoryBindInfo where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 x.image)
    p_binds <- if V.null x.binds then pure nullPtr else newVector x.binds
    liftIO (pokeByteOff p 16 p_binds)
    liftIO (pokeByteOff p 8 (fromIntegral (V.length x.binds) :: Word32))
    pure ()

instance FromCStruct SparseImageMemoryBindInfo where
  peekCStruct p = do
    v_image <- (peekByteOff p 0)
    v_binds <- do
      n <- (peekByteOff p 8 :: IO Word32)
      q <- peekByteOff p 16
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure SparseImageMemoryBindInfo{image = v_image, binds = v_binds}

instance Nil SparseImageMemoryBindInfo where
  nil =
    SparseImageMemoryBindInfo
      { image = nil
      , binds = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSparseImageOpaqueMemoryBindInfo.html VkSparseImageOpaqueMemoryBindInfo>
-- Available: VK_VERSION_1_0
--
-- Marshalling:
--   * @bindCount@ is derived from @binds@'s length.
data SparseImageOpaqueMemoryBindInfo = SparseImageOpaqueMemoryBindInfo
  { image :: Image
  , binds :: V.Vector SparseMemoryBind
  }

instance CStruct SparseImageOpaqueMemoryBindInfo where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 x.image)
    p_binds <- if V.null x.binds then pure nullPtr else newVector x.binds
    liftIO (pokeByteOff p 16 p_binds)
    liftIO (pokeByteOff p 8 (fromIntegral (V.length x.binds) :: Word32))
    pure ()

instance FromCStruct SparseImageOpaqueMemoryBindInfo where
  peekCStruct p = do
    v_image <- (peekByteOff p 0)
    v_binds <- do
      n <- (peekByteOff p 8 :: IO Word32)
      q <- peekByteOff p 16
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure SparseImageOpaqueMemoryBindInfo{image = v_image, binds = v_binds}

instance Nil SparseImageOpaqueMemoryBindInfo where
  nil =
    SparseImageOpaqueMemoryBindInfo
      { image = nil
      , binds = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetImageSparseMemoryRequirements.html vkGetImageSparseMemoryRequirements>
-- Available: VK_VERSION_1_0
getImageSparseMemoryRequirements
  :: forall m
   . (MonadIO m)
  => Device
  -> Image
  -> m (V.Vector SparseImageMemoryRequirements)
getImageSparseMemoryRequirements device image =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          _ <-
            liftIO
              ( mkGetImageSparseMemoryRequirements
                  (checkCommandPtr "vkGetImageSparseMemoryRequirements" device.cmds.pGetImageSparseMemoryRequirements)
                  (castPtr device.handle)
                  (case image of Image w_ -> w_)
                  (castPtr p_n_)
                  nullPtr
              )
          n_pSparseMemoryRequirementCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
          p_o_sparseMemoryRequirements <-
            arenaBytes (48 * max 1 (fromIntegral n_pSparseMemoryRequirementCount)) 8
          forM_
            ([0 .. fromIntegral n_pSparseMemoryRequirementCount - 1] :: [Int])
            ( \i ->
                pokeCStruct
                  (castPtr (p_o_sparseMemoryRequirements `plusPtr` (i * 48)))
                  (nil :: SparseImageMemoryRequirements)
            )
          _ <-
            liftIO
              ( mkGetImageSparseMemoryRequirements
                  (checkCommandPtr "vkGetImageSparseMemoryRequirements" device.cmds.pGetImageSparseMemoryRequirements)
                  (castPtr device.handle)
                  (case image of Image w_ -> w_)
                  (castPtr p_n_)
                  (castPtr p_o_sparseMemoryRequirements)
              )
          n_pSparseMemoryRequirementCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
          o_sparseMemoryRequirements <-
            liftIO
              (peekElems (fromIntegral n_pSparseMemoryRequirementCount') (castPtr p_o_sparseMemoryRequirements))
          pure o_sparseMemoryRequirements
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkQueueBindSparse.html vkQueueBindSparse>
-- Available: VK_VERSION_1_0
queueBindSparse
  :: forall m
   . (MonadIO m)
  => Queue
  -> V.Vector (SomeStruct BindSparseInfo)
  -> Fence
  -> m (Outcome ())
queueBindSparse queue bindInfo fence =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_bindInfoCount = fromIntegral (V.length bindInfo) :: Word32
    p_bindInfo <- do
      pv_ <- arenaBytes (96 * max 1 (V.length bindInfo)) 8
      V.imapM_ (\i e_ -> withSomeStruct e_ (pokeCStruct (castPtr (pv_ `plusPtr` (i * 96))))) bindInfo
      pure (castPtr pv_ :: Ptr ())
    r_ <-
      liftIO
        ( mkQueueBindSparse
            (checkCommandPtr "vkQueueBindSparse" queue.cmds.pQueueBindSparse)
            (castPtr queue.handle)
            n_bindInfoCount
            p_bindInfo
            (case fence of Fence w_ -> w_)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
