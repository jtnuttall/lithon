-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core10.EventCommands (
  EventCreateInfo (..),
  createEvent,
  withEvent,
  destroyEvent,
  getEventStatus,
  resetEvent,
  setEvent,
) where

import Control.Exception (bracket)
import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Control.Monad.IO.Unlift (MonadUnliftIO, withRunInIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.DeviceInitialization
import Lithon.Vk.Core10.Enums.EventCreateFlagBits
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkEventCreateInfo.html VkEventCreateInfo>
-- Available: VK_VERSION_1_0
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data EventCreateInfo = EventCreateInfo
  { flags :: EventCreateFlags
  }

instance CStruct EventCreateInfo where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (10 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    pure ()

instance FromCStruct EventCreateInfo where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    pure EventCreateInfo{flags = v_flags}

instance Nil EventCreateInfo where
  nil =
    EventCreateInfo
      { flags = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateEvent.html vkCreateEvent>
-- Available: VK_VERSION_1_0
createEvent
  :: forall m
   . (MonadIO m)
  => Device
  -> EventCreateInfo
  -> Maybe AllocationCallbacks
  -> m (Outcome Event)
createEvent device createInfo allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_createInfo :: Ptr () <- castPtr <$> new createInfo
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    p_o_event <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkCreateEvent
            (checkCommandPtr "vkCreateEvent" device.cmds.pCreateEvent)
            (castPtr device.handle)
            p_createInfo
            p_allocator
            (castPtr p_o_event)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_event <- liftIO (peekByteOff p_o_event 0 :: IO Word64)
      let o_event = Event w_o_event
      pure (Ok (openFromWire r_) o_event)

withEvent
  :: forall m r
   . (MonadUnliftIO m)
  => Device
  -> EventCreateInfo
  -> Maybe AllocationCallbacks
  -> (Outcome Event -> m r)
  -> m r
withEvent device createInfo allocator cont_ =
  withRunInIO \run_ ->
    bracket
      (run_ (createEvent device createInfo allocator))
      ( \o_ -> case o_ of
          Ok _ h_ -> run_ (destroyEvent device h_ allocator)
          Err _ -> pure ()
      )
      (run_ . cont_)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkDestroyEvent.html vkDestroyEvent>
-- Available: VK_VERSION_1_0
destroyEvent
  :: forall m
   . (MonadIO m)
  => Device
  -> Event
  -> Maybe AllocationCallbacks
  -> m ()
destroyEvent device event allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    liftIO
      ( mkDestroyEvent
          (checkCommandPtr "vkDestroyEvent" device.cmds.pDestroyEvent)
          (castPtr device.handle)
          (case event of Event w_ -> w_)
          p_allocator
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetEventStatus.html vkGetEventStatus>
-- Available: VK_VERSION_1_0
getEventStatus
  :: forall m
   . (MonadIO m)
  => Device
  -> Event
  -> m (Outcome ())
getEventStatus device event =
  liftIO do
    r_ <-
      liftIO
        ( mkGetEventStatus
            (checkCommandPtr "vkGetEventStatus" device.cmds.pGetEventStatus)
            (castPtr device.handle)
            (case event of Event w_ -> w_)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkResetEvent.html vkResetEvent>
-- Available: VK_VERSION_1_0
resetEvent
  :: forall m
   . (MonadIO m)
  => Device
  -> Event
  -> m (Outcome ())
resetEvent device event =
  liftIO do
    r_ <-
      liftIO
        ( mkResetEvent
            (checkCommandPtr "vkResetEvent" device.cmds.pResetEvent)
            (castPtr device.handle)
            (case event of Event w_ -> w_)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkSetEvent.html vkSetEvent>
-- Available: VK_VERSION_1_0
setEvent
  :: forall m
   . (MonadIO m)
  => Device
  -> Event
  -> m (Outcome ())
setEvent device event =
  liftIO do
    r_ <-
      liftIO
        ( mkSetEvent
            (checkCommandPtr "vkSetEvent" device.cmds.pSetEvent)
            (castPtr device.handle)
            (case event of Event w_ -> w_)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
