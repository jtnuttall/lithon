-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_device_address_commands (
  AddressCommandFlagBitsKHR (..),
  AddressCommandFlagsKHR,
  DeviceAddressRangeKHR (..),
  AccelerationStructureCreateInfo2KHR (..),
  MemoryRangeBarrierKHR (..),
  MemoryRangeBarriersInfoKHR (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable (..), peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (BitPos (..), Flags (..), FlagsWire)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core13.Enums.AccessFlagBits2
import Lithon.Vk.Core13.Enums.PipelineStageFlagBits2
import Lithon.Vk.Extensions.KHR_acceleration_structure

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkAddressCommandFlagBitsKHR.html VkAddressCommandFlagBitsKHR>
-- Available: VK_KHR_device_address_commands
data AddressCommandFlagBitsKHR
  = AddressCommandProtectedBitKHR
  | AddressCommandFullyBoundBitKHR
  | AddressCommandStorageBufferUsageBitKHR
  | AddressCommandUnknownStorageBufferUsageBitKHR
  deriving stock (Eq, Ord, Show)

type instance FlagsWire AddressCommandFlagBitsKHR = Word32

instance BitPos AddressCommandFlagBitsKHR where
  bitOf = \case
    AddressCommandProtectedBitKHR -> Flags 0x1
    AddressCommandFullyBoundBitKHR -> Flags 0x2
    AddressCommandStorageBufferUsageBitKHR -> Flags 0x4
    AddressCommandUnknownStorageBufferUsageBitKHR -> Flags 0x8
  allBits =
    [ AddressCommandProtectedBitKHR
    , AddressCommandFullyBoundBitKHR
    , AddressCommandStorageBufferUsageBitKHR
    , AddressCommandUnknownStorageBufferUsageBitKHR
    ]

type AddressCommandFlagsKHR = Flags AddressCommandFlagBitsKHR

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDeviceAddressRangeKHR.html VkDeviceAddressRangeKHR>
-- Available: VK_KHR_device_address_commands
data DeviceAddressRangeKHR = DeviceAddressRangeKHR
  { address :: !Word64
  , size :: !Word64
  }
  deriving stock (Eq, Show)

instance Storable DeviceAddressRangeKHR where
  sizeOf _ = 16
  alignment _ = 8
  peek p =
    DeviceAddressRangeKHR
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 8)
  poke p x = do
    pokeByteOff p 0 x.address
    pokeByteOff p 8 x.size

instance CStruct DeviceAddressRangeKHR where
  cSize = 16
  cAlign = 8
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct DeviceAddressRangeKHR where
  peekCStruct = peek

instance Nil DeviceAddressRangeKHR where
  nil =
    DeviceAddressRangeKHR
      { address = 0
      , size = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkAccelerationStructureCreateInfo2KHR.html VkAccelerationStructureCreateInfo2KHR>
-- Available: VK_KHR_acceleration_structure+VK_KHR_device_address_commands
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
data AccelerationStructureCreateInfo2KHR (es :: [Type]) = AccelerationStructureCreateInfo2KHR
  { next :: Chain es
  , createFlags :: AccelerationStructureCreateFlagsKHR
  , addressRange :: DeviceAddressRangeKHR
  , addressFlags :: AddressCommandFlagsKHR
  , type' :: AccelerationStructureTypeKHR
  }

instance
  (AllExtends AccelerationStructureCreateInfo2KHR es, PokeChain es)
  => CStruct (AccelerationStructureCreateInfo2KHR es)
  where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000318015 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.createFlags)
    liftIO (pokeByteOff p 24 x.addressRange)
    liftIO (pokeByteOff p 40 x.addressFlags)
    liftIO (pokeByteOff p 44 (toWire x.type'))
    pure ()

instance
  (AllExtends AccelerationStructureCreateInfo2KHR es, PokeChain es, PeekChain es)
  => FromCStruct (AccelerationStructureCreateInfo2KHR es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_createFlags <- (peekByteOff p 16)
    v_addressRange <- (peekByteOff p 24)
    v_addressFlags <- (peekByteOff p 40)
    v_type' <- (unsafeFromWire <$> peekByteOff p 44)
    pure
      AccelerationStructureCreateInfo2KHR
        { next = v_next
        , createFlags = v_createFlags
        , addressRange = v_addressRange
        , addressFlags = v_addressFlags
        , type' = v_type'
        }

instance (NilChain es) => Nil (AccelerationStructureCreateInfo2KHR es) where
  nil =
    AccelerationStructureCreateInfo2KHR
      { next = nilChain
      , createFlags = mempty
      , addressRange = nil
      , addressFlags = mempty
      , type' = AccelerationStructureTypeTopLevelKHR
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMemoryRangeBarrierKHR.html VkMemoryRangeBarrierKHR>
-- Available: VK_KHR_device_address_commands
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data MemoryRangeBarrierKHR = MemoryRangeBarrierKHR
  { srcStageMask :: PipelineStageFlags2
  , srcAccessMask :: AccessFlags2
  , dstStageMask :: PipelineStageFlags2
  , dstAccessMask :: AccessFlags2
  , srcQueueFamilyIndex :: Word32
  , dstQueueFamilyIndex :: Word32
  , addressRange :: DeviceAddressRangeKHR
  , addressFlags :: AddressCommandFlagsKHR
  }

instance CStruct MemoryRangeBarrierKHR where
  cSize = 80
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000318005 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.srcStageMask)
    liftIO (pokeByteOff p 24 x.srcAccessMask)
    liftIO (pokeByteOff p 32 x.dstStageMask)
    liftIO (pokeByteOff p 40 x.dstAccessMask)
    liftIO (pokeByteOff p 48 x.srcQueueFamilyIndex)
    liftIO (pokeByteOff p 52 x.dstQueueFamilyIndex)
    liftIO (pokeByteOff p 56 x.addressRange)
    liftIO (pokeByteOff p 72 x.addressFlags)
    pure ()

instance FromCStruct MemoryRangeBarrierKHR where
  peekCStruct p = do
    v_srcStageMask <- (peekByteOff p 16)
    v_srcAccessMask <- (peekByteOff p 24)
    v_dstStageMask <- (peekByteOff p 32)
    v_dstAccessMask <- (peekByteOff p 40)
    v_srcQueueFamilyIndex <- (peekByteOff p 48)
    v_dstQueueFamilyIndex <- (peekByteOff p 52)
    v_addressRange <- (peekByteOff p 56)
    v_addressFlags <- (peekByteOff p 72)
    pure
      MemoryRangeBarrierKHR
        { srcStageMask = v_srcStageMask
        , srcAccessMask = v_srcAccessMask
        , dstStageMask = v_dstStageMask
        , dstAccessMask = v_dstAccessMask
        , srcQueueFamilyIndex = v_srcQueueFamilyIndex
        , dstQueueFamilyIndex = v_dstQueueFamilyIndex
        , addressRange = v_addressRange
        , addressFlags = v_addressFlags
        }

instance Nil MemoryRangeBarrierKHR where
  nil =
    MemoryRangeBarrierKHR
      { srcStageMask = mempty
      , srcAccessMask = mempty
      , dstStageMask = mempty
      , dstAccessMask = mempty
      , srcQueueFamilyIndex = 0
      , dstQueueFamilyIndex = 0
      , addressRange = nil
      , addressFlags = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMemoryRangeBarriersInfoKHR.html VkMemoryRangeBarriersInfoKHR>
-- Available: VK_KHR_device_address_commands
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @memoryRangeBarrierCount@ is derived from @memoryRangeBarriers@'s length.
data MemoryRangeBarriersInfoKHR (es :: [Type]) = MemoryRangeBarriersInfoKHR
  { next :: Chain es
  , memoryRangeBarriers :: V.Vector MemoryRangeBarrierKHR
  }

instance (AllExtends MemoryRangeBarriersInfoKHR es, PokeChain es) => CStruct (MemoryRangeBarriersInfoKHR es) where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000318004 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    p_memoryRangeBarriers <-
      if V.null x.memoryRangeBarriers then pure nullPtr else newVector x.memoryRangeBarriers
    liftIO (pokeByteOff p 24 p_memoryRangeBarriers)
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.memoryRangeBarriers) :: Word32))
    pure ()

instance
  (AllExtends MemoryRangeBarriersInfoKHR es, PokeChain es, PeekChain es)
  => FromCStruct (MemoryRangeBarriersInfoKHR es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_memoryRangeBarriers <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure MemoryRangeBarriersInfoKHR{next = v_next, memoryRangeBarriers = v_memoryRangeBarriers}

instance (NilChain es) => Nil (MemoryRangeBarriersInfoKHR es) where
  nil =
    MemoryRangeBarriersInfoKHR
      { next = nilChain
      , memoryRangeBarriers = V.empty
      }

instance
  (AllExtends MemoryRangeBarriersInfoKHR es, PokeChain es)
  => Chainable (MemoryRangeBarriersInfoKHR es)
  where
  sTypeWire = 1000318004
  chainOffset = 8
