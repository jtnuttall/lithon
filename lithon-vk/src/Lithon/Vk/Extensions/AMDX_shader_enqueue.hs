-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.AMDX_shader_enqueue (
  ExecutionGraphPipelineCreateInfoAMDX (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.ComputePipelineCommands
import Lithon.Vk.Core10.Enums.PipelineCreateFlagBits
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Extensions.KHR_pipeline_library

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkExecutionGraphPipelineCreateInfoAMDX.html VkExecutionGraphPipelineCreateInfoAMDX>
-- Available: VK_AMDX_shader_enqueue
-- Platform-specific (guarded by @VK_ENABLE_BETA_EXTENSIONS@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @stageCount@ is derived from @stages@'s length.
data ExecutionGraphPipelineCreateInfoAMDX (es :: [Type]) = ExecutionGraphPipelineCreateInfoAMDX
  { next :: Chain es
  , flags :: PipelineCreateFlags
  , stages :: V.Vector (SomeStruct PipelineShaderStageCreateInfo)
  , libraryInfo :: Maybe PipelineLibraryCreateInfoKHR
  , layout :: PipelineLayout
  , basePipelineHandle :: Pipeline
  , basePipelineIndex :: Int32
  }

instance
  (AllExtends ExecutionGraphPipelineCreateInfoAMDX es, PokeChain es)
  => CStruct (ExecutionGraphPipelineCreateInfoAMDX es)
  where
  cSize = 64
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000134003 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.flags)
    p_stages <- arenaBytes (48 * max 1 (V.length x.stages)) 8
    V.imapM_ (\i e -> withSomeStruct e (pokeCStruct (castPtr (p_stages `plusPtr` (i * 48))))) x.stages
    liftIO (pokeByteOff p 24 (if V.null x.stages then nullPtr else (castPtr p_stages :: Ptr ())))
    liftIO (pokeByteOff p 20 (fromIntegral (V.length x.stages) :: Word32))
    p_libraryInfo <- maybe (pure nullPtr) new x.libraryInfo
    liftIO (pokeByteOff p 32 p_libraryInfo)
    liftIO (pokeByteOff p 40 x.layout)
    liftIO (pokeByteOff p 48 x.basePipelineHandle)
    liftIO (pokeByteOff p 56 x.basePipelineIndex)
    pure ()

instance (NilChain es) => Nil (ExecutionGraphPipelineCreateInfoAMDX es) where
  nil =
    ExecutionGraphPipelineCreateInfoAMDX
      { next = nilChain
      , flags = mempty
      , stages = V.empty
      , libraryInfo = Nothing
      , layout = nil
      , basePipelineHandle = nil
      , basePipelineIndex = 0
      }
