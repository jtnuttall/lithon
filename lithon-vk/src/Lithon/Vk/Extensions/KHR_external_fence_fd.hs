-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_external_fence_fd (
  FenceGetFdInfoKHR (..),
  ImportFenceFdInfoKHR (..),
  getFenceFdKHR,
  importFenceFdKHR,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core11.Enums.ExternalFenceHandleTypeFlagBits
import Lithon.Vk.Core11.Enums.FenceImportFlagBits
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkFenceGetFdInfoKHR.html VkFenceGetFdInfoKHR>
-- Available: VK_KHR_external_fence_fd
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data FenceGetFdInfoKHR = FenceGetFdInfoKHR
  { fence :: Fence
  , handleType :: Flags ExternalFenceHandleTypeFlagBits
  }

instance CStruct FenceGetFdInfoKHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000115001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.fence)
    liftIO (pokeByteOff p 24 x.handleType)
    pure ()

instance FromCStruct FenceGetFdInfoKHR where
  peekCStruct p = do
    v_fence <- (peekByteOff p 16)
    v_handleType <- (peekByteOff p 24)
    pure FenceGetFdInfoKHR{fence = v_fence, handleType = v_handleType}

instance Nil FenceGetFdInfoKHR where
  nil =
    FenceGetFdInfoKHR
      { fence = nil
      , handleType = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkImportFenceFdInfoKHR.html VkImportFenceFdInfoKHR>
-- Available: VK_KHR_external_fence_fd
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data ImportFenceFdInfoKHR = ImportFenceFdInfoKHR
  { fence :: Fence
  , flags :: FenceImportFlags
  , handleType :: Flags ExternalFenceHandleTypeFlagBits
  , fd :: Int32
  }

instance CStruct ImportFenceFdInfoKHR where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000115000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.fence)
    liftIO (pokeByteOff p 24 x.flags)
    liftIO (pokeByteOff p 28 x.handleType)
    liftIO (pokeByteOff p 32 x.fd)
    pure ()

instance FromCStruct ImportFenceFdInfoKHR where
  peekCStruct p = do
    v_fence <- (peekByteOff p 16)
    v_flags <- (peekByteOff p 24)
    v_handleType <- (peekByteOff p 28)
    v_fd <- (peekByteOff p 32)
    pure ImportFenceFdInfoKHR{fence = v_fence, flags = v_flags, handleType = v_handleType, fd = v_fd}

instance Nil ImportFenceFdInfoKHR where
  nil =
    ImportFenceFdInfoKHR
      { fence = nil
      , flags = mempty
      , handleType = mempty
      , fd = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetFenceFdKHR.html vkGetFenceFdKHR>
-- Available: VK_KHR_external_fence_fd
getFenceFdKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> FenceGetFdInfoKHR
  -> m (Outcome Int32)
getFenceFdKHR device getFdInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_getFdInfo :: Ptr () <- castPtr <$> new getFdInfo
    p_o_fd <- arenaBytes 4 4
    r_ <-
      liftIO
        ( mkGetFenceFdKHR
            (checkCommandPtr "vkGetFenceFdKHR" device.cmds.pGetFenceFdKHR)
            (castPtr device.handle)
            p_getFdInfo
            (castPtr p_o_fd)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_fd <- liftIO (peekByteOff p_o_fd 0 :: IO Int32)
      let o_fd = w_o_fd
      pure (Ok (openFromWire r_) o_fd)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkImportFenceFdKHR.html vkImportFenceFdKHR>
-- Available: VK_KHR_external_fence_fd
importFenceFdKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> ImportFenceFdInfoKHR
  -> m (Outcome ())
importFenceFdKHR device importFenceFdInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_importFenceFdInfo :: Ptr () <- castPtr <$> new importFenceFdInfo
    r_ <-
      liftIO
        ( mkImportFenceFdKHR
            (checkCommandPtr "vkImportFenceFdKHR" device.cmds.pImportFenceFdKHR)
            (castPtr device.handle)
            p_importFenceFdInfo
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
