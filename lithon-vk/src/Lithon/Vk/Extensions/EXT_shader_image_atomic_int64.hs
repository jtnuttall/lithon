-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_shader_image_atomic_int64 (
  PhysicalDeviceShaderImageAtomicInt64FeaturesEXT (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceShaderImageAtomicInt64FeaturesEXT.html VkPhysicalDeviceShaderImageAtomicInt64FeaturesEXT>
-- Available: VK_EXT_shader_image_atomic_int64
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceShaderImageAtomicInt64FeaturesEXT = PhysicalDeviceShaderImageAtomicInt64FeaturesEXT
  { shaderImageInt64Atomics :: Bool
  , sparseImageInt64Atomics :: Bool
  }

instance CStruct PhysicalDeviceShaderImageAtomicInt64FeaturesEXT where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000234000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.shaderImageInt64Atomics then TRUE else FALSE))
    liftIO (pokeByteOff p 20 (if x.sparseImageInt64Atomics then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceShaderImageAtomicInt64FeaturesEXT where
  peekCStruct p = do
    v_shaderImageInt64Atomics <- (toBool <$> peekByteOff p 16)
    v_sparseImageInt64Atomics <- (toBool <$> peekByteOff p 20)
    pure
      PhysicalDeviceShaderImageAtomicInt64FeaturesEXT
        { shaderImageInt64Atomics = v_shaderImageInt64Atomics
        , sparseImageInt64Atomics = v_sparseImageInt64Atomics
        }

instance Nil PhysicalDeviceShaderImageAtomicInt64FeaturesEXT where
  nil =
    PhysicalDeviceShaderImageAtomicInt64FeaturesEXT
      { shaderImageInt64Atomics = False
      , sparseImageInt64Atomics = False
      }

instance Chainable PhysicalDeviceShaderImageAtomicInt64FeaturesEXT where
  sTypeWire = 1000234000
  chainOffset = 8
