-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.AMDX_dense_geometry_format (
  CompressedTriangleFormatAMDX (..),
  AccelerationStructureDenseGeometryFormatTrianglesDataAMDX (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Extensions.KHR_acceleration_structure

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkCompressedTriangleFormatAMDX.html VkCompressedTriangleFormatAMDX>
-- Available: VK_AMDX_dense_geometry_format
data CompressedTriangleFormatAMDX
  = CompressedTriangleFormatDgf1AMDX
  deriving stock (Eq, Ord, Show)

instance HasWire CompressedTriangleFormatAMDX where
  toWire = \case
    CompressedTriangleFormatDgf1AMDX -> 0
  fromWire = \case
    0 -> Just CompressedTriangleFormatDgf1AMDX
    _ -> Nothing

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkAccelerationStructureDenseGeometryFormatTrianglesDataAMDX.html VkAccelerationStructureDenseGeometryFormatTrianglesDataAMDX>
-- Available: VK_AMDX_dense_geometry_format
-- Platform-specific (guarded by @VK_ENABLE_BETA_EXTENSIONS@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
data AccelerationStructureDenseGeometryFormatTrianglesDataAMDX (es :: [Type]) = AccelerationStructureDenseGeometryFormatTrianglesDataAMDX
  { next :: Chain es
  , compressedData :: DeviceOrHostAddressConstKHR
  , dataSize :: Word64
  , numTriangles :: Word32
  , numVertices :: Word32
  , maxPrimitiveIndex :: Word32
  , maxGeometryIndex :: Word32
  , format :: CompressedTriangleFormatAMDX
  }

instance
  (AllExtends AccelerationStructureDenseGeometryFormatTrianglesDataAMDX es, PokeChain es)
  => CStruct (AccelerationStructureDenseGeometryFormatTrianglesDataAMDX es)
  where
  cSize = 56
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000478001 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    pokeCStruct (castPtr (p `plusPtr` 16)) x.compressedData
    liftIO (pokeByteOff p 24 x.dataSize)
    liftIO (pokeByteOff p 32 x.numTriangles)
    liftIO (pokeByteOff p 36 x.numVertices)
    liftIO (pokeByteOff p 40 x.maxPrimitiveIndex)
    liftIO (pokeByteOff p 44 x.maxGeometryIndex)
    liftIO (pokeByteOff p 48 (toWire x.format))
    pure ()

instance
  ( AllExtends AccelerationStructureDenseGeometryFormatTrianglesDataAMDX es
  , PokeChain es
  , PeekChain es
  )
  => FromCStruct (AccelerationStructureDenseGeometryFormatTrianglesDataAMDX es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_compressedData <- peekCStruct (castPtr (p `plusPtr` 16))
    v_dataSize <- (peekByteOff p 24)
    v_numTriangles <- (peekByteOff p 32)
    v_numVertices <- (peekByteOff p 36)
    v_maxPrimitiveIndex <- (peekByteOff p 40)
    v_maxGeometryIndex <- (peekByteOff p 44)
    v_format <- (unsafeFromWire <$> peekByteOff p 48)
    pure
      AccelerationStructureDenseGeometryFormatTrianglesDataAMDX
        { next = v_next
        , compressedData = v_compressedData
        , dataSize = v_dataSize
        , numTriangles = v_numTriangles
        , numVertices = v_numVertices
        , maxPrimitiveIndex = v_maxPrimitiveIndex
        , maxGeometryIndex = v_maxGeometryIndex
        , format = v_format
        }

instance (NilChain es) => Nil (AccelerationStructureDenseGeometryFormatTrianglesDataAMDX es) where
  nil =
    AccelerationStructureDenseGeometryFormatTrianglesDataAMDX
      { next = nilChain
      , compressedData = nil
      , dataSize = 0
      , numTriangles = 0
      , numVertices = 0
      , maxPrimitiveIndex = 0
      , maxGeometryIndex = 0
      , format = CompressedTriangleFormatDgf1AMDX
      }

instance
  (AllExtends AccelerationStructureDenseGeometryFormatTrianglesDataAMDX es, PokeChain es)
  => Chainable (AccelerationStructureDenseGeometryFormatTrianglesDataAMDX es)
  where
  sTypeWire = 1000478001
  chainOffset = 8
