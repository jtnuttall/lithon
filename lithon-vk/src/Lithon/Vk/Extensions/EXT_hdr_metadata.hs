-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_hdr_metadata (
  XYColorEXT (..),
  HdrMetadataEXT (..),
  setHdrMetadataEXT,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable (..), peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Extensions.KHR_swapchain
import Lithon.Vk.Result (Outcome (..))

-- | Chromaticity coordinate
-- Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkXYColorEXT.html VkXYColorEXT>
-- Available: VK_EXT_hdr_metadata
data XYColorEXT = XYColorEXT
  { x :: !Float
  , y :: !Float
  }
  deriving stock (Eq, Show)

instance Storable XYColorEXT where
  sizeOf _ = 8
  alignment _ = 4
  peek p =
    XYColorEXT
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 4)
  poke p x = do
    pokeByteOff p 0 x.x
    pokeByteOff p 4 x.y

instance CStruct XYColorEXT where
  cSize = 8
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct XYColorEXT where
  peekCStruct = peek

instance Nil XYColorEXT where
  nil =
    XYColorEXT
      { x = 0
      , y = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkHdrMetadataEXT.html VkHdrMetadataEXT>
-- Available: VK_EXT_hdr_metadata
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data HdrMetadataEXT = HdrMetadataEXT
  { displayPrimaryRed :: XYColorEXT
  , displayPrimaryGreen :: XYColorEXT
  , displayPrimaryBlue :: XYColorEXT
  , whitePoint :: XYColorEXT
  , maxLuminance :: Float
  , minLuminance :: Float
  , maxContentLightLevel :: Float
  , maxFrameAverageLightLevel :: Float
  }

instance CStruct HdrMetadataEXT where
  cSize = 64
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000105000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.displayPrimaryRed)
    liftIO (pokeByteOff p 24 x.displayPrimaryGreen)
    liftIO (pokeByteOff p 32 x.displayPrimaryBlue)
    liftIO (pokeByteOff p 40 x.whitePoint)
    liftIO (pokeByteOff p 48 x.maxLuminance)
    liftIO (pokeByteOff p 52 x.minLuminance)
    liftIO (pokeByteOff p 56 x.maxContentLightLevel)
    liftIO (pokeByteOff p 60 x.maxFrameAverageLightLevel)
    pure ()

instance FromCStruct HdrMetadataEXT where
  peekCStruct p = do
    v_displayPrimaryRed <- (peekByteOff p 16)
    v_displayPrimaryGreen <- (peekByteOff p 24)
    v_displayPrimaryBlue <- (peekByteOff p 32)
    v_whitePoint <- (peekByteOff p 40)
    v_maxLuminance <- (peekByteOff p 48)
    v_minLuminance <- (peekByteOff p 52)
    v_maxContentLightLevel <- (peekByteOff p 56)
    v_maxFrameAverageLightLevel <- (peekByteOff p 60)
    pure
      HdrMetadataEXT
        { displayPrimaryRed = v_displayPrimaryRed
        , displayPrimaryGreen = v_displayPrimaryGreen
        , displayPrimaryBlue = v_displayPrimaryBlue
        , whitePoint = v_whitePoint
        , maxLuminance = v_maxLuminance
        , minLuminance = v_minLuminance
        , maxContentLightLevel = v_maxContentLightLevel
        , maxFrameAverageLightLevel = v_maxFrameAverageLightLevel
        }

instance Nil HdrMetadataEXT where
  nil =
    HdrMetadataEXT
      { displayPrimaryRed = nil
      , displayPrimaryGreen = nil
      , displayPrimaryBlue = nil
      , whitePoint = nil
      , maxLuminance = 0
      , minLuminance = 0
      , maxContentLightLevel = 0
      , maxFrameAverageLightLevel = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkSetHdrMetadataEXT.html vkSetHdrMetadataEXT>
-- Available: VK_EXT_hdr_metadata
setHdrMetadataEXT
  :: forall m
   . (MonadIO m)
  => Device
  -> V.Vector SwapchainKHR
  -> V.Vector HdrMetadataEXT
  -> m ()
setHdrMetadataEXT device swapchains metadata =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_swapchainCount = fromIntegral (V.length swapchains) :: Word32
    p_swapchains <- do
      pv_ <- arenaBytes (8 * max 1 (V.length swapchains)) 8
      liftIO (V.imapM_ (\i v_e -> pokeByteOff pv_ (i * 8) (case v_e of SwapchainKHR w_ -> w_)) swapchains)
      pure (castPtr pv_ :: Ptr ())
    p_metadata <- do
      unless
        (V.length metadata == fromIntegral n_swapchainCount)
        (error "lithon: vkSetHdrMetadataEXT: metadata length must match the count source")
      pv_ <- newVector metadata
      pure (castPtr pv_ :: Ptr ())
    liftIO
      ( mkSetHdrMetadataEXT
          (checkCommandPtr "vkSetHdrMetadataEXT" device.cmds.pSetHdrMetadataEXT)
          (castPtr device.handle)
          n_swapchainCount
          p_swapchains
          p_metadata
      )
    pure ()
