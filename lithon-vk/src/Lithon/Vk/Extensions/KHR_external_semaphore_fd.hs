-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_external_semaphore_fd (
  ImportSemaphoreFdInfoKHR (..),
  SemaphoreGetFdInfoKHR (..),
  getSemaphoreFdKHR,
  importSemaphoreFdKHR,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core11.Enums.ExternalSemaphoreHandleTypeFlagBits
import Lithon.Vk.Core11.Enums.SemaphoreImportFlagBits
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkImportSemaphoreFdInfoKHR.html VkImportSemaphoreFdInfoKHR>
-- Available: VK_KHR_external_semaphore_fd
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data ImportSemaphoreFdInfoKHR = ImportSemaphoreFdInfoKHR
  { semaphore :: Semaphore
  , flags :: SemaphoreImportFlags
  , handleType :: Flags ExternalSemaphoreHandleTypeFlagBits
  , fd :: Int32
  }

instance CStruct ImportSemaphoreFdInfoKHR where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000079000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.semaphore)
    liftIO (pokeByteOff p 24 x.flags)
    liftIO (pokeByteOff p 28 x.handleType)
    liftIO (pokeByteOff p 32 x.fd)
    pure ()

instance FromCStruct ImportSemaphoreFdInfoKHR where
  peekCStruct p = do
    v_semaphore <- (peekByteOff p 16)
    v_flags <- (peekByteOff p 24)
    v_handleType <- (peekByteOff p 28)
    v_fd <- (peekByteOff p 32)
    pure
      ImportSemaphoreFdInfoKHR
        { semaphore = v_semaphore
        , flags = v_flags
        , handleType = v_handleType
        , fd = v_fd
        }

instance Nil ImportSemaphoreFdInfoKHR where
  nil =
    ImportSemaphoreFdInfoKHR
      { semaphore = nil
      , flags = mempty
      , handleType = mempty
      , fd = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSemaphoreGetFdInfoKHR.html VkSemaphoreGetFdInfoKHR>
-- Available: VK_KHR_external_semaphore_fd
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data SemaphoreGetFdInfoKHR = SemaphoreGetFdInfoKHR
  { semaphore :: Semaphore
  , handleType :: Flags ExternalSemaphoreHandleTypeFlagBits
  }

instance CStruct SemaphoreGetFdInfoKHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000079001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.semaphore)
    liftIO (pokeByteOff p 24 x.handleType)
    pure ()

instance FromCStruct SemaphoreGetFdInfoKHR where
  peekCStruct p = do
    v_semaphore <- (peekByteOff p 16)
    v_handleType <- (peekByteOff p 24)
    pure SemaphoreGetFdInfoKHR{semaphore = v_semaphore, handleType = v_handleType}

instance Nil SemaphoreGetFdInfoKHR where
  nil =
    SemaphoreGetFdInfoKHR
      { semaphore = nil
      , handleType = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetSemaphoreFdKHR.html vkGetSemaphoreFdKHR>
-- Available: VK_KHR_external_semaphore_fd
getSemaphoreFdKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> SemaphoreGetFdInfoKHR
  -> m (Outcome Int32)
getSemaphoreFdKHR device getFdInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_getFdInfo :: Ptr () <- castPtr <$> new getFdInfo
    p_o_fd <- arenaBytes 4 4
    r_ <-
      liftIO
        ( mkGetSemaphoreFdKHR
            (checkCommandPtr "vkGetSemaphoreFdKHR" device.cmds.pGetSemaphoreFdKHR)
            (castPtr device.handle)
            p_getFdInfo
            (castPtr p_o_fd)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_fd <- liftIO (peekByteOff p_o_fd 0 :: IO Int32)
      let o_fd = w_o_fd
      pure (Ok (openFromWire r_) o_fd)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkImportSemaphoreFdKHR.html vkImportSemaphoreFdKHR>
-- Available: VK_KHR_external_semaphore_fd
importSemaphoreFdKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> ImportSemaphoreFdInfoKHR
  -> m (Outcome ())
importSemaphoreFdKHR device importSemaphoreFdInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_importSemaphoreFdInfo :: Ptr () <- castPtr <$> new importSemaphoreFdInfo
    r_ <-
      liftIO
        ( mkImportSemaphoreFdKHR
            (checkCommandPtr "vkImportSemaphoreFdKHR" device.cmds.pImportSemaphoreFdKHR)
            (castPtr device.handle)
            p_importSemaphoreFdInfo
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
