-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_swapchain (
  DeviceGroupPresentModeFlagBitsKHR (..),
  SwapchainCreateFlagBitsKHR (..),
  DeviceGroupPresentModeFlagsKHR,
  SwapchainCreateFlagsKHR,
  SwapchainKHR (..),
  AcquireNextImageInfoKHR (..),
  BindImageMemorySwapchainInfoKHR (..),
  DeviceGroupPresentCapabilitiesKHR (..),
  DeviceGroupPresentInfoKHR (..),
  DeviceGroupSwapchainCreateInfoKHR (..),
  ImageSwapchainCreateInfoKHR (..),
  PresentInfoKHR (..),
  SwapchainCreateInfoKHR (..),
  acquireNextImage2KHR,
  acquireNextImageKHR,
  createSwapchainKHR,
  withSwapchainKHR,
  destroySwapchainKHR,
  getDeviceGroupPresentCapabilitiesKHR,
  getDeviceGroupSurfacePresentModesKHR,
  getPhysicalDevicePresentRectanglesKHR,
  getSwapchainImagesKHR,
  queuePresentKHR,
) where

import Control.Exception (bracket)
import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Control.Monad.IO.Unlift (MonadUnliftIO, withRunInIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Primitive.Types (Prim)
import Data.Vector qualified as V
import Data.Vector.Generic qualified as VG
import Data.Vector.Generic.Mutable qualified as VGM
import Data.Vector.Primitive qualified as P
import Data.Vector.Storable.Sized qualified as VSS
import Data.Vector.Unboxed qualified as VU
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable, peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (BitPos (..), Flags (..), FlagsWire)
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)
import Lithon.Core.Unbox (UnboxViaPrim (..))

import Lithon.Vk.Core10.DeviceInitialization
import Lithon.Vk.Core10.Enums.Format
import Lithon.Vk.Core10.Enums.ImageUsageFlagBits
import Lithon.Vk.Core10.Enums.SharingMode
import Lithon.Vk.Core10.FundamentalTypesUsedByManyCommandsAndStructures
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Extensions.KHR_surface
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDeviceGroupPresentModeFlagBitsKHR.html VkDeviceGroupPresentModeFlagBitsKHR>
-- Available: VK_KHR_device_group+VK_KHR_surface,(VK_KHR_swapchain+VK_VERSION_1_1)
data DeviceGroupPresentModeFlagBitsKHR
  = DeviceGroupPresentModeLocalBitKHR
  | DeviceGroupPresentModeRemoteBitKHR
  | DeviceGroupPresentModeSumBitKHR
  | DeviceGroupPresentModeLocalMultiDeviceBitKHR
  deriving stock (Eq, Ord, Show)

type instance FlagsWire DeviceGroupPresentModeFlagBitsKHR = Word32

instance BitPos DeviceGroupPresentModeFlagBitsKHR where
  bitOf = \case
    DeviceGroupPresentModeLocalBitKHR -> Flags 0x1
    DeviceGroupPresentModeRemoteBitKHR -> Flags 0x2
    DeviceGroupPresentModeSumBitKHR -> Flags 0x4
    DeviceGroupPresentModeLocalMultiDeviceBitKHR -> Flags 0x8
  allBits =
    [ DeviceGroupPresentModeLocalBitKHR
    , DeviceGroupPresentModeRemoteBitKHR
    , DeviceGroupPresentModeSumBitKHR
    , DeviceGroupPresentModeLocalMultiDeviceBitKHR
    ]

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSwapchainCreateFlagBitsKHR.html VkSwapchainCreateFlagBitsKHR>
-- Available: VK_KHR_swapchain
data SwapchainCreateFlagBitsKHR
  = SwapchainCreateSplitInstanceBindRegionsBitKHR
  | SwapchainCreateProtectedBitKHR
  deriving stock (Eq, Ord, Show)

type instance FlagsWire SwapchainCreateFlagBitsKHR = Word32

instance BitPos SwapchainCreateFlagBitsKHR where
  bitOf = \case
    SwapchainCreateSplitInstanceBindRegionsBitKHR -> Flags 0x1
    SwapchainCreateProtectedBitKHR -> Flags 0x2
  allBits =
    [ SwapchainCreateSplitInstanceBindRegionsBitKHR
    , SwapchainCreateProtectedBitKHR
    ]

type DeviceGroupPresentModeFlagsKHR = Flags DeviceGroupPresentModeFlagBitsKHR

type SwapchainCreateFlagsKHR = Flags SwapchainCreateFlagBitsKHR

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSwapchainKHR.html VkSwapchainKHR>
-- Available: VK_KHR_swapchain
newtype SwapchainKHR = SwapchainKHR Word64
  deriving newtype (Eq, Ord, Prim, Show, Storable)

newtype instance VU.MVector s SwapchainKHR = MV_SwapchainKHR (P.MVector s SwapchainKHR)

newtype instance VU.Vector SwapchainKHR = V_SwapchainKHR (P.Vector SwapchainKHR)

deriving via (UnboxViaPrim SwapchainKHR) instance VGM.MVector VU.MVector SwapchainKHR

deriving via (UnboxViaPrim SwapchainKHR) instance VG.Vector VU.Vector SwapchainKHR

instance VU.Unbox SwapchainKHR

-- | @VK_NULL_HANDLE@.
instance Nil SwapchainKHR where
  nil = SwapchainKHR 0

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkAcquireNextImageInfoKHR.html VkAcquireNextImageInfoKHR>
-- Available: VK_KHR_device_group+VK_KHR_swapchain,(VK_KHR_swapchain+VK_VERSION_1_1)
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data AcquireNextImageInfoKHR = AcquireNextImageInfoKHR
  { swapchain :: SwapchainKHR
  , timeout :: Word64
  , semaphore :: Semaphore
  , fence :: Fence
  , deviceMask :: Word32
  }

instance CStruct AcquireNextImageInfoKHR where
  cSize = 56
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000060010 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.swapchain)
    liftIO (pokeByteOff p 24 x.timeout)
    liftIO (pokeByteOff p 32 x.semaphore)
    liftIO (pokeByteOff p 40 x.fence)
    liftIO (pokeByteOff p 48 x.deviceMask)
    pure ()

instance FromCStruct AcquireNextImageInfoKHR where
  peekCStruct p = do
    v_swapchain <- (peekByteOff p 16)
    v_timeout <- (peekByteOff p 24)
    v_semaphore <- (peekByteOff p 32)
    v_fence <- (peekByteOff p 40)
    v_deviceMask <- (peekByteOff p 48)
    pure
      AcquireNextImageInfoKHR
        { swapchain = v_swapchain
        , timeout = v_timeout
        , semaphore = v_semaphore
        , fence = v_fence
        , deviceMask = v_deviceMask
        }

instance Nil AcquireNextImageInfoKHR where
  nil =
    AcquireNextImageInfoKHR
      { swapchain = nil
      , timeout = 0
      , semaphore = nil
      , fence = nil
      , deviceMask = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkBindImageMemorySwapchainInfoKHR.html VkBindImageMemorySwapchainInfoKHR>
-- Available: VK_KHR_device_group+VK_KHR_swapchain,(VK_KHR_swapchain+VK_VERSION_1_1)
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data BindImageMemorySwapchainInfoKHR = BindImageMemorySwapchainInfoKHR
  { swapchain :: SwapchainKHR
  , imageIndex :: Word32
  }

instance CStruct BindImageMemorySwapchainInfoKHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000060009 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.swapchain)
    liftIO (pokeByteOff p 24 x.imageIndex)
    pure ()

instance FromCStruct BindImageMemorySwapchainInfoKHR where
  peekCStruct p = do
    v_swapchain <- (peekByteOff p 16)
    v_imageIndex <- (peekByteOff p 24)
    pure BindImageMemorySwapchainInfoKHR{swapchain = v_swapchain, imageIndex = v_imageIndex}

instance Nil BindImageMemorySwapchainInfoKHR where
  nil =
    BindImageMemorySwapchainInfoKHR
      { swapchain = nil
      , imageIndex = 0
      }

instance Chainable BindImageMemorySwapchainInfoKHR where
  sTypeWire = 1000060009
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDeviceGroupPresentCapabilitiesKHR.html VkDeviceGroupPresentCapabilitiesKHR>
-- Available: VK_KHR_device_group+VK_KHR_surface,(VK_KHR_swapchain+VK_VERSION_1_1)
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DeviceGroupPresentCapabilitiesKHR = DeviceGroupPresentCapabilitiesKHR
  { presentMask :: VSS.Vector 32 Word32
  , modes :: DeviceGroupPresentModeFlagsKHR
  }

instance CStruct DeviceGroupPresentCapabilitiesKHR where
  cSize = 152
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000060007 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.presentMask)
    liftIO (pokeByteOff p 144 x.modes)
    pure ()

instance FromCStruct DeviceGroupPresentCapabilitiesKHR where
  peekCStruct p = do
    v_presentMask <- (peekByteOff p 16)
    v_modes <- (peekByteOff p 144)
    pure DeviceGroupPresentCapabilitiesKHR{presentMask = v_presentMask, modes = v_modes}

instance Nil DeviceGroupPresentCapabilitiesKHR where
  nil =
    DeviceGroupPresentCapabilitiesKHR
      { presentMask = VSS.replicate 0
      , modes = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDeviceGroupPresentInfoKHR.html VkDeviceGroupPresentInfoKHR>
-- Available: VK_KHR_device_group+VK_KHR_swapchain,(VK_KHR_swapchain+VK_VERSION_1_1)
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @swapchainCount@ is derived from @deviceMasks@'s length.
data DeviceGroupPresentInfoKHR = DeviceGroupPresentInfoKHR
  { deviceMasks :: V.Vector Word32
  , mode :: Flags DeviceGroupPresentModeFlagBitsKHR
  }

instance CStruct DeviceGroupPresentInfoKHR where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000060011 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    p_deviceMasks <- arenaBytes (4 * max 1 (V.length x.deviceMasks)) 4
    liftIO (V.imapM_ (\i v -> pokeByteOff p_deviceMasks (i * 4) v) x.deviceMasks)
    liftIO
      (pokeByteOff p 24 (if V.null x.deviceMasks then nullPtr else (castPtr p_deviceMasks :: Ptr ())))
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.deviceMasks) :: Word32))
    liftIO (pokeByteOff p 32 x.mode)
    pure ()

instance FromCStruct DeviceGroupPresentInfoKHR where
  peekCStruct p = do
    v_deviceMasks <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 4))
    v_mode <- (peekByteOff p 32)
    pure DeviceGroupPresentInfoKHR{deviceMasks = v_deviceMasks, mode = v_mode}

instance Nil DeviceGroupPresentInfoKHR where
  nil =
    DeviceGroupPresentInfoKHR
      { deviceMasks = V.empty
      , mode = mempty
      }

instance Chainable DeviceGroupPresentInfoKHR where
  sTypeWire = 1000060011
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDeviceGroupSwapchainCreateInfoKHR.html VkDeviceGroupSwapchainCreateInfoKHR>
-- Available: VK_KHR_device_group+VK_KHR_swapchain,(VK_KHR_swapchain+VK_VERSION_1_1)
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DeviceGroupSwapchainCreateInfoKHR = DeviceGroupSwapchainCreateInfoKHR
  { modes :: DeviceGroupPresentModeFlagsKHR
  }

instance CStruct DeviceGroupSwapchainCreateInfoKHR where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000060012 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.modes)
    pure ()

instance FromCStruct DeviceGroupSwapchainCreateInfoKHR where
  peekCStruct p = do
    v_modes <- (peekByteOff p 16)
    pure DeviceGroupSwapchainCreateInfoKHR{modes = v_modes}

instance Nil DeviceGroupSwapchainCreateInfoKHR where
  nil =
    DeviceGroupSwapchainCreateInfoKHR
      { modes = mempty
      }

instance Chainable DeviceGroupSwapchainCreateInfoKHR where
  sTypeWire = 1000060012
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkImageSwapchainCreateInfoKHR.html VkImageSwapchainCreateInfoKHR>
-- Available: VK_KHR_device_group+VK_KHR_swapchain,(VK_KHR_swapchain+VK_VERSION_1_1)
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data ImageSwapchainCreateInfoKHR = ImageSwapchainCreateInfoKHR
  { swapchain :: SwapchainKHR
  }

instance CStruct ImageSwapchainCreateInfoKHR where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000060008 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.swapchain)
    pure ()

instance FromCStruct ImageSwapchainCreateInfoKHR where
  peekCStruct p = do
    v_swapchain <- (peekByteOff p 16)
    pure ImageSwapchainCreateInfoKHR{swapchain = v_swapchain}

instance Nil ImageSwapchainCreateInfoKHR where
  nil =
    ImageSwapchainCreateInfoKHR
      { swapchain = nil
      }

instance Chainable ImageSwapchainCreateInfoKHR where
  sTypeWire = 1000060008
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPresentInfoKHR.html VkPresentInfoKHR>
-- Available: VK_KHR_swapchain
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @waitSemaphoreCount@ is derived from @waitSemaphores@'s length.
--   * @swapchainCount@ is derived from @swapchains@'s length.
--   * @pResults@ is deliberately un-modelled (raw pointer); see the module notes.
data PresentInfoKHR (es :: [Type]) = PresentInfoKHR
  { next :: Chain es
  , waitSemaphores :: V.Vector Semaphore
  , swapchains :: V.Vector SwapchainKHR
  , imageIndices :: V.Vector Word32
  , pResults :: Ptr ()
  }

instance (AllExtends PresentInfoKHR es, PokeChain es) => CStruct (PresentInfoKHR es) where
  cSize = 64
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000001001 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    p_waitSemaphores <- arenaBytes (8 * max 1 (V.length x.waitSemaphores)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_waitSemaphores (i * 8) v) x.waitSemaphores)
    liftIO
      (pokeByteOff p 24 (if V.null x.waitSemaphores then nullPtr else (castPtr p_waitSemaphores :: Ptr ())))
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.waitSemaphores) :: Word32))
    p_swapchains <- arenaBytes (8 * max 1 (V.length x.swapchains)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_swapchains (i * 8) v) x.swapchains)
    liftIO
      (pokeByteOff p 40 (if V.null x.swapchains then nullPtr else (castPtr p_swapchains :: Ptr ())))
    liftIO (pokeByteOff p 32 (fromIntegral (V.length x.swapchains) :: Word32))
    p_imageIndices <- arenaBytes (4 * max 1 (V.length x.imageIndices)) 4
    liftIO (V.imapM_ (\i v -> pokeByteOff p_imageIndices (i * 4) v) x.imageIndices)
    liftIO
      (pokeByteOff p 48 (if V.null x.imageIndices then nullPtr else (castPtr p_imageIndices :: Ptr ())))
    unless
      (V.length x.imageIndices == V.length x.swapchains)
      (error "lithon: imageIndices length must match swapchains")
    liftIO (pokeByteOff p 56 x.pResults)
    pure ()

instance (AllExtends PresentInfoKHR es, PokeChain es, PeekChain es) => FromCStruct (PresentInfoKHR es) where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_waitSemaphores <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_swapchains <- do
      n <- (peekByteOff p 32 :: IO Word32)
      q <- peekByteOff p 40 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_imageIndices <- do
      n <- (peekByteOff p 32 :: IO Word32)
      q <- peekByteOff p 48 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 4))
    v_pResults <- peekByteOff p 56
    pure
      PresentInfoKHR
        { next = v_next
        , waitSemaphores = v_waitSemaphores
        , swapchains = v_swapchains
        , imageIndices = v_imageIndices
        , pResults = v_pResults
        }

instance (NilChain es) => Nil (PresentInfoKHR es) where
  nil =
    PresentInfoKHR
      { next = nilChain
      , waitSemaphores = V.empty
      , swapchains = V.empty
      , imageIndices = V.empty
      , pResults = nullPtr
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSwapchainCreateInfoKHR.html VkSwapchainCreateInfoKHR>
-- Available: VK_KHR_swapchain
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @queueFamilyIndexCount@ is derived from @queueFamilyIndices@'s length.
data SwapchainCreateInfoKHR (es :: [Type]) = SwapchainCreateInfoKHR
  { next :: Chain es
  , flags :: SwapchainCreateFlagsKHR
  , surface :: SurfaceKHR
  , minImageCount :: Word32
  , imageFormat :: Open Format
  , imageColorSpace :: Open ColorSpaceKHR
  , imageExtent :: Extent2D
  , imageArrayLayers :: Word32
  , imageUsage :: ImageUsageFlags
  , imageSharingMode :: SharingMode
  , queueFamilyIndices :: V.Vector Word32
  , preTransform :: Flags SurfaceTransformFlagBitsKHR
  , compositeAlpha :: Flags CompositeAlphaFlagBitsKHR
  , presentMode :: Open PresentModeKHR
  , clipped :: Bool
  , oldSwapchain :: SwapchainKHR
  }

instance (AllExtends SwapchainCreateInfoKHR es, PokeChain es) => CStruct (SwapchainCreateInfoKHR es) where
  cSize = 104
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000001000 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.surface)
    liftIO (pokeByteOff p 32 x.minImageCount)
    liftIO (pokeByteOff p 36 (openToWire x.imageFormat))
    liftIO (pokeByteOff p 40 (openToWire x.imageColorSpace))
    liftIO (pokeByteOff p 44 x.imageExtent)
    liftIO (pokeByteOff p 52 x.imageArrayLayers)
    liftIO (pokeByteOff p 56 x.imageUsage)
    liftIO (pokeByteOff p 60 (toWire x.imageSharingMode))
    p_queueFamilyIndices <- arenaBytes (4 * max 1 (V.length x.queueFamilyIndices)) 4
    liftIO (V.imapM_ (\i v -> pokeByteOff p_queueFamilyIndices (i * 4) v) x.queueFamilyIndices)
    liftIO
      ( pokeByteOff
          p
          72
          (if V.null x.queueFamilyIndices then nullPtr else (castPtr p_queueFamilyIndices :: Ptr ()))
      )
    liftIO (pokeByteOff p 64 (fromIntegral (V.length x.queueFamilyIndices) :: Word32))
    liftIO (pokeByteOff p 80 x.preTransform)
    liftIO (pokeByteOff p 84 x.compositeAlpha)
    liftIO (pokeByteOff p 88 (openToWire x.presentMode))
    liftIO (pokeByteOff p 92 (if x.clipped then TRUE else FALSE))
    liftIO (pokeByteOff p 96 x.oldSwapchain)
    pure ()

instance
  (AllExtends SwapchainCreateInfoKHR es, PokeChain es, PeekChain es)
  => FromCStruct (SwapchainCreateInfoKHR es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_flags <- (peekByteOff p 16)
    v_surface <- (peekByteOff p 24)
    v_minImageCount <- (peekByteOff p 32)
    v_imageFormat <- (openFromWire <$> peekByteOff p 36)
    v_imageColorSpace <- (openFromWire <$> peekByteOff p 40)
    v_imageExtent <- (peekByteOff p 44)
    v_imageArrayLayers <- (peekByteOff p 52)
    v_imageUsage <- (peekByteOff p 56)
    v_imageSharingMode <- (unsafeFromWire <$> peekByteOff p 60)
    v_queueFamilyIndices <- do
      n <- (peekByteOff p 64 :: IO Word32)
      q <- peekByteOff p 72 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 4))
    v_preTransform <- (peekByteOff p 80)
    v_compositeAlpha <- (peekByteOff p 84)
    v_presentMode <- (openFromWire <$> peekByteOff p 88)
    v_clipped <- (toBool <$> peekByteOff p 92)
    v_oldSwapchain <- (peekByteOff p 96)
    pure
      SwapchainCreateInfoKHR
        { next = v_next
        , flags = v_flags
        , surface = v_surface
        , minImageCount = v_minImageCount
        , imageFormat = v_imageFormat
        , imageColorSpace = v_imageColorSpace
        , imageExtent = v_imageExtent
        , imageArrayLayers = v_imageArrayLayers
        , imageUsage = v_imageUsage
        , imageSharingMode = v_imageSharingMode
        , queueFamilyIndices = v_queueFamilyIndices
        , preTransform = v_preTransform
        , compositeAlpha = v_compositeAlpha
        , presentMode = v_presentMode
        , clipped = v_clipped
        , oldSwapchain = v_oldSwapchain
        }

instance (NilChain es) => Nil (SwapchainCreateInfoKHR es) where
  nil =
    SwapchainCreateInfoKHR
      { next = nilChain
      , flags = mempty
      , surface = nil
      , minImageCount = 0
      , imageFormat = Known FormatUndefined
      , imageColorSpace = Known ColorSpaceSrgbNonlinearKHR
      , imageExtent = nil
      , imageArrayLayers = 0
      , imageUsage = mempty
      , imageSharingMode = SharingModeExclusive
      , queueFamilyIndices = V.empty
      , preTransform = mempty
      , compositeAlpha = mempty
      , presentMode = Known PresentModeImmediateKHR
      , clipped = False
      , oldSwapchain = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkAcquireNextImage2KHR.html vkAcquireNextImage2KHR>
-- Available: VK_KHR_device_group+VK_KHR_swapchain,(VK_KHR_swapchain+VK_VERSION_1_1)
acquireNextImage2KHR
  :: forall m
   . (MonadIO m)
  => Device
  -> AcquireNextImageInfoKHR
  -> m (Outcome (Maybe Word32))
  -- ^ 'Nothing' when the driver returns VK_TIMEOUT \/ VK_NOT_READY without writing the out params.
acquireNextImage2KHR device acquireInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_acquireInfo :: Ptr () <- castPtr <$> new acquireInfo
    p_o_imageIndex <- arenaBytes 4 4
    r_ <-
      liftIO
        ( mkAcquireNextImage2KHR
            (checkCommandPtr "vkAcquireNextImage2KHR" device.cmds.pAcquireNextImage2KHR)
            (castPtr device.handle)
            p_acquireInfo
            (castPtr p_o_imageIndex)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else
      -- completed without writing the out params on these codes
      if r_ == 2 || r_ == 1 then
        pure (Ok (openFromWire r_) Nothing)
      else do
        w_o_imageIndex <- liftIO (peekByteOff p_o_imageIndex 0 :: IO Word32)
        let o_imageIndex = w_o_imageIndex
        pure (Ok (openFromWire r_) (Just o_imageIndex))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkAcquireNextImageKHR.html vkAcquireNextImageKHR>
-- Available: VK_KHR_swapchain
acquireNextImageKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> SwapchainKHR
  -> Word64
  -> Semaphore
  -> Fence
  -> m (Outcome (Maybe Word32))
  -- ^ 'Nothing' when the driver returns VK_TIMEOUT \/ VK_NOT_READY without writing the out params.
acquireNextImageKHR device swapchain timeout semaphore fence =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_o_imageIndex <- arenaBytes 4 4
    r_ <-
      liftIO
        ( mkAcquireNextImageKHR
            (checkCommandPtr "vkAcquireNextImageKHR" device.cmds.pAcquireNextImageKHR)
            (castPtr device.handle)
            (case swapchain of SwapchainKHR w_ -> w_)
            timeout
            (case semaphore of Semaphore w_ -> w_)
            (case fence of Fence w_ -> w_)
            (castPtr p_o_imageIndex)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else
      -- completed without writing the out params on these codes
      if r_ == 2 || r_ == 1 then
        pure (Ok (openFromWire r_) Nothing)
      else do
        w_o_imageIndex <- liftIO (peekByteOff p_o_imageIndex 0 :: IO Word32)
        let o_imageIndex = w_o_imageIndex
        pure (Ok (openFromWire r_) (Just o_imageIndex))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateSwapchainKHR.html vkCreateSwapchainKHR>
-- Available: VK_KHR_swapchain
createSwapchainKHR
  :: forall m es1
   . (MonadIO m, AllExtends SwapchainCreateInfoKHR es1, PokeChain es1)
  => Device
  -> SwapchainCreateInfoKHR es1
  -> Maybe AllocationCallbacks
  -> m (Outcome SwapchainKHR)
createSwapchainKHR device createInfo allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_createInfo :: Ptr () <- castPtr <$> new createInfo
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    p_o_swapchain <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkCreateSwapchainKHR
            (checkCommandPtr "vkCreateSwapchainKHR" device.cmds.pCreateSwapchainKHR)
            (castPtr device.handle)
            p_createInfo
            p_allocator
            (castPtr p_o_swapchain)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_swapchain <- liftIO (peekByteOff p_o_swapchain 0 :: IO Word64)
      let o_swapchain = SwapchainKHR w_o_swapchain
      pure (Ok (openFromWire r_) o_swapchain)

withSwapchainKHR
  :: forall m r es1
   . (MonadUnliftIO m, AllExtends SwapchainCreateInfoKHR es1, PokeChain es1)
  => Device
  -> SwapchainCreateInfoKHR es1
  -> Maybe AllocationCallbacks
  -> (Outcome SwapchainKHR -> m r)
  -> m r
withSwapchainKHR device createInfo allocator cont_ =
  withRunInIO \run_ ->
    bracket
      (run_ (createSwapchainKHR device createInfo allocator))
      ( \o_ -> case o_ of
          Ok _ h_ -> run_ (destroySwapchainKHR device h_ allocator)
          Err _ -> pure ()
      )
      (run_ . cont_)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkDestroySwapchainKHR.html vkDestroySwapchainKHR>
-- Available: VK_KHR_swapchain
destroySwapchainKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> SwapchainKHR
  -> Maybe AllocationCallbacks
  -> m ()
destroySwapchainKHR device swapchain allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    liftIO
      ( mkDestroySwapchainKHR
          (checkCommandPtr "vkDestroySwapchainKHR" device.cmds.pDestroySwapchainKHR)
          (castPtr device.handle)
          (case swapchain of SwapchainKHR w_ -> w_)
          p_allocator
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetDeviceGroupPresentCapabilitiesKHR.html vkGetDeviceGroupPresentCapabilitiesKHR>
-- Available: VK_KHR_device_group+VK_KHR_surface,(VK_KHR_swapchain+VK_VERSION_1_1)
getDeviceGroupPresentCapabilitiesKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> m (Outcome DeviceGroupPresentCapabilitiesKHR)
getDeviceGroupPresentCapabilitiesKHR device =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_o_deviceGroupPresentCapabilities :: Ptr () <-
      castPtr <$> new (nil :: DeviceGroupPresentCapabilitiesKHR)
    r_ <-
      liftIO
        ( mkGetDeviceGroupPresentCapabilitiesKHR
            ( checkCommandPtr
                "vkGetDeviceGroupPresentCapabilitiesKHR"
                device.cmds.pGetDeviceGroupPresentCapabilitiesKHR
            )
            (castPtr device.handle)
            p_o_deviceGroupPresentCapabilities
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      o_deviceGroupPresentCapabilities <-
        liftIO (peekCStruct (castPtr p_o_deviceGroupPresentCapabilities))
      pure (Ok (openFromWire r_) o_deviceGroupPresentCapabilities)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetDeviceGroupSurfacePresentModesKHR.html vkGetDeviceGroupSurfacePresentModesKHR>
-- Available: VK_KHR_device_group+VK_KHR_surface,(VK_KHR_swapchain+VK_VERSION_1_1)
getDeviceGroupSurfacePresentModesKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> SurfaceKHR
  -> m (Outcome DeviceGroupPresentModeFlagsKHR)
getDeviceGroupSurfacePresentModesKHR device surface =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_o_modes <- arenaBytes 4 4
    r_ <-
      liftIO
        ( mkGetDeviceGroupSurfacePresentModesKHR
            ( checkCommandPtr
                "vkGetDeviceGroupSurfacePresentModesKHR"
                device.cmds.pGetDeviceGroupSurfacePresentModesKHR
            )
            (castPtr device.handle)
            (case surface of SurfaceKHR w_ -> w_)
            (castPtr p_o_modes)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_modes <- liftIO (peekByteOff p_o_modes 0 :: IO Word32)
      let o_modes = Flags w_o_modes
      pure (Ok (openFromWire r_) o_modes)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetPhysicalDevicePresentRectanglesKHR.html vkGetPhysicalDevicePresentRectanglesKHR>
-- Available: VK_KHR_device_group+VK_KHR_surface,(VK_KHR_swapchain+VK_VERSION_1_1)
getPhysicalDevicePresentRectanglesKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> SurfaceKHR
  -> m (Outcome (V.Vector Rect2D))
getPhysicalDevicePresentRectanglesKHR physicalDevice surface =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetPhysicalDevicePresentRectanglesKHR
                  ( checkCommandPtr
                      "vkGetPhysicalDevicePresentRectanglesKHR"
                      physicalDevice.cmds.pGetPhysicalDevicePresentRectanglesKHR
                  )
                  (castPtr physicalDevice.handle)
                  (case surface of SurfaceKHR w_ -> w_)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pRectCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_rects <- arenaBytes (16 * max 1 (fromIntegral n_pRectCount)) 4
            forM_
              ([0 .. fromIntegral n_pRectCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_rects `plusPtr` (i * 16))) (nil :: Rect2D))
            r2_ <-
              liftIO
                ( mkGetPhysicalDevicePresentRectanglesKHR
                    ( checkCommandPtr
                        "vkGetPhysicalDevicePresentRectanglesKHR"
                        physicalDevice.cmds.pGetPhysicalDevicePresentRectanglesKHR
                    )
                    (castPtr physicalDevice.handle)
                    (case surface of SurfaceKHR w_ -> w_)
                    (castPtr p_n_)
                    (castPtr p_o_rects)
                )
            n_pRectCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_rects <- liftIO (peekElems (fromIntegral n_pRectCount') (castPtr p_o_rects))
                pure (Ok (openFromWire r2_) o_rects)
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetSwapchainImagesKHR.html vkGetSwapchainImagesKHR>
-- Available: VK_KHR_swapchain
getSwapchainImagesKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> SwapchainKHR
  -> m (Outcome (V.Vector Image))
getSwapchainImagesKHR device swapchain =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetSwapchainImagesKHR
                  (checkCommandPtr "vkGetSwapchainImagesKHR" device.cmds.pGetSwapchainImagesKHR)
                  (castPtr device.handle)
                  (case swapchain of SwapchainKHR w_ -> w_)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pSwapchainImageCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_swapchainImages <- arenaBytes (8 * max 1 (fromIntegral n_pSwapchainImageCount)) 8
            r2_ <-
              liftIO
                ( mkGetSwapchainImagesKHR
                    (checkCommandPtr "vkGetSwapchainImagesKHR" device.cmds.pGetSwapchainImagesKHR)
                    (castPtr device.handle)
                    (case swapchain of SwapchainKHR w_ -> w_)
                    (castPtr p_n_)
                    (castPtr p_o_swapchainImages)
                )
            n_pSwapchainImageCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_swapchainImages <-
                  liftIO
                    ( V.generateM
                        (fromIntegral n_pSwapchainImageCount')
                        (\i -> Image <$> peekElemOff (castPtr p_o_swapchainImages :: Ptr Word64) i)
                    )
                pure (Ok (openFromWire r2_) o_swapchainImages)
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkQueuePresentKHR.html vkQueuePresentKHR>
-- Available: VK_KHR_swapchain
queuePresentKHR
  :: forall m es1
   . (MonadIO m, AllExtends PresentInfoKHR es1, PokeChain es1)
  => Queue
  -> PresentInfoKHR es1
  -> m (Outcome ())
queuePresentKHR queue presentInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_presentInfo :: Ptr () <- castPtr <$> new presentInfo
    r_ <-
      liftIO
        ( mkQueuePresentKHR
            (checkCommandPtr "vkQueuePresentKHR" queue.cmds.pQueuePresentKHR)
            (castPtr queue.handle)
            p_presentInfo
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
