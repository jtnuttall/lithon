-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_pageable_device_local_memory (
  PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT (..),
  setDeviceMemoryPriorityEXT,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDevicePageableDeviceLocalMemoryFeaturesEXT.html VkPhysicalDevicePageableDeviceLocalMemoryFeaturesEXT>
-- Available: VK_EXT_pageable_device_local_memory
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT = PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT
  { pageableDeviceLocalMemory :: Bool
  }

instance CStruct PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000412000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.pageableDeviceLocalMemory then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT where
  peekCStruct p = do
    v_pageableDeviceLocalMemory <- (toBool <$> peekByteOff p 16)
    pure
      PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT
        { pageableDeviceLocalMemory = v_pageableDeviceLocalMemory
        }

instance Nil PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT where
  nil =
    PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT
      { pageableDeviceLocalMemory = False
      }

instance Chainable PhysicalDevicePageableDeviceLocalMemoryFeaturesEXT where
  sTypeWire = 1000412000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkSetDeviceMemoryPriorityEXT.html vkSetDeviceMemoryPriorityEXT>
-- Available: VK_EXT_pageable_device_local_memory
setDeviceMemoryPriorityEXT
  :: forall m
   . (MonadIO m)
  => Device
  -> DeviceMemory
  -> Float
  -> m ()
setDeviceMemoryPriorityEXT device memory priority =
  liftIO do
    liftIO
      ( mkSetDeviceMemoryPriorityEXT
          (checkCommandPtr "vkSetDeviceMemoryPriorityEXT" device.cmds.pSetDeviceMemoryPriorityEXT)
          (castPtr device.handle)
          (case memory of DeviceMemory w_ -> w_)
          priority
      )
    pure ()
