-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_external_semaphore_win32 (
  D3D12FenceSubmitInfoKHR (..),
  ExportSemaphoreWin32HandleInfoKHR (..),
  ImportSemaphoreWin32HandleInfoKHR (..),
  SemaphoreGetWin32HandleInfoKHR (..),
  getSemaphoreWin32HandleKHR,
  importSemaphoreWin32HandleKHR,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)
import Lithon.Core.Platform (HANDLE (..), LPCWSTR (..))

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core11.Enums.ExternalSemaphoreHandleTypeFlagBits
import Lithon.Vk.Core11.Enums.SemaphoreImportFlagBits
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkD3D12FenceSubmitInfoKHR.html VkD3D12FenceSubmitInfoKHR>
-- Available: VK_KHR_external_semaphore_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @waitSemaphoreValuesCount@ is derived from @waitSemaphoreValues@'s length.
--   * @signalSemaphoreValuesCount@ is derived from @signalSemaphoreValues@'s length.
data D3D12FenceSubmitInfoKHR = D3D12FenceSubmitInfoKHR
  { waitSemaphoreValues :: V.Vector Word64
  , signalSemaphoreValues :: V.Vector Word64
  }

instance CStruct D3D12FenceSubmitInfoKHR where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000078002 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    p_waitSemaphoreValues <- arenaBytes (8 * max 1 (V.length x.waitSemaphoreValues)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_waitSemaphoreValues (i * 8) v) x.waitSemaphoreValues)
    liftIO
      ( pokeByteOff
          p
          24
          (if V.null x.waitSemaphoreValues then nullPtr else (castPtr p_waitSemaphoreValues :: Ptr ()))
      )
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.waitSemaphoreValues) :: Word32))
    p_signalSemaphoreValues <- arenaBytes (8 * max 1 (V.length x.signalSemaphoreValues)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_signalSemaphoreValues (i * 8) v) x.signalSemaphoreValues)
    liftIO
      ( pokeByteOff
          p
          40
          (if V.null x.signalSemaphoreValues then nullPtr else (castPtr p_signalSemaphoreValues :: Ptr ()))
      )
    liftIO (pokeByteOff p 32 (fromIntegral (V.length x.signalSemaphoreValues) :: Word32))
    pure ()

instance FromCStruct D3D12FenceSubmitInfoKHR where
  peekCStruct p = do
    v_waitSemaphoreValues <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_signalSemaphoreValues <- do
      n <- (peekByteOff p 32 :: IO Word32)
      q <- peekByteOff p 40 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    pure
      D3D12FenceSubmitInfoKHR
        { waitSemaphoreValues = v_waitSemaphoreValues
        , signalSemaphoreValues = v_signalSemaphoreValues
        }

instance Nil D3D12FenceSubmitInfoKHR where
  nil =
    D3D12FenceSubmitInfoKHR
      { waitSemaphoreValues = V.empty
      , signalSemaphoreValues = V.empty
      }

instance Chainable D3D12FenceSubmitInfoKHR where
  sTypeWire = 1000078002
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkExportSemaphoreWin32HandleInfoKHR.html VkExportSemaphoreWin32HandleInfoKHR>
-- Available: VK_KHR_external_semaphore_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @pAttributes@ is deliberately un-modelled (raw pointer); see the module notes.
data ExportSemaphoreWin32HandleInfoKHR = ExportSemaphoreWin32HandleInfoKHR
  { pAttributes :: Ptr ()
  , dwAccess :: Word32
  , name :: LPCWSTR
  }

instance CStruct ExportSemaphoreWin32HandleInfoKHR where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000078001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.pAttributes)
    liftIO (pokeByteOff p 24 x.dwAccess)
    liftIO (pokeByteOff p 32 x.name)
    pure ()

instance FromCStruct ExportSemaphoreWin32HandleInfoKHR where
  peekCStruct p = do
    v_pAttributes <- peekByteOff p 16
    v_dwAccess <- (peekByteOff p 24)
    v_name <- (peekByteOff p 32)
    pure
      ExportSemaphoreWin32HandleInfoKHR{pAttributes = v_pAttributes, dwAccess = v_dwAccess, name = v_name}

instance Nil ExportSemaphoreWin32HandleInfoKHR where
  nil =
    ExportSemaphoreWin32HandleInfoKHR
      { pAttributes = nullPtr
      , dwAccess = 0
      , name = LPCWSTR nullPtr
      }

instance Chainable ExportSemaphoreWin32HandleInfoKHR where
  sTypeWire = 1000078001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkImportSemaphoreWin32HandleInfoKHR.html VkImportSemaphoreWin32HandleInfoKHR>
-- Available: VK_KHR_external_semaphore_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data ImportSemaphoreWin32HandleInfoKHR = ImportSemaphoreWin32HandleInfoKHR
  { semaphore :: Semaphore
  , flags :: SemaphoreImportFlags
  , handleType :: Flags ExternalSemaphoreHandleTypeFlagBits
  , handle :: HANDLE
  , name :: LPCWSTR
  }

instance CStruct ImportSemaphoreWin32HandleInfoKHR where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000078000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.semaphore)
    liftIO (pokeByteOff p 24 x.flags)
    liftIO (pokeByteOff p 28 x.handleType)
    liftIO (pokeByteOff p 32 x.handle)
    liftIO (pokeByteOff p 40 x.name)
    pure ()

instance FromCStruct ImportSemaphoreWin32HandleInfoKHR where
  peekCStruct p = do
    v_semaphore <- (peekByteOff p 16)
    v_flags <- (peekByteOff p 24)
    v_handleType <- (peekByteOff p 28)
    v_handle <- (peekByteOff p 32)
    v_name <- (peekByteOff p 40)
    pure
      ImportSemaphoreWin32HandleInfoKHR
        { semaphore = v_semaphore
        , flags = v_flags
        , handleType = v_handleType
        , handle = v_handle
        , name = v_name
        }

instance Nil ImportSemaphoreWin32HandleInfoKHR where
  nil =
    ImportSemaphoreWin32HandleInfoKHR
      { semaphore = nil
      , flags = mempty
      , handleType = mempty
      , handle = HANDLE nullPtr
      , name = LPCWSTR nullPtr
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSemaphoreGetWin32HandleInfoKHR.html VkSemaphoreGetWin32HandleInfoKHR>
-- Available: VK_KHR_external_semaphore_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data SemaphoreGetWin32HandleInfoKHR = SemaphoreGetWin32HandleInfoKHR
  { semaphore :: Semaphore
  , handleType :: Flags ExternalSemaphoreHandleTypeFlagBits
  }

instance CStruct SemaphoreGetWin32HandleInfoKHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000078003 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.semaphore)
    liftIO (pokeByteOff p 24 x.handleType)
    pure ()

instance FromCStruct SemaphoreGetWin32HandleInfoKHR where
  peekCStruct p = do
    v_semaphore <- (peekByteOff p 16)
    v_handleType <- (peekByteOff p 24)
    pure SemaphoreGetWin32HandleInfoKHR{semaphore = v_semaphore, handleType = v_handleType}

instance Nil SemaphoreGetWin32HandleInfoKHR where
  nil =
    SemaphoreGetWin32HandleInfoKHR
      { semaphore = nil
      , handleType = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetSemaphoreWin32HandleKHR.html vkGetSemaphoreWin32HandleKHR>
-- Available: VK_KHR_external_semaphore_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
getSemaphoreWin32HandleKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> SemaphoreGetWin32HandleInfoKHR
  -> m (Outcome (Ptr ()))
getSemaphoreWin32HandleKHR device getWin32HandleInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_getWin32HandleInfo :: Ptr () <- castPtr <$> new getWin32HandleInfo
    p_o_handle <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkGetSemaphoreWin32HandleKHR
            (checkCommandPtr "vkGetSemaphoreWin32HandleKHR" device.cmds.pGetSemaphoreWin32HandleKHR)
            (castPtr device.handle)
            p_getWin32HandleInfo
            (castPtr p_o_handle)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_handle <- liftIO (peekByteOff p_o_handle 0 :: IO (Ptr ()))
      let o_handle = w_o_handle
      pure (Ok (openFromWire r_) o_handle)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkImportSemaphoreWin32HandleKHR.html vkImportSemaphoreWin32HandleKHR>
-- Available: VK_KHR_external_semaphore_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
importSemaphoreWin32HandleKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> ImportSemaphoreWin32HandleInfoKHR
  -> m (Outcome ())
importSemaphoreWin32HandleKHR device importSemaphoreWin32HandleInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_importSemaphoreWin32HandleInfo :: Ptr () <- castPtr <$> new importSemaphoreWin32HandleInfo
    r_ <-
      liftIO
        ( mkImportSemaphoreWin32HandleKHR
            (checkCommandPtr "vkImportSemaphoreWin32HandleKHR" device.cmds.pImportSemaphoreWin32HandleKHR)
            (castPtr device.handle)
            p_importSemaphoreWin32HandleInfo
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
