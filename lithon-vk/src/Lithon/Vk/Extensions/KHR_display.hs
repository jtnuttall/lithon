-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_display (
  DisplayPlaneAlphaFlagBitsKHR (..),
  DisplayModeCreateFlagsKHR,
  DisplayModeCreateFlagsKHRBits,
  DisplayPlaneAlphaFlagsKHR,
  DisplaySurfaceCreateFlagsKHR,
  DisplaySurfaceCreateFlagsKHRBits,
  DisplayKHR (..),
  DisplayModeKHR (..),
  DisplayModeParametersKHR (..),
  DisplayModePropertiesKHR (..),
  DisplayPlaneCapabilitiesKHR (..),
  DisplayPlanePropertiesKHR (..),
  DisplayModeCreateInfoKHR (..),
  DisplayPropertiesKHR (..),
  DisplaySurfaceCreateInfoKHR (..),
  createDisplayModeKHR,
  createDisplayPlaneSurfaceKHR,
  withDisplayPlaneSurfaceKHR,
  getDisplayModePropertiesKHR,
  getDisplayPlaneCapabilitiesKHR,
  getDisplayPlaneSupportedDisplaysKHR,
  getPhysicalDeviceDisplayPlanePropertiesKHR,
  getPhysicalDeviceDisplayPropertiesKHR,
) where

import Control.Exception (bracket)
import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Control.Monad.IO.Unlift (MonadUnliftIO, withRunInIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Primitive.Types (Prim)
import Data.Vector qualified as V
import Data.Vector.Generic qualified as VG
import Data.Vector.Generic.Mutable qualified as VGM
import Data.Vector.Primitive qualified as P
import Data.Vector.Unboxed qualified as VU
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable (..), peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (BitPos (..), Flags (..), FlagsWire)
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)
import Lithon.Core.Unbox (UnboxViaPrim (..))

import Lithon.Vk.Core10.DeviceInitialization
import Lithon.Vk.Core10.FundamentalTypesUsedByManyCommandsAndStructures
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Extensions.KHR_surface
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayPlaneAlphaFlagBitsKHR.html VkDisplayPlaneAlphaFlagBitsKHR>
-- Available: VK_KHR_display
data DisplayPlaneAlphaFlagBitsKHR
  = DisplayPlaneAlphaOpaqueBitKHR
  | DisplayPlaneAlphaGlobalBitKHR
  | DisplayPlaneAlphaPerPixelBitKHR
  | DisplayPlaneAlphaPerPixelPremultipliedBitKHR
  deriving stock (Eq, Ord, Show)

type instance FlagsWire DisplayPlaneAlphaFlagBitsKHR = Word32

instance BitPos DisplayPlaneAlphaFlagBitsKHR where
  bitOf = \case
    DisplayPlaneAlphaOpaqueBitKHR -> Flags 0x1
    DisplayPlaneAlphaGlobalBitKHR -> Flags 0x2
    DisplayPlaneAlphaPerPixelBitKHR -> Flags 0x4
    DisplayPlaneAlphaPerPixelPremultipliedBitKHR -> Flags 0x8
  allBits =
    [ DisplayPlaneAlphaOpaqueBitKHR
    , DisplayPlaneAlphaGlobalBitKHR
    , DisplayPlaneAlphaPerPixelBitKHR
    , DisplayPlaneAlphaPerPixelPremultipliedBitKHR
    ]

-- | Reserved-empty mask: no bits are defined yet.
data DisplayModeCreateFlagsKHRBits
  deriving stock (Eq, Ord, Show)

type instance FlagsWire DisplayModeCreateFlagsKHRBits = Word32

instance BitPos DisplayModeCreateFlagsKHRBits where
  bitOf = \case {}
  allBits = []

type DisplayModeCreateFlagsKHR = Flags DisplayModeCreateFlagsKHRBits

type DisplayPlaneAlphaFlagsKHR = Flags DisplayPlaneAlphaFlagBitsKHR

-- | Reserved-empty mask: no bits are defined yet.
data DisplaySurfaceCreateFlagsKHRBits
  deriving stock (Eq, Ord, Show)

type instance FlagsWire DisplaySurfaceCreateFlagsKHRBits = Word32

instance BitPos DisplaySurfaceCreateFlagsKHRBits where
  bitOf = \case {}
  allBits = []

type DisplaySurfaceCreateFlagsKHR = Flags DisplaySurfaceCreateFlagsKHRBits

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayKHR.html VkDisplayKHR>
-- Available: VK_KHR_display
newtype DisplayKHR = DisplayKHR Word64
  deriving newtype (Eq, Ord, Prim, Show, Storable)

newtype instance VU.MVector s DisplayKHR = MV_DisplayKHR (P.MVector s DisplayKHR)

newtype instance VU.Vector DisplayKHR = V_DisplayKHR (P.Vector DisplayKHR)

deriving via (UnboxViaPrim DisplayKHR) instance VGM.MVector VU.MVector DisplayKHR

deriving via (UnboxViaPrim DisplayKHR) instance VG.Vector VU.Vector DisplayKHR

instance VU.Unbox DisplayKHR

-- | @VK_NULL_HANDLE@.
instance Nil DisplayKHR where
  nil = DisplayKHR 0

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayModeKHR.html VkDisplayModeKHR>
-- Available: VK_KHR_display
newtype DisplayModeKHR = DisplayModeKHR Word64
  deriving newtype (Eq, Ord, Prim, Show, Storable)

newtype instance VU.MVector s DisplayModeKHR = MV_DisplayModeKHR (P.MVector s DisplayModeKHR)

newtype instance VU.Vector DisplayModeKHR = V_DisplayModeKHR (P.Vector DisplayModeKHR)

deriving via (UnboxViaPrim DisplayModeKHR) instance VGM.MVector VU.MVector DisplayModeKHR

deriving via (UnboxViaPrim DisplayModeKHR) instance VG.Vector VU.Vector DisplayModeKHR

instance VU.Unbox DisplayModeKHR

-- | @VK_NULL_HANDLE@.
instance Nil DisplayModeKHR where
  nil = DisplayModeKHR 0

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayModeParametersKHR.html VkDisplayModeParametersKHR>
-- Available: VK_KHR_display
data DisplayModeParametersKHR = DisplayModeParametersKHR
  { visibleRegion :: !Extent2D
  , refreshRate :: !Word32
  }
  deriving stock (Eq, Show)

instance Storable DisplayModeParametersKHR where
  sizeOf _ = 12
  alignment _ = 4
  peek p =
    DisplayModeParametersKHR
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 8)
  poke p x = do
    pokeByteOff p 0 x.visibleRegion
    pokeByteOff p 8 x.refreshRate

instance CStruct DisplayModeParametersKHR where
  cSize = 12
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct DisplayModeParametersKHR where
  peekCStruct = peek

instance Nil DisplayModeParametersKHR where
  nil =
    DisplayModeParametersKHR
      { visibleRegion = nil
      , refreshRate = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayModePropertiesKHR.html VkDisplayModePropertiesKHR>
-- Available: VK_KHR_display
data DisplayModePropertiesKHR = DisplayModePropertiesKHR
  { displayMode :: !DisplayModeKHR
  , parameters :: !DisplayModeParametersKHR
  }
  deriving stock (Eq, Show)

instance Storable DisplayModePropertiesKHR where
  sizeOf _ = 24
  alignment _ = 8
  peek p =
    DisplayModePropertiesKHR
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 8)
  poke p x = do
    pokeByteOff p 0 x.displayMode
    pokeByteOff p 8 x.parameters

instance CStruct DisplayModePropertiesKHR where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct DisplayModePropertiesKHR where
  peekCStruct = peek

instance Nil DisplayModePropertiesKHR where
  nil =
    DisplayModePropertiesKHR
      { displayMode = nil
      , parameters = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayPlaneCapabilitiesKHR.html VkDisplayPlaneCapabilitiesKHR>
-- Available: VK_KHR_display
data DisplayPlaneCapabilitiesKHR = DisplayPlaneCapabilitiesKHR
  { supportedAlpha :: !DisplayPlaneAlphaFlagsKHR
  , minSrcPosition :: !Offset2D
  , maxSrcPosition :: !Offset2D
  , minSrcExtent :: !Extent2D
  , maxSrcExtent :: !Extent2D
  , minDstPosition :: !Offset2D
  , maxDstPosition :: !Offset2D
  , minDstExtent :: !Extent2D
  , maxDstExtent :: !Extent2D
  }
  deriving stock (Eq, Show)

instance Storable DisplayPlaneCapabilitiesKHR where
  sizeOf _ = 68
  alignment _ = 4
  peek p =
    DisplayPlaneCapabilitiesKHR
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 4)
      <*> (peekByteOff p 12)
      <*> (peekByteOff p 20)
      <*> (peekByteOff p 28)
      <*> (peekByteOff p 36)
      <*> (peekByteOff p 44)
      <*> (peekByteOff p 52)
      <*> (peekByteOff p 60)
  poke p x = do
    pokeByteOff p 0 x.supportedAlpha
    pokeByteOff p 4 x.minSrcPosition
    pokeByteOff p 12 x.maxSrcPosition
    pokeByteOff p 20 x.minSrcExtent
    pokeByteOff p 28 x.maxSrcExtent
    pokeByteOff p 36 x.minDstPosition
    pokeByteOff p 44 x.maxDstPosition
    pokeByteOff p 52 x.minDstExtent
    pokeByteOff p 60 x.maxDstExtent

instance CStruct DisplayPlaneCapabilitiesKHR where
  cSize = 68
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct DisplayPlaneCapabilitiesKHR where
  peekCStruct = peek

instance Nil DisplayPlaneCapabilitiesKHR where
  nil =
    DisplayPlaneCapabilitiesKHR
      { supportedAlpha = mempty
      , minSrcPosition = nil
      , maxSrcPosition = nil
      , minSrcExtent = nil
      , maxSrcExtent = nil
      , minDstPosition = nil
      , maxDstPosition = nil
      , minDstExtent = nil
      , maxDstExtent = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayPlanePropertiesKHR.html VkDisplayPlanePropertiesKHR>
-- Available: VK_KHR_display
data DisplayPlanePropertiesKHR = DisplayPlanePropertiesKHR
  { currentDisplay :: !DisplayKHR
  , currentStackIndex :: !Word32
  }
  deriving stock (Eq, Show)

instance Storable DisplayPlanePropertiesKHR where
  sizeOf _ = 16
  alignment _ = 8
  peek p =
    DisplayPlanePropertiesKHR
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 8)
  poke p x = do
    pokeByteOff p 0 x.currentDisplay
    pokeByteOff p 8 x.currentStackIndex

instance CStruct DisplayPlanePropertiesKHR where
  cSize = 16
  cAlign = 8
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct DisplayPlanePropertiesKHR where
  peekCStruct = peek

instance Nil DisplayPlanePropertiesKHR where
  nil =
    DisplayPlanePropertiesKHR
      { currentDisplay = nil
      , currentStackIndex = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayModeCreateInfoKHR.html VkDisplayModeCreateInfoKHR>
-- Available: VK_KHR_display
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DisplayModeCreateInfoKHR = DisplayModeCreateInfoKHR
  { flags :: DisplayModeCreateFlagsKHR
  , parameters :: DisplayModeParametersKHR
  }

instance CStruct DisplayModeCreateInfoKHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000002000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 20 x.parameters)
    pure ()

instance FromCStruct DisplayModeCreateInfoKHR where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_parameters <- (peekByteOff p 20)
    pure DisplayModeCreateInfoKHR{flags = v_flags, parameters = v_parameters}

instance Nil DisplayModeCreateInfoKHR where
  nil =
    DisplayModeCreateInfoKHR
      { flags = mempty
      , parameters = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayPropertiesKHR.html VkDisplayPropertiesKHR>
-- Available: VK_KHR_display
data DisplayPropertiesKHR = DisplayPropertiesKHR
  { display :: DisplayKHR
  , displayName :: ByteString
  , physicalDimensions :: Extent2D
  , physicalResolution :: Extent2D
  , supportedTransforms :: SurfaceTransformFlagsKHR
  , planeReorderPossible :: Bool
  , persistentContent :: Bool
  }

instance CStruct DisplayPropertiesKHR where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 x.display)
    p_displayName <- newCString x.displayName
    liftIO (pokeByteOff p 8 p_displayName)
    liftIO (pokeByteOff p 16 x.physicalDimensions)
    liftIO (pokeByteOff p 24 x.physicalResolution)
    liftIO (pokeByteOff p 32 x.supportedTransforms)
    liftIO (pokeByteOff p 36 (if x.planeReorderPossible then TRUE else FALSE))
    liftIO (pokeByteOff p 40 (if x.persistentContent then TRUE else FALSE))
    pure ()

instance FromCStruct DisplayPropertiesKHR where
  peekCStruct p = do
    v_display <- (peekByteOff p 0)
    v_displayName <- BS.packCString =<< peekByteOff p 8
    v_physicalDimensions <- (peekByteOff p 16)
    v_physicalResolution <- (peekByteOff p 24)
    v_supportedTransforms <- (peekByteOff p 32)
    v_planeReorderPossible <- (toBool <$> peekByteOff p 36)
    v_persistentContent <- (toBool <$> peekByteOff p 40)
    pure
      DisplayPropertiesKHR
        { display = v_display
        , displayName = v_displayName
        , physicalDimensions = v_physicalDimensions
        , physicalResolution = v_physicalResolution
        , supportedTransforms = v_supportedTransforms
        , planeReorderPossible = v_planeReorderPossible
        , persistentContent = v_persistentContent
        }

instance Nil DisplayPropertiesKHR where
  nil =
    DisplayPropertiesKHR
      { display = nil
      , displayName = ""
      , physicalDimensions = nil
      , physicalResolution = nil
      , supportedTransforms = mempty
      , planeReorderPossible = False
      , persistentContent = False
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplaySurfaceCreateInfoKHR.html VkDisplaySurfaceCreateInfoKHR>
-- Available: VK_KHR_display
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DisplaySurfaceCreateInfoKHR = DisplaySurfaceCreateInfoKHR
  { flags :: DisplaySurfaceCreateFlagsKHR
  , displayMode :: DisplayModeKHR
  , planeIndex :: Word32
  , planeStackIndex :: Word32
  , transform :: Flags SurfaceTransformFlagBitsKHR
  , globalAlpha :: Float
  , alphaMode :: Flags DisplayPlaneAlphaFlagBitsKHR
  , imageExtent :: Extent2D
  }

instance CStruct DisplaySurfaceCreateInfoKHR where
  cSize = 64
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000002001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.displayMode)
    liftIO (pokeByteOff p 32 x.planeIndex)
    liftIO (pokeByteOff p 36 x.planeStackIndex)
    liftIO (pokeByteOff p 40 x.transform)
    liftIO (pokeByteOff p 44 x.globalAlpha)
    liftIO (pokeByteOff p 48 x.alphaMode)
    liftIO (pokeByteOff p 52 x.imageExtent)
    pure ()

instance FromCStruct DisplaySurfaceCreateInfoKHR where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_displayMode <- (peekByteOff p 24)
    v_planeIndex <- (peekByteOff p 32)
    v_planeStackIndex <- (peekByteOff p 36)
    v_transform <- (peekByteOff p 40)
    v_globalAlpha <- (peekByteOff p 44)
    v_alphaMode <- (peekByteOff p 48)
    v_imageExtent <- (peekByteOff p 52)
    pure
      DisplaySurfaceCreateInfoKHR
        { flags = v_flags
        , displayMode = v_displayMode
        , planeIndex = v_planeIndex
        , planeStackIndex = v_planeStackIndex
        , transform = v_transform
        , globalAlpha = v_globalAlpha
        , alphaMode = v_alphaMode
        , imageExtent = v_imageExtent
        }

instance Nil DisplaySurfaceCreateInfoKHR where
  nil =
    DisplaySurfaceCreateInfoKHR
      { flags = mempty
      , displayMode = nil
      , planeIndex = 0
      , planeStackIndex = 0
      , transform = mempty
      , globalAlpha = 0
      , alphaMode = mempty
      , imageExtent = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateDisplayModeKHR.html vkCreateDisplayModeKHR>
-- Available: VK_KHR_display
createDisplayModeKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> DisplayKHR
  -> DisplayModeCreateInfoKHR
  -> Maybe AllocationCallbacks
  -> m (Outcome DisplayModeKHR)
createDisplayModeKHR physicalDevice display createInfo allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_createInfo :: Ptr () <- castPtr <$> new createInfo
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    p_o_mode <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkCreateDisplayModeKHR
            (checkCommandPtr "vkCreateDisplayModeKHR" physicalDevice.cmds.pCreateDisplayModeKHR)
            (castPtr physicalDevice.handle)
            (case display of DisplayKHR w_ -> w_)
            p_createInfo
            p_allocator
            (castPtr p_o_mode)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_mode <- liftIO (peekByteOff p_o_mode 0 :: IO Word64)
      let o_mode = DisplayModeKHR w_o_mode
      pure (Ok (openFromWire r_) o_mode)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateDisplayPlaneSurfaceKHR.html vkCreateDisplayPlaneSurfaceKHR>
-- Available: VK_KHR_display
createDisplayPlaneSurfaceKHR
  :: forall m
   . (MonadIO m)
  => Instance
  -> DisplaySurfaceCreateInfoKHR
  -> Maybe AllocationCallbacks
  -> m (Outcome SurfaceKHR)
createDisplayPlaneSurfaceKHR instance' createInfo allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_createInfo :: Ptr () <- castPtr <$> new createInfo
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    p_o_surface <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkCreateDisplayPlaneSurfaceKHR
            (checkCommandPtr "vkCreateDisplayPlaneSurfaceKHR" instance'.cmds.pCreateDisplayPlaneSurfaceKHR)
            (castPtr instance'.handle)
            p_createInfo
            p_allocator
            (castPtr p_o_surface)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_surface <- liftIO (peekByteOff p_o_surface 0 :: IO Word64)
      let o_surface = SurfaceKHR w_o_surface
      pure (Ok (openFromWire r_) o_surface)

withDisplayPlaneSurfaceKHR
  :: forall m r
   . (MonadUnliftIO m)
  => Instance
  -> DisplaySurfaceCreateInfoKHR
  -> Maybe AllocationCallbacks
  -> (Outcome SurfaceKHR -> m r)
  -> m r
withDisplayPlaneSurfaceKHR instance' createInfo allocator cont_ =
  withRunInIO \run_ ->
    bracket
      (run_ (createDisplayPlaneSurfaceKHR instance' createInfo allocator))
      ( \o_ -> case o_ of
          Ok _ h_ -> run_ (destroySurfaceKHR instance' h_ allocator)
          Err _ -> pure ()
      )
      (run_ . cont_)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetDisplayModePropertiesKHR.html vkGetDisplayModePropertiesKHR>
-- Available: VK_KHR_display
getDisplayModePropertiesKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> DisplayKHR
  -> m (Outcome (V.Vector DisplayModePropertiesKHR))
getDisplayModePropertiesKHR physicalDevice display =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetDisplayModePropertiesKHR
                  (checkCommandPtr "vkGetDisplayModePropertiesKHR" physicalDevice.cmds.pGetDisplayModePropertiesKHR)
                  (castPtr physicalDevice.handle)
                  (case display of DisplayKHR w_ -> w_)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pPropertyCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_properties <- arenaBytes (24 * max 1 (fromIntegral n_pPropertyCount)) 8
            forM_
              ([0 .. fromIntegral n_pPropertyCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_properties `plusPtr` (i * 24))) (nil :: DisplayModePropertiesKHR))
            r2_ <-
              liftIO
                ( mkGetDisplayModePropertiesKHR
                    (checkCommandPtr "vkGetDisplayModePropertiesKHR" physicalDevice.cmds.pGetDisplayModePropertiesKHR)
                    (castPtr physicalDevice.handle)
                    (case display of DisplayKHR w_ -> w_)
                    (castPtr p_n_)
                    (castPtr p_o_properties)
                )
            n_pPropertyCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_properties <- liftIO (peekElems (fromIntegral n_pPropertyCount') (castPtr p_o_properties))
                pure (Ok (openFromWire r2_) o_properties)
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetDisplayPlaneCapabilitiesKHR.html vkGetDisplayPlaneCapabilitiesKHR>
-- Available: VK_KHR_display
getDisplayPlaneCapabilitiesKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> DisplayModeKHR
  -> Word32
  -> m (Outcome DisplayPlaneCapabilitiesKHR)
getDisplayPlaneCapabilitiesKHR physicalDevice mode planeIndex =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_o_capabilities :: Ptr () <- castPtr <$> new (nil :: DisplayPlaneCapabilitiesKHR)
    r_ <-
      liftIO
        ( mkGetDisplayPlaneCapabilitiesKHR
            ( checkCommandPtr
                "vkGetDisplayPlaneCapabilitiesKHR"
                physicalDevice.cmds.pGetDisplayPlaneCapabilitiesKHR
            )
            (castPtr physicalDevice.handle)
            (case mode of DisplayModeKHR w_ -> w_)
            planeIndex
            p_o_capabilities
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      o_capabilities <- liftIO (peekCStruct (castPtr p_o_capabilities))
      pure (Ok (openFromWire r_) o_capabilities)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetDisplayPlaneSupportedDisplaysKHR.html vkGetDisplayPlaneSupportedDisplaysKHR>
-- Available: VK_KHR_display
getDisplayPlaneSupportedDisplaysKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> Word32
  -> m (Outcome (V.Vector DisplayKHR))
getDisplayPlaneSupportedDisplaysKHR physicalDevice planeIndex =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetDisplayPlaneSupportedDisplaysKHR
                  ( checkCommandPtr
                      "vkGetDisplayPlaneSupportedDisplaysKHR"
                      physicalDevice.cmds.pGetDisplayPlaneSupportedDisplaysKHR
                  )
                  (castPtr physicalDevice.handle)
                  planeIndex
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pDisplayCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_displays <- arenaBytes (8 * max 1 (fromIntegral n_pDisplayCount)) 8
            r2_ <-
              liftIO
                ( mkGetDisplayPlaneSupportedDisplaysKHR
                    ( checkCommandPtr
                        "vkGetDisplayPlaneSupportedDisplaysKHR"
                        physicalDevice.cmds.pGetDisplayPlaneSupportedDisplaysKHR
                    )
                    (castPtr physicalDevice.handle)
                    planeIndex
                    (castPtr p_n_)
                    (castPtr p_o_displays)
                )
            n_pDisplayCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_displays <-
                  liftIO
                    ( V.generateM
                        (fromIntegral n_pDisplayCount')
                        (\i -> DisplayKHR <$> peekElemOff (castPtr p_o_displays :: Ptr Word64) i)
                    )
                pure (Ok (openFromWire r2_) o_displays)
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetPhysicalDeviceDisplayPlanePropertiesKHR.html vkGetPhysicalDeviceDisplayPlanePropertiesKHR>
-- Available: VK_KHR_display
getPhysicalDeviceDisplayPlanePropertiesKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> m (Outcome (V.Vector DisplayPlanePropertiesKHR))
getPhysicalDeviceDisplayPlanePropertiesKHR physicalDevice =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetPhysicalDeviceDisplayPlanePropertiesKHR
                  ( checkCommandPtr
                      "vkGetPhysicalDeviceDisplayPlanePropertiesKHR"
                      physicalDevice.cmds.pGetPhysicalDeviceDisplayPlanePropertiesKHR
                  )
                  (castPtr physicalDevice.handle)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pPropertyCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_properties <- arenaBytes (16 * max 1 (fromIntegral n_pPropertyCount)) 8
            forM_
              ([0 .. fromIntegral n_pPropertyCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_properties `plusPtr` (i * 16))) (nil :: DisplayPlanePropertiesKHR))
            r2_ <-
              liftIO
                ( mkGetPhysicalDeviceDisplayPlanePropertiesKHR
                    ( checkCommandPtr
                        "vkGetPhysicalDeviceDisplayPlanePropertiesKHR"
                        physicalDevice.cmds.pGetPhysicalDeviceDisplayPlanePropertiesKHR
                    )
                    (castPtr physicalDevice.handle)
                    (castPtr p_n_)
                    (castPtr p_o_properties)
                )
            n_pPropertyCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_properties <- liftIO (peekElems (fromIntegral n_pPropertyCount') (castPtr p_o_properties))
                pure (Ok (openFromWire r2_) o_properties)
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetPhysicalDeviceDisplayPropertiesKHR.html vkGetPhysicalDeviceDisplayPropertiesKHR>
-- Available: VK_KHR_display
getPhysicalDeviceDisplayPropertiesKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> m (Outcome (V.Vector DisplayPropertiesKHR))
getPhysicalDeviceDisplayPropertiesKHR physicalDevice =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetPhysicalDeviceDisplayPropertiesKHR
                  ( checkCommandPtr
                      "vkGetPhysicalDeviceDisplayPropertiesKHR"
                      physicalDevice.cmds.pGetPhysicalDeviceDisplayPropertiesKHR
                  )
                  (castPtr physicalDevice.handle)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pPropertyCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_properties <- arenaBytes (48 * max 1 (fromIntegral n_pPropertyCount)) 8
            forM_
              ([0 .. fromIntegral n_pPropertyCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_properties `plusPtr` (i * 48))) (nil :: DisplayPropertiesKHR))
            r2_ <-
              liftIO
                ( mkGetPhysicalDeviceDisplayPropertiesKHR
                    ( checkCommandPtr
                        "vkGetPhysicalDeviceDisplayPropertiesKHR"
                        physicalDevice.cmds.pGetPhysicalDeviceDisplayPropertiesKHR
                    )
                    (castPtr physicalDevice.handle)
                    (castPtr p_n_)
                    (castPtr p_o_properties)
                )
            n_pPropertyCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_properties <- liftIO (peekElems (fromIntegral n_pPropertyCount') (castPtr p_o_properties))
                pure (Ok (openFromWire r2_) o_properties)
    goDual_
