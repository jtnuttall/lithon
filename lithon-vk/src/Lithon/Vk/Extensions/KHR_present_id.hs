-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_present_id (
  PhysicalDevicePresentIdFeaturesKHR (..),
  PresentIdKHR (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDevicePresentIdFeaturesKHR.html VkPhysicalDevicePresentIdFeaturesKHR>
-- Available: VK_KHR_present_id
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDevicePresentIdFeaturesKHR = PhysicalDevicePresentIdFeaturesKHR
  { presentId :: Bool
  }

instance CStruct PhysicalDevicePresentIdFeaturesKHR where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000294001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.presentId then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDevicePresentIdFeaturesKHR where
  peekCStruct p = do
    v_presentId <- (toBool <$> peekByteOff p 16)
    pure PhysicalDevicePresentIdFeaturesKHR{presentId = v_presentId}

instance Nil PhysicalDevicePresentIdFeaturesKHR where
  nil =
    PhysicalDevicePresentIdFeaturesKHR
      { presentId = False
      }

instance Chainable PhysicalDevicePresentIdFeaturesKHR where
  sTypeWire = 1000294001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPresentIdKHR.html VkPresentIdKHR>
-- Available: VK_KHR_present_id
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @swapchainCount@ is derived from @presentIds@'s length.
data PresentIdKHR = PresentIdKHR
  { presentIds :: V.Vector Word64
  }

instance CStruct PresentIdKHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000294000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    p_presentIds <- arenaBytes (8 * max 1 (V.length x.presentIds)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_presentIds (i * 8) v) x.presentIds)
    liftIO
      (pokeByteOff p 24 (if V.null x.presentIds then nullPtr else (castPtr p_presentIds :: Ptr ())))
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.presentIds) :: Word32))
    pure ()

instance FromCStruct PresentIdKHR where
  peekCStruct p = do
    v_presentIds <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    pure PresentIdKHR{presentIds = v_presentIds}

instance Nil PresentIdKHR where
  nil =
    PresentIdKHR
      { presentIds = V.empty
      }

instance Chainable PresentIdKHR where
  sTypeWire = 1000294000
  chainOffset = 8
