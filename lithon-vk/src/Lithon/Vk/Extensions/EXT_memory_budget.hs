-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_memory_budget (
  PhysicalDeviceMemoryBudgetPropertiesEXT (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Vector.Storable.Sized qualified as VSS
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceMemoryBudgetPropertiesEXT.html VkPhysicalDeviceMemoryBudgetPropertiesEXT>
-- Available: VK_EXT_memory_budget
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceMemoryBudgetPropertiesEXT = PhysicalDeviceMemoryBudgetPropertiesEXT
  { heapBudget :: VSS.Vector 16 Word64
  , heapUsage :: VSS.Vector 16 Word64
  }

instance CStruct PhysicalDeviceMemoryBudgetPropertiesEXT where
  cSize = 272
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000237000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.heapBudget)
    liftIO (pokeByteOff p 144 x.heapUsage)
    pure ()

instance FromCStruct PhysicalDeviceMemoryBudgetPropertiesEXT where
  peekCStruct p = do
    v_heapBudget <- (peekByteOff p 16)
    v_heapUsage <- (peekByteOff p 144)
    pure PhysicalDeviceMemoryBudgetPropertiesEXT{heapBudget = v_heapBudget, heapUsage = v_heapUsage}

instance Nil PhysicalDeviceMemoryBudgetPropertiesEXT where
  nil =
    PhysicalDeviceMemoryBudgetPropertiesEXT
      { heapBudget = VSS.replicate 0
      , heapUsage = VSS.replicate 0
      }

instance Chainable PhysicalDeviceMemoryBudgetPropertiesEXT where
  sTypeWire = 1000237000
  chainOffset = 8
