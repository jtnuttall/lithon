-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_calibrated_timestamps (
  TimeDomainKHR (..),
  CalibratedTimestampInfoKHR (..),
  getCalibratedTimestampsKHR,
  getPhysicalDeviceCalibrateableTimeDomainsKHR,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkTimeDomainKHR.html VkTimeDomainKHR>
-- Available: VK_KHR_calibrated_timestamps
data TimeDomainKHR
  = TimeDomainDeviceKHR
  | TimeDomainClockMonotonicKHR
  | TimeDomainClockMonotonicRawKHR
  | TimeDomainQueryPerformanceCounterKHR
  deriving stock (Eq, Ord, Show)

instance HasWire TimeDomainKHR where
  toWire = \case
    TimeDomainDeviceKHR -> 0
    TimeDomainClockMonotonicKHR -> 1
    TimeDomainClockMonotonicRawKHR -> 2
    TimeDomainQueryPerformanceCounterKHR -> 3
  fromWire = \case
    0 -> Just TimeDomainDeviceKHR
    1 -> Just TimeDomainClockMonotonicKHR
    2 -> Just TimeDomainClockMonotonicRawKHR
    3 -> Just TimeDomainQueryPerformanceCounterKHR
    _ -> Nothing

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkCalibratedTimestampInfoKHR.html VkCalibratedTimestampInfoKHR>
-- Available: VK_KHR_calibrated_timestamps
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data CalibratedTimestampInfoKHR = CalibratedTimestampInfoKHR
  { timeDomain :: Open TimeDomainKHR
  }

instance CStruct CalibratedTimestampInfoKHR where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000184000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (openToWire x.timeDomain))
    pure ()

instance FromCStruct CalibratedTimestampInfoKHR where
  peekCStruct p = do
    v_timeDomain <- (openFromWire <$> peekByteOff p 16)
    pure CalibratedTimestampInfoKHR{timeDomain = v_timeDomain}

instance Nil CalibratedTimestampInfoKHR where
  nil =
    CalibratedTimestampInfoKHR
      { timeDomain = Known TimeDomainDeviceKHR
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetCalibratedTimestampsKHR.html vkGetCalibratedTimestampsKHR>
-- Available: VK_KHR_calibrated_timestamps
getCalibratedTimestampsKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> V.Vector CalibratedTimestampInfoKHR
  -> m (Outcome ((V.Vector Word64, Word64)))
getCalibratedTimestampsKHR device timestampInfos =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_timestampCount = fromIntegral (V.length timestampInfos) :: Word32
        n_timestampCount' = n_timestampCount
    p_timestampInfos <- do pv_ <- newVector timestampInfos; pure (castPtr pv_ :: Ptr ())
    p_o_timestamps <- arenaBytes (8 * max 1 (fromIntegral n_timestampCount)) 8
    p_o_maxDeviation <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkGetCalibratedTimestampsKHR
            (checkCommandPtr "vkGetCalibratedTimestampsKHR" device.cmds.pGetCalibratedTimestampsKHR)
            (castPtr device.handle)
            n_timestampCount
            p_timestampInfos
            (castPtr p_o_timestamps)
            (castPtr p_o_maxDeviation)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      o_timestamps <-
        liftIO
          ( V.generateM
              (fromIntegral n_timestampCount')
              (\i -> (\w_ -> w_) <$> (peekByteOff p_o_timestamps (i * 8) :: IO Word64))
          )
      w_o_maxDeviation <- liftIO (peekByteOff p_o_maxDeviation 0 :: IO Word64)
      let o_maxDeviation = w_o_maxDeviation
      pure (Ok (openFromWire r_) ((o_timestamps, o_maxDeviation)))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetPhysicalDeviceCalibrateableTimeDomainsKHR.html vkGetPhysicalDeviceCalibrateableTimeDomainsKHR>
-- Available: VK_KHR_calibrated_timestamps
getPhysicalDeviceCalibrateableTimeDomainsKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> m (Outcome (V.Vector (Open TimeDomainKHR)))
getPhysicalDeviceCalibrateableTimeDomainsKHR physicalDevice =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetPhysicalDeviceCalibrateableTimeDomainsKHR
                  ( checkCommandPtr
                      "vkGetPhysicalDeviceCalibrateableTimeDomainsKHR"
                      physicalDevice.cmds.pGetPhysicalDeviceCalibrateableTimeDomainsKHR
                  )
                  (castPtr physicalDevice.handle)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pTimeDomainCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_timeDomains <- arenaBytes (4 * max 1 (fromIntegral n_pTimeDomainCount)) 4
            r2_ <-
              liftIO
                ( mkGetPhysicalDeviceCalibrateableTimeDomainsKHR
                    ( checkCommandPtr
                        "vkGetPhysicalDeviceCalibrateableTimeDomainsKHR"
                        physicalDevice.cmds.pGetPhysicalDeviceCalibrateableTimeDomainsKHR
                    )
                    (castPtr physicalDevice.handle)
                    (castPtr p_n_)
                    (castPtr p_o_timeDomains)
                )
            n_pTimeDomainCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_timeDomains <-
                  liftIO
                    ( V.generateM
                        (fromIntegral n_pTimeDomainCount')
                        (\i -> (\w_ -> openFromWire w_) <$> (peekByteOff p_o_timeDomains (i * 4) :: IO Int32))
                    )
                pure (Ok (openFromWire r2_) o_timeDomains)
    goDual_
