-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_present_wait (
  PhysicalDevicePresentWaitFeaturesKHR (..),
  waitForPresentKHR,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Extensions.KHR_swapchain
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDevicePresentWaitFeaturesKHR.html VkPhysicalDevicePresentWaitFeaturesKHR>
-- Available: VK_KHR_present_wait
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDevicePresentWaitFeaturesKHR = PhysicalDevicePresentWaitFeaturesKHR
  { presentWait :: Bool
  }

instance CStruct PhysicalDevicePresentWaitFeaturesKHR where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000248000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.presentWait then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDevicePresentWaitFeaturesKHR where
  peekCStruct p = do
    v_presentWait <- (toBool <$> peekByteOff p 16)
    pure PhysicalDevicePresentWaitFeaturesKHR{presentWait = v_presentWait}

instance Nil PhysicalDevicePresentWaitFeaturesKHR where
  nil =
    PhysicalDevicePresentWaitFeaturesKHR
      { presentWait = False
      }

instance Chainable PhysicalDevicePresentWaitFeaturesKHR where
  sTypeWire = 1000248000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkWaitForPresentKHR.html vkWaitForPresentKHR>
-- Available: VK_KHR_present_wait
waitForPresentKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> SwapchainKHR
  -> Word64
  -> Word64
  -> m (Outcome ())
waitForPresentKHR device swapchain presentId timeout =
  liftIO do
    r_ <-
      liftIO
        ( mkWaitForPresentKHR
            (checkCommandPtr "vkWaitForPresentKHR" device.cmds.pWaitForPresentKHR)
            (castPtr device.handle)
            (case swapchain of SwapchainKHR w_ -> w_)
            presentId
            timeout
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
