-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_multi_draw (
  MultiDrawIndexedInfoEXT (..),
  MultiDrawInfoEXT (..),
  PhysicalDeviceMultiDrawFeaturesEXT (..),
  PhysicalDeviceMultiDrawPropertiesEXT (..),
  cmdDrawMultiEXT,
  cmdDrawMultiIndexedEXT,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable (..), peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMultiDrawIndexedInfoEXT.html VkMultiDrawIndexedInfoEXT>
-- Available: VK_EXT_multi_draw
data MultiDrawIndexedInfoEXT = MultiDrawIndexedInfoEXT
  { firstIndex :: !Word32
  , indexCount :: !Word32
  , vertexOffset :: !Int32
  }
  deriving stock (Eq, Show)

instance Storable MultiDrawIndexedInfoEXT where
  sizeOf _ = 12
  alignment _ = 4
  peek p =
    MultiDrawIndexedInfoEXT
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 4)
      <*> (peekByteOff p 8)
  poke p x = do
    pokeByteOff p 0 x.firstIndex
    pokeByteOff p 4 x.indexCount
    pokeByteOff p 8 x.vertexOffset

instance CStruct MultiDrawIndexedInfoEXT where
  cSize = 12
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct MultiDrawIndexedInfoEXT where
  peekCStruct = peek

instance Nil MultiDrawIndexedInfoEXT where
  nil =
    MultiDrawIndexedInfoEXT
      { firstIndex = 0
      , indexCount = 0
      , vertexOffset = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMultiDrawInfoEXT.html VkMultiDrawInfoEXT>
-- Available: VK_EXT_multi_draw
data MultiDrawInfoEXT = MultiDrawInfoEXT
  { firstVertex :: !Word32
  , vertexCount :: !Word32
  }
  deriving stock (Eq, Show)

instance Storable MultiDrawInfoEXT where
  sizeOf _ = 8
  alignment _ = 4
  peek p =
    MultiDrawInfoEXT
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 4)
  poke p x = do
    pokeByteOff p 0 x.firstVertex
    pokeByteOff p 4 x.vertexCount

instance CStruct MultiDrawInfoEXT where
  cSize = 8
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct MultiDrawInfoEXT where
  peekCStruct = peek

instance Nil MultiDrawInfoEXT where
  nil =
    MultiDrawInfoEXT
      { firstVertex = 0
      , vertexCount = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceMultiDrawFeaturesEXT.html VkPhysicalDeviceMultiDrawFeaturesEXT>
-- Available: VK_EXT_multi_draw
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceMultiDrawFeaturesEXT = PhysicalDeviceMultiDrawFeaturesEXT
  { multiDraw :: Bool
  }

instance CStruct PhysicalDeviceMultiDrawFeaturesEXT where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000392000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.multiDraw then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceMultiDrawFeaturesEXT where
  peekCStruct p = do
    v_multiDraw <- (toBool <$> peekByteOff p 16)
    pure PhysicalDeviceMultiDrawFeaturesEXT{multiDraw = v_multiDraw}

instance Nil PhysicalDeviceMultiDrawFeaturesEXT where
  nil =
    PhysicalDeviceMultiDrawFeaturesEXT
      { multiDraw = False
      }

instance Chainable PhysicalDeviceMultiDrawFeaturesEXT where
  sTypeWire = 1000392000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceMultiDrawPropertiesEXT.html VkPhysicalDeviceMultiDrawPropertiesEXT>
-- Available: VK_EXT_multi_draw
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceMultiDrawPropertiesEXT = PhysicalDeviceMultiDrawPropertiesEXT
  { maxMultiDrawCount :: Word32
  }

instance CStruct PhysicalDeviceMultiDrawPropertiesEXT where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000392001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.maxMultiDrawCount)
    pure ()

instance FromCStruct PhysicalDeviceMultiDrawPropertiesEXT where
  peekCStruct p = do
    v_maxMultiDrawCount <- (peekByteOff p 16)
    pure PhysicalDeviceMultiDrawPropertiesEXT{maxMultiDrawCount = v_maxMultiDrawCount}

instance Nil PhysicalDeviceMultiDrawPropertiesEXT where
  nil =
    PhysicalDeviceMultiDrawPropertiesEXT
      { maxMultiDrawCount = 0
      }

instance Chainable PhysicalDeviceMultiDrawPropertiesEXT where
  sTypeWire = 1000392001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdDrawMultiEXT.html vkCmdDrawMultiEXT>
-- Available: VK_EXT_multi_draw
cmdDrawMultiEXT
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> V.Vector MultiDrawInfoEXT
  -> Word32
  -> Word32
  -> Word32
  -> m ()
cmdDrawMultiEXT commandBuffer vertexInfo instanceCount firstInstance stride =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_drawCount = fromIntegral (V.length vertexInfo) :: Word32
    p_vertexInfo <- do pv_ <- newVector vertexInfo; pure (castPtr pv_ :: Ptr ())
    liftIO
      ( mkCmdDrawMultiEXT
          (checkCommandPtr "vkCmdDrawMultiEXT" commandBuffer.cmds.pCmdDrawMultiEXT)
          (castPtr commandBuffer.handle)
          n_drawCount
          p_vertexInfo
          instanceCount
          firstInstance
          stride
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdDrawMultiIndexedEXT.html vkCmdDrawMultiIndexedEXT>
-- Available: VK_EXT_multi_draw
cmdDrawMultiIndexedEXT
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> V.Vector MultiDrawIndexedInfoEXT
  -> Word32
  -> Word32
  -> Word32
  -> Maybe Int32
  -> m ()
cmdDrawMultiIndexedEXT commandBuffer indexInfo instanceCount firstInstance stride vertexOffset =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_drawCount = fromIntegral (V.length indexInfo) :: Word32
    p_indexInfo <- do pv_ <- newVector indexInfo; pure (castPtr pv_ :: Ptr ())
    p_vertexOffset <-
      maybe
        (pure nullPtr)
        (\v_ -> do pv_ <- arenaBytes 4 4; liftIO (pokeByteOff pv_ 0 v_); pure pv_)
        vertexOffset
    liftIO
      ( mkCmdDrawMultiIndexedEXT
          (checkCommandPtr "vkCmdDrawMultiIndexedEXT" commandBuffer.cmds.pCmdDrawMultiIndexedEXT)
          (castPtr commandBuffer.handle)
          n_drawCount
          p_indexInfo
          instanceCount
          firstInstance
          stride
          (castPtr p_vertexOffset)
      )
    pure ()
