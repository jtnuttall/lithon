-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_xlib_surface (
  XlibSurfaceCreateFlagsKHR,
  XlibSurfaceCreateFlagsKHRBits,
  XlibSurfaceCreateInfoKHR (..),
  createXlibSurfaceKHR,
  withXlibSurfaceKHR,
  getPhysicalDeviceXlibPresentationSupportKHR,
) where

import Control.Exception (bracket)
import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Control.Monad.IO.Unlift (MonadUnliftIO, withRunInIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (BitPos (..), Flags (..), FlagsWire)
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.DeviceInitialization
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Extensions.KHR_surface
import Lithon.Vk.Result (Outcome (..))

-- | Reserved-empty mask: no bits are defined yet.
data XlibSurfaceCreateFlagsKHRBits
  deriving stock (Eq, Ord, Show)

type instance FlagsWire XlibSurfaceCreateFlagsKHRBits = Word32

instance BitPos XlibSurfaceCreateFlagsKHRBits where
  bitOf = \case {}
  allBits = []

type XlibSurfaceCreateFlagsKHR = Flags XlibSurfaceCreateFlagsKHRBits

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkXlibSurfaceCreateInfoKHR.html VkXlibSurfaceCreateInfoKHR>
-- Available: VK_KHR_xlib_surface
-- Platform-specific (guarded by @VK_USE_PLATFORM_XLIB_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @dpy@ is deliberately un-modelled (raw pointer); see the module notes.
data XlibSurfaceCreateInfoKHR = XlibSurfaceCreateInfoKHR
  { flags :: XlibSurfaceCreateFlagsKHR
  , dpy :: Ptr ()
  , window :: Word64
  }

instance CStruct XlibSurfaceCreateInfoKHR where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000004000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.dpy)
    liftIO (pokeByteOff p 32 x.window)
    pure ()

instance FromCStruct XlibSurfaceCreateInfoKHR where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_dpy <- peekByteOff p 24
    v_window <- (peekByteOff p 32)
    pure XlibSurfaceCreateInfoKHR{flags = v_flags, dpy = v_dpy, window = v_window}

instance Nil XlibSurfaceCreateInfoKHR where
  nil =
    XlibSurfaceCreateInfoKHR
      { flags = mempty
      , dpy = nullPtr
      , window = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateXlibSurfaceKHR.html vkCreateXlibSurfaceKHR>
-- Available: VK_KHR_xlib_surface
-- Platform-specific (guarded by @VK_USE_PLATFORM_XLIB_KHR@ in C; emitted unconditionally here).
createXlibSurfaceKHR
  :: forall m
   . (MonadIO m)
  => Instance
  -> XlibSurfaceCreateInfoKHR
  -> Maybe AllocationCallbacks
  -> m (Outcome SurfaceKHR)
createXlibSurfaceKHR instance' createInfo allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_createInfo :: Ptr () <- castPtr <$> new createInfo
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    p_o_surface <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkCreateXlibSurfaceKHR
            (checkCommandPtr "vkCreateXlibSurfaceKHR" instance'.cmds.pCreateXlibSurfaceKHR)
            (castPtr instance'.handle)
            p_createInfo
            p_allocator
            (castPtr p_o_surface)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_surface <- liftIO (peekByteOff p_o_surface 0 :: IO Word64)
      let o_surface = SurfaceKHR w_o_surface
      pure (Ok (openFromWire r_) o_surface)

withXlibSurfaceKHR
  :: forall m r
   . (MonadUnliftIO m)
  => Instance
  -> XlibSurfaceCreateInfoKHR
  -> Maybe AllocationCallbacks
  -> (Outcome SurfaceKHR -> m r)
  -> m r
withXlibSurfaceKHR instance' createInfo allocator cont_ =
  withRunInIO \run_ ->
    bracket
      (run_ (createXlibSurfaceKHR instance' createInfo allocator))
      ( \o_ -> case o_ of
          Ok _ h_ -> run_ (destroySurfaceKHR instance' h_ allocator)
          Err _ -> pure ()
      )
      (run_ . cont_)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetPhysicalDeviceXlibPresentationSupportKHR.html vkGetPhysicalDeviceXlibPresentationSupportKHR>
-- Available: VK_KHR_xlib_surface
-- Platform-specific (guarded by @VK_USE_PLATFORM_XLIB_KHR@ in C; emitted unconditionally here).
getPhysicalDeviceXlibPresentationSupportKHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> Word32
  -> Ptr ()
  -> Word64
  -> m Bool
getPhysicalDeviceXlibPresentationSupportKHR physicalDevice queueFamilyIndex dpy visualID =
  liftIO do
    retw_ <-
      liftIO
        ( mkGetPhysicalDeviceXlibPresentationSupportKHR
            ( checkCommandPtr
                "vkGetPhysicalDeviceXlibPresentationSupportKHR"
                physicalDevice.cmds.pGetPhysicalDeviceXlibPresentationSupportKHR
            )
            (castPtr physicalDevice.handle)
            queueFamilyIndex
            dpy
            visualID
        )
    let ret_ = (retw_ /= (0 :: Word32))
    pure ret_
