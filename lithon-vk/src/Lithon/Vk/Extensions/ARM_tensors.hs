-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.ARM_tensors (
  TensorCreateFlagBitsARM (..),
  TensorTilingARM (..),
  TensorUsageFlagBitsARM (..),
  TensorViewCreateFlagBitsARM,
  TensorCreateFlagsARM,
  TensorUsageFlagsARM,
  TensorViewCreateFlagsARM,
  TensorCreateInfoARM (..),
  TensorDescriptionARM (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (BitPos (..), Flags (..), FlagsWire)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Enums.Format
import Lithon.Vk.Core10.Enums.SharingMode

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkTensorCreateFlagBitsARM.html VkTensorCreateFlagBitsARM>
-- Available: VK_ARM_tensors
data TensorCreateFlagBitsARM
  = TensorCreateMutableFormatBitARM
  | TensorCreateProtectedBitARM
  deriving stock (Eq, Ord, Show)

type instance FlagsWire TensorCreateFlagBitsARM = Word64

instance BitPos TensorCreateFlagBitsARM where
  bitOf = \case
    TensorCreateMutableFormatBitARM -> Flags 0x1
    TensorCreateProtectedBitARM -> Flags 0x2
  allBits =
    [ TensorCreateMutableFormatBitARM
    , TensorCreateProtectedBitARM
    ]

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkTensorTilingARM.html VkTensorTilingARM>
-- Available: VK_ARM_tensors
data TensorTilingARM
  = TensorTilingOptimalARM
  | TensorTilingLinearARM
  deriving stock (Eq, Ord, Show)

instance HasWire TensorTilingARM where
  toWire = \case
    TensorTilingOptimalARM -> 0
    TensorTilingLinearARM -> 1
  fromWire = \case
    0 -> Just TensorTilingOptimalARM
    1 -> Just TensorTilingLinearARM
    _ -> Nothing

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkTensorUsageFlagBitsARM.html VkTensorUsageFlagBitsARM>
-- Available: VK_ARM_tensors
data TensorUsageFlagBitsARM
  = TensorUsageShaderBitARM
  | TensorUsageTransferSrcBitARM
  | TensorUsageTransferDstBitARM
  | TensorUsageImageAliasingBitARM
  deriving stock (Eq, Ord, Show)

type instance FlagsWire TensorUsageFlagBitsARM = Word64

instance BitPos TensorUsageFlagBitsARM where
  bitOf = \case
    TensorUsageShaderBitARM -> Flags 0x2
    TensorUsageTransferSrcBitARM -> Flags 0x4
    TensorUsageTransferDstBitARM -> Flags 0x8
    TensorUsageImageAliasingBitARM -> Flags 0x10
  allBits =
    [ TensorUsageShaderBitARM
    , TensorUsageTransferSrcBitARM
    , TensorUsageTransferDstBitARM
    , TensorUsageImageAliasingBitARM
    ]

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkTensorViewCreateFlagBitsARM.html VkTensorViewCreateFlagBitsARM>
-- Available: VK_ARM_tensors
data TensorViewCreateFlagBitsARM
  deriving stock (Eq, Ord, Show)

type instance FlagsWire TensorViewCreateFlagBitsARM = Word64

instance BitPos TensorViewCreateFlagBitsARM where
  bitOf = \case {}
  allBits =
    []

type TensorCreateFlagsARM = Flags TensorCreateFlagBitsARM

type TensorUsageFlagsARM = Flags TensorUsageFlagBitsARM

type TensorViewCreateFlagsARM = Flags TensorViewCreateFlagBitsARM

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkTensorCreateInfoARM.html VkTensorCreateInfoARM>
-- Available: VK_ARM_tensors
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @queueFamilyIndexCount@ is derived from @queueFamilyIndices@'s length.
data TensorCreateInfoARM (es :: [Type]) = TensorCreateInfoARM
  { next :: Chain es
  , flags :: TensorCreateFlagsARM
  , description :: TensorDescriptionARM
  , sharingMode :: SharingMode
  , queueFamilyIndices :: V.Vector Word32
  }

instance (AllExtends TensorCreateInfoARM es, PokeChain es) => CStruct (TensorCreateInfoARM es) where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000460000 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.flags)
    p_description <- new x.description
    liftIO (pokeByteOff p 24 p_description)
    liftIO (pokeByteOff p 32 (toWire x.sharingMode))
    p_queueFamilyIndices <- arenaBytes (4 * max 1 (V.length x.queueFamilyIndices)) 4
    liftIO (V.imapM_ (\i v -> pokeByteOff p_queueFamilyIndices (i * 4) v) x.queueFamilyIndices)
    liftIO
      ( pokeByteOff
          p
          40
          (if V.null x.queueFamilyIndices then nullPtr else (castPtr p_queueFamilyIndices :: Ptr ()))
      )
    liftIO (pokeByteOff p 36 (fromIntegral (V.length x.queueFamilyIndices) :: Word32))
    pure ()

instance
  (AllExtends TensorCreateInfoARM es, PokeChain es, PeekChain es)
  => FromCStruct (TensorCreateInfoARM es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_flags <- (peekByteOff p 16)
    v_description <- peekCStruct =<< peekByteOff p 24
    v_sharingMode <- (unsafeFromWire <$> peekByteOff p 32)
    v_queueFamilyIndices <- do
      n <- (peekByteOff p 36 :: IO Word32)
      q <- peekByteOff p 40 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 4))
    pure
      TensorCreateInfoARM
        { next = v_next
        , flags = v_flags
        , description = v_description
        , sharingMode = v_sharingMode
        , queueFamilyIndices = v_queueFamilyIndices
        }

instance (NilChain es) => Nil (TensorCreateInfoARM es) where
  nil =
    TensorCreateInfoARM
      { next = nilChain
      , flags = mempty
      , description = nil
      , sharingMode = SharingModeExclusive
      , queueFamilyIndices = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkTensorDescriptionARM.html VkTensorDescriptionARM>
-- Available: VK_ARM_tensors
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @dimensionCount@ is derived from @dimensions@'s length.
data TensorDescriptionARM = TensorDescriptionARM
  { tiling :: TensorTilingARM
  , format :: Open Format
  , dimensions :: V.Vector Int64
  , strides :: V.Vector Int64
  , usage :: TensorUsageFlagsARM
  }

instance CStruct TensorDescriptionARM where
  cSize = 56
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000460006 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (toWire x.tiling))
    liftIO (pokeByteOff p 20 (openToWire x.format))
    p_dimensions <- arenaBytes (8 * max 1 (V.length x.dimensions)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_dimensions (i * 8) v) x.dimensions)
    liftIO
      (pokeByteOff p 32 (if V.null x.dimensions then nullPtr else (castPtr p_dimensions :: Ptr ())))
    liftIO (pokeByteOff p 24 (fromIntegral (V.length x.dimensions) :: Word32))
    p_strides <- arenaBytes (8 * max 1 (V.length x.strides)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_strides (i * 8) v) x.strides)
    liftIO (pokeByteOff p 40 (if V.null x.strides then nullPtr else (castPtr p_strides :: Ptr ())))
    unless
      (V.length x.strides == V.length x.dimensions)
      (error "lithon: strides length must match dimensions")
    liftIO (pokeByteOff p 48 x.usage)
    pure ()

instance FromCStruct TensorDescriptionARM where
  peekCStruct p = do
    v_tiling <- (unsafeFromWire <$> peekByteOff p 16)
    v_format <- (openFromWire <$> peekByteOff p 20)
    v_dimensions <- do
      n <- (peekByteOff p 24 :: IO Word32)
      q <- peekByteOff p 32 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_strides <- do
      n <- (peekByteOff p 24 :: IO Word32)
      q <- peekByteOff p 40 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_usage <- (peekByteOff p 48)
    pure
      TensorDescriptionARM
        { tiling = v_tiling
        , format = v_format
        , dimensions = v_dimensions
        , strides = v_strides
        , usage = v_usage
        }

instance Nil TensorDescriptionARM where
  nil =
    TensorDescriptionARM
      { tiling = TensorTilingOptimalARM
      , format = Known FormatUndefined
      , dimensions = V.empty
      , strides = V.empty
      , usage = mempty
      }

instance Chainable TensorDescriptionARM where
  sTypeWire = 1000460006
  chainOffset = 8
