-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_unified_image_layouts (
  PhysicalDeviceUnifiedImageLayoutsFeaturesKHR (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceUnifiedImageLayoutsFeaturesKHR.html VkPhysicalDeviceUnifiedImageLayoutsFeaturesKHR>
-- Available: VK_KHR_unified_image_layouts
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceUnifiedImageLayoutsFeaturesKHR = PhysicalDeviceUnifiedImageLayoutsFeaturesKHR
  { unifiedImageLayouts :: Bool
  , unifiedImageLayoutsVideo :: Bool
  }

instance CStruct PhysicalDeviceUnifiedImageLayoutsFeaturesKHR where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000527000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.unifiedImageLayouts then TRUE else FALSE))
    liftIO (pokeByteOff p 20 (if x.unifiedImageLayoutsVideo then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceUnifiedImageLayoutsFeaturesKHR where
  peekCStruct p = do
    v_unifiedImageLayouts <- (toBool <$> peekByteOff p 16)
    v_unifiedImageLayoutsVideo <- (toBool <$> peekByteOff p 20)
    pure
      PhysicalDeviceUnifiedImageLayoutsFeaturesKHR
        { unifiedImageLayouts = v_unifiedImageLayouts
        , unifiedImageLayoutsVideo = v_unifiedImageLayoutsVideo
        }

instance Nil PhysicalDeviceUnifiedImageLayoutsFeaturesKHR where
  nil =
    PhysicalDeviceUnifiedImageLayoutsFeaturesKHR
      { unifiedImageLayouts = False
      , unifiedImageLayoutsVideo = False
      }

instance Chainable PhysicalDeviceUnifiedImageLayoutsFeaturesKHR where
  sTypeWire = 1000527000
  chainOffset = 8
