-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_memory_priority (
  MemoryPriorityAllocateInfoEXT (..),
  PhysicalDeviceMemoryPriorityFeaturesEXT (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMemoryPriorityAllocateInfoEXT.html VkMemoryPriorityAllocateInfoEXT>
-- Available: VK_EXT_memory_priority
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data MemoryPriorityAllocateInfoEXT = MemoryPriorityAllocateInfoEXT
  { priority :: Float
  }

instance CStruct MemoryPriorityAllocateInfoEXT where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000238001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.priority)
    pure ()

instance FromCStruct MemoryPriorityAllocateInfoEXT where
  peekCStruct p = do
    v_priority <- (peekByteOff p 16)
    pure MemoryPriorityAllocateInfoEXT{priority = v_priority}

instance Nil MemoryPriorityAllocateInfoEXT where
  nil =
    MemoryPriorityAllocateInfoEXT
      { priority = 0
      }

instance Chainable MemoryPriorityAllocateInfoEXT where
  sTypeWire = 1000238001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceMemoryPriorityFeaturesEXT.html VkPhysicalDeviceMemoryPriorityFeaturesEXT>
-- Available: VK_EXT_memory_priority
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceMemoryPriorityFeaturesEXT = PhysicalDeviceMemoryPriorityFeaturesEXT
  { memoryPriority :: Bool
  }

instance CStruct PhysicalDeviceMemoryPriorityFeaturesEXT where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000238000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.memoryPriority then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceMemoryPriorityFeaturesEXT where
  peekCStruct p = do
    v_memoryPriority <- (toBool <$> peekByteOff p 16)
    pure PhysicalDeviceMemoryPriorityFeaturesEXT{memoryPriority = v_memoryPriority}

instance Nil PhysicalDeviceMemoryPriorityFeaturesEXT where
  nil =
    PhysicalDeviceMemoryPriorityFeaturesEXT
      { memoryPriority = False
      }

instance Chainable PhysicalDeviceMemoryPriorityFeaturesEXT where
  sTypeWire = 1000238000
  chainOffset = 8
