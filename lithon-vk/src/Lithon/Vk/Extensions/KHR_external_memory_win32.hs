-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_external_memory_win32 (
  ExportMemoryWin32HandleInfoKHR (..),
  ImportMemoryWin32HandleInfoKHR (..),
  MemoryGetWin32HandleInfoKHR (..),
  MemoryWin32HandlePropertiesKHR (..),
  getMemoryWin32HandleKHR,
  getMemoryWin32HandlePropertiesKHR,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)
import Lithon.Core.Platform (HANDLE (..), LPCWSTR (..))

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core11.Enums.ExternalMemoryHandleTypeFlagBits
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkExportMemoryWin32HandleInfoKHR.html VkExportMemoryWin32HandleInfoKHR>
-- Available: VK_KHR_external_memory_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @pAttributes@ is deliberately un-modelled (raw pointer); see the module notes.
data ExportMemoryWin32HandleInfoKHR = ExportMemoryWin32HandleInfoKHR
  { pAttributes :: Ptr ()
  , dwAccess :: Word32
  , name :: LPCWSTR
  }

instance CStruct ExportMemoryWin32HandleInfoKHR where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000073001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.pAttributes)
    liftIO (pokeByteOff p 24 x.dwAccess)
    liftIO (pokeByteOff p 32 x.name)
    pure ()

instance FromCStruct ExportMemoryWin32HandleInfoKHR where
  peekCStruct p = do
    v_pAttributes <- peekByteOff p 16
    v_dwAccess <- (peekByteOff p 24)
    v_name <- (peekByteOff p 32)
    pure
      ExportMemoryWin32HandleInfoKHR{pAttributes = v_pAttributes, dwAccess = v_dwAccess, name = v_name}

instance Nil ExportMemoryWin32HandleInfoKHR where
  nil =
    ExportMemoryWin32HandleInfoKHR
      { pAttributes = nullPtr
      , dwAccess = 0
      , name = LPCWSTR nullPtr
      }

instance Chainable ExportMemoryWin32HandleInfoKHR where
  sTypeWire = 1000073001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkImportMemoryWin32HandleInfoKHR.html VkImportMemoryWin32HandleInfoKHR>
-- Available: VK_KHR_external_memory_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data ImportMemoryWin32HandleInfoKHR = ImportMemoryWin32HandleInfoKHR
  { handleType :: Flags ExternalMemoryHandleTypeFlagBits
  , handle :: HANDLE
  , name :: LPCWSTR
  }

instance CStruct ImportMemoryWin32HandleInfoKHR where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000073000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.handleType)
    liftIO (pokeByteOff p 24 x.handle)
    liftIO (pokeByteOff p 32 x.name)
    pure ()

instance FromCStruct ImportMemoryWin32HandleInfoKHR where
  peekCStruct p = do
    v_handleType <- (peekByteOff p 16)
    v_handle <- (peekByteOff p 24)
    v_name <- (peekByteOff p 32)
    pure ImportMemoryWin32HandleInfoKHR{handleType = v_handleType, handle = v_handle, name = v_name}

instance Nil ImportMemoryWin32HandleInfoKHR where
  nil =
    ImportMemoryWin32HandleInfoKHR
      { handleType = mempty
      , handle = HANDLE nullPtr
      , name = LPCWSTR nullPtr
      }

instance Chainable ImportMemoryWin32HandleInfoKHR where
  sTypeWire = 1000073000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMemoryGetWin32HandleInfoKHR.html VkMemoryGetWin32HandleInfoKHR>
-- Available: VK_KHR_external_memory_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data MemoryGetWin32HandleInfoKHR = MemoryGetWin32HandleInfoKHR
  { memory :: DeviceMemory
  , handleType :: Flags ExternalMemoryHandleTypeFlagBits
  }

instance CStruct MemoryGetWin32HandleInfoKHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000073003 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.memory)
    liftIO (pokeByteOff p 24 x.handleType)
    pure ()

instance FromCStruct MemoryGetWin32HandleInfoKHR where
  peekCStruct p = do
    v_memory <- (peekByteOff p 16)
    v_handleType <- (peekByteOff p 24)
    pure MemoryGetWin32HandleInfoKHR{memory = v_memory, handleType = v_handleType}

instance Nil MemoryGetWin32HandleInfoKHR where
  nil =
    MemoryGetWin32HandleInfoKHR
      { memory = nil
      , handleType = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMemoryWin32HandlePropertiesKHR.html VkMemoryWin32HandlePropertiesKHR>
-- Available: VK_KHR_external_memory_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data MemoryWin32HandlePropertiesKHR = MemoryWin32HandlePropertiesKHR
  { memoryTypeBits :: Word32
  }

instance CStruct MemoryWin32HandlePropertiesKHR where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000073002 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.memoryTypeBits)
    pure ()

instance FromCStruct MemoryWin32HandlePropertiesKHR where
  peekCStruct p = do
    v_memoryTypeBits <- (peekByteOff p 16)
    pure MemoryWin32HandlePropertiesKHR{memoryTypeBits = v_memoryTypeBits}

instance Nil MemoryWin32HandlePropertiesKHR where
  nil =
    MemoryWin32HandlePropertiesKHR
      { memoryTypeBits = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetMemoryWin32HandleKHR.html vkGetMemoryWin32HandleKHR>
-- Available: VK_KHR_external_memory_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
getMemoryWin32HandleKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> MemoryGetWin32HandleInfoKHR
  -> m (Outcome (Ptr ()))
getMemoryWin32HandleKHR device getWin32HandleInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_getWin32HandleInfo :: Ptr () <- castPtr <$> new getWin32HandleInfo
    p_o_handle <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkGetMemoryWin32HandleKHR
            (checkCommandPtr "vkGetMemoryWin32HandleKHR" device.cmds.pGetMemoryWin32HandleKHR)
            (castPtr device.handle)
            p_getWin32HandleInfo
            (castPtr p_o_handle)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_handle <- liftIO (peekByteOff p_o_handle 0 :: IO (Ptr ()))
      let o_handle = w_o_handle
      pure (Ok (openFromWire r_) o_handle)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetMemoryWin32HandlePropertiesKHR.html vkGetMemoryWin32HandlePropertiesKHR>
-- Available: VK_KHR_external_memory_win32
-- Platform-specific (guarded by @VK_USE_PLATFORM_WIN32_KHR@ in C; emitted unconditionally here).
getMemoryWin32HandlePropertiesKHR
  :: forall m
   . (MonadIO m)
  => Device
  -> Flags ExternalMemoryHandleTypeFlagBits
  -> Ptr ()
  -> m (Outcome MemoryWin32HandlePropertiesKHR)
getMemoryWin32HandlePropertiesKHR device handleType handle =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_o_memoryWin32HandleProperties :: Ptr () <- castPtr <$> new (nil :: MemoryWin32HandlePropertiesKHR)
    r_ <-
      liftIO
        ( mkGetMemoryWin32HandlePropertiesKHR
            (checkCommandPtr "vkGetMemoryWin32HandlePropertiesKHR" device.cmds.pGetMemoryWin32HandlePropertiesKHR)
            (castPtr device.handle)
            ((\(Flags w) -> w) handleType)
            handle
            p_o_memoryWin32HandleProperties
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      o_memoryWin32HandleProperties <- liftIO (peekCStruct (castPtr p_o_memoryWin32HandleProperties))
      pure (Ok (openFromWire r_) o_memoryWin32HandleProperties)
