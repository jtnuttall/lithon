-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_shader_object (
  ShaderCodeTypeEXT (..),
  ShaderCreateFlagBitsEXT (..),
  ShaderCreateFlagsEXT,
  ShaderEXT (..),
  PhysicalDeviceShaderObjectFeaturesEXT (..),
  PhysicalDeviceShaderObjectPropertiesEXT (..),
  ShaderCreateInfoEXT (..),
  cmdBindShadersEXT,
  cmdSetLogicOpEXT,
  cmdSetPatchControlPointsEXT,
  createShadersEXT,
  withShadersEXT,
  destroyShaderEXT,
  getShaderBinaryDataEXT,
) where

import Control.Exception (bracket)
import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Control.Monad.IO.Unlift (MonadUnliftIO, withRunInIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Primitive.Types (Prim)
import Data.Vector qualified as V
import Data.Vector.Generic qualified as VG
import Data.Vector.Generic.Mutable qualified as VGM
import Data.Vector.Primitive qualified as P
import Data.Vector.Unboxed qualified as VU
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable, peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems, peekFixedBytes, pokeFixedBytes)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (BitPos (..), Flags (..), FlagsWire)
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)
import Lithon.Core.Unbox (UnboxViaPrim (..))

import Lithon.Vk.Core10.ComputePipelineCommands
import Lithon.Vk.Core10.DeviceInitialization
import Lithon.Vk.Core10.Enums.LogicOp
import Lithon.Vk.Core10.Enums.ShaderStageFlagBits
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core10.PipelineLayoutCommands
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkShaderCodeTypeEXT.html VkShaderCodeTypeEXT>
-- Available: VK_EXT_shader_object
data ShaderCodeTypeEXT
  = ShaderCodeTypeBinaryEXT
  | ShaderCodeTypeSpirvEXT
  deriving stock (Eq, Ord, Show)

instance HasWire ShaderCodeTypeEXT where
  toWire = \case
    ShaderCodeTypeBinaryEXT -> 0
    ShaderCodeTypeSpirvEXT -> 1
  fromWire = \case
    0 -> Just ShaderCodeTypeBinaryEXT
    1 -> Just ShaderCodeTypeSpirvEXT
    _ -> Nothing

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkShaderCreateFlagBitsEXT.html VkShaderCreateFlagBitsEXT>
-- Available: VK_EXT_shader_object
data ShaderCreateFlagBitsEXT
  = ShaderCreateLinkStageBitEXT
  | ShaderCreateAllowVaryingSubgroupSizeBitEXT
  | ShaderCreateRequireFullSubgroupsBitEXT
  | ShaderCreateNoTaskShaderBitEXT
  | ShaderCreateDispatchBaseBitEXT
  | ShaderCreateFragmentShadingRateAttachmentBitEXT
  deriving stock (Eq, Ord, Show)

type instance FlagsWire ShaderCreateFlagBitsEXT = Word32

instance BitPos ShaderCreateFlagBitsEXT where
  bitOf = \case
    ShaderCreateLinkStageBitEXT -> Flags 0x1
    ShaderCreateAllowVaryingSubgroupSizeBitEXT -> Flags 0x2
    ShaderCreateRequireFullSubgroupsBitEXT -> Flags 0x4
    ShaderCreateNoTaskShaderBitEXT -> Flags 0x8
    ShaderCreateDispatchBaseBitEXT -> Flags 0x10
    ShaderCreateFragmentShadingRateAttachmentBitEXT -> Flags 0x20
  allBits =
    [ ShaderCreateLinkStageBitEXT
    , ShaderCreateAllowVaryingSubgroupSizeBitEXT
    , ShaderCreateRequireFullSubgroupsBitEXT
    , ShaderCreateNoTaskShaderBitEXT
    , ShaderCreateDispatchBaseBitEXT
    , ShaderCreateFragmentShadingRateAttachmentBitEXT
    ]

type ShaderCreateFlagsEXT = Flags ShaderCreateFlagBitsEXT

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkShaderEXT.html VkShaderEXT>
-- Available: VK_EXT_shader_object
newtype ShaderEXT = ShaderEXT Word64
  deriving newtype (Eq, Ord, Prim, Show, Storable)

newtype instance VU.MVector s ShaderEXT = MV_ShaderEXT (P.MVector s ShaderEXT)

newtype instance VU.Vector ShaderEXT = V_ShaderEXT (P.Vector ShaderEXT)

deriving via (UnboxViaPrim ShaderEXT) instance VGM.MVector VU.MVector ShaderEXT

deriving via (UnboxViaPrim ShaderEXT) instance VG.Vector VU.Vector ShaderEXT

instance VU.Unbox ShaderEXT

-- | @VK_NULL_HANDLE@.
instance Nil ShaderEXT where
  nil = ShaderEXT 0

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceShaderObjectFeaturesEXT.html VkPhysicalDeviceShaderObjectFeaturesEXT>
-- Available: VK_EXT_shader_object
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceShaderObjectFeaturesEXT = PhysicalDeviceShaderObjectFeaturesEXT
  { shaderObject :: Bool
  }

instance CStruct PhysicalDeviceShaderObjectFeaturesEXT where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000482000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.shaderObject then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceShaderObjectFeaturesEXT where
  peekCStruct p = do
    v_shaderObject <- (toBool <$> peekByteOff p 16)
    pure PhysicalDeviceShaderObjectFeaturesEXT{shaderObject = v_shaderObject}

instance Nil PhysicalDeviceShaderObjectFeaturesEXT where
  nil =
    PhysicalDeviceShaderObjectFeaturesEXT
      { shaderObject = False
      }

instance Chainable PhysicalDeviceShaderObjectFeaturesEXT where
  sTypeWire = 1000482000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceShaderObjectPropertiesEXT.html VkPhysicalDeviceShaderObjectPropertiesEXT>
-- Available: VK_EXT_shader_object
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceShaderObjectPropertiesEXT = PhysicalDeviceShaderObjectPropertiesEXT
  { shaderBinaryUUID :: ByteString
  , shaderBinaryVersion :: Word32
  }

instance CStruct PhysicalDeviceShaderObjectPropertiesEXT where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000482001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeFixedBytes 16 p 16 x.shaderBinaryUUID)
    liftIO (pokeByteOff p 32 x.shaderBinaryVersion)
    pure ()

instance FromCStruct PhysicalDeviceShaderObjectPropertiesEXT where
  peekCStruct p = do
    v_shaderBinaryUUID <- (peekFixedBytes 16 p 16)
    v_shaderBinaryVersion <- (peekByteOff p 32)
    pure
      PhysicalDeviceShaderObjectPropertiesEXT
        { shaderBinaryUUID = v_shaderBinaryUUID
        , shaderBinaryVersion = v_shaderBinaryVersion
        }

instance Nil PhysicalDeviceShaderObjectPropertiesEXT where
  nil =
    PhysicalDeviceShaderObjectPropertiesEXT
      { shaderBinaryUUID = ""
      , shaderBinaryVersion = 0
      }

instance Chainable PhysicalDeviceShaderObjectPropertiesEXT where
  sTypeWire = 1000482001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkShaderCreateInfoEXT.html VkShaderCreateInfoEXT>
-- Available: VK_EXT_shader_object
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @codeSize@ is derived from @code@'s length.
--   * @setLayoutCount@ is derived from @setLayouts@'s length.
--   * @pushConstantRangeCount@ is derived from @pushConstantRanges@'s length.
data ShaderCreateInfoEXT (es :: [Type]) = ShaderCreateInfoEXT
  { next :: Chain es
  , flags :: ShaderCreateFlagsEXT
  , stage :: Flags ShaderStageFlagBits
  , nextStage :: ShaderStageFlags
  , codeType :: ShaderCodeTypeEXT
  , code :: ByteString
  , name :: Maybe ByteString
  , setLayouts :: V.Vector DescriptorSetLayout
  , pushConstantRanges :: V.Vector PushConstantRange
  , specializationInfo :: Maybe SpecializationInfo
  }

instance (AllExtends ShaderCreateInfoEXT es, PokeChain es) => CStruct (ShaderCreateInfoEXT es) where
  cSize = 96
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000482002 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 20 x.stage)
    liftIO (pokeByteOff p 24 x.nextStage)
    liftIO (pokeByteOff p 28 (toWire x.codeType))
    p_code <- newBytes x.code
    liftIO (pokeByteOff p 40 (if BS.null x.code then nullPtr else (castPtr p_code :: Ptr ())))
    liftIO (pokeByteOff p 32 (fromIntegral (BS.length x.code) :: Word64))
    p_name <- maybe (pure nullPtr) newCString x.name
    liftIO (pokeByteOff p 48 p_name)
    p_setLayouts <- arenaBytes (8 * max 1 (V.length x.setLayouts)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_setLayouts (i * 8) v) x.setLayouts)
    liftIO
      (pokeByteOff p 64 (if V.null x.setLayouts then nullPtr else (castPtr p_setLayouts :: Ptr ())))
    liftIO (pokeByteOff p 56 (fromIntegral (V.length x.setLayouts) :: Word32))
    p_pushConstantRanges <-
      if V.null x.pushConstantRanges then pure nullPtr else newVector x.pushConstantRanges
    liftIO (pokeByteOff p 80 p_pushConstantRanges)
    liftIO (pokeByteOff p 72 (fromIntegral (V.length x.pushConstantRanges) :: Word32))
    p_specializationInfo <- maybe (pure nullPtr) new x.specializationInfo
    liftIO (pokeByteOff p 88 p_specializationInfo)
    pure ()

instance
  (AllExtends ShaderCreateInfoEXT es, PokeChain es, PeekChain es)
  => FromCStruct (ShaderCreateInfoEXT es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_flags <- (peekByteOff p 16)
    v_stage <- (peekByteOff p 20)
    v_nextStage <- (peekByteOff p 24)
    v_codeType <- (unsafeFromWire <$> peekByteOff p 28)
    v_code <- do
      n <- (peekByteOff p 32 :: IO Word64)
      q <- peekByteOff p 40
      if q == nullPtr then pure BS.empty else BS.packCStringLen (q, fromIntegral n)
    v_name <- do q <- peekByteOff p 48; if q == nullPtr then pure Nothing else Just <$> BS.packCString q
    v_setLayouts <- do
      n <- (peekByteOff p 56 :: IO Word32)
      q <- peekByteOff p 64 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_pushConstantRanges <- do
      n <- (peekByteOff p 72 :: IO Word32)
      q <- peekByteOff p 80
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    v_specializationInfo <- do q <- peekByteOff p 88; if q == nullPtr then pure Nothing else Just <$> peekCStruct q
    pure
      ShaderCreateInfoEXT
        { next = v_next
        , flags = v_flags
        , stage = v_stage
        , nextStage = v_nextStage
        , codeType = v_codeType
        , code = v_code
        , name = v_name
        , setLayouts = v_setLayouts
        , pushConstantRanges = v_pushConstantRanges
        , specializationInfo = v_specializationInfo
        }

instance (NilChain es) => Nil (ShaderCreateInfoEXT es) where
  nil =
    ShaderCreateInfoEXT
      { next = nilChain
      , flags = mempty
      , stage = mempty
      , nextStage = mempty
      , codeType = ShaderCodeTypeBinaryEXT
      , code = ""
      , name = Nothing
      , setLayouts = V.empty
      , pushConstantRanges = V.empty
      , specializationInfo = Nothing
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdBindShadersEXT.html vkCmdBindShadersEXT>
-- Available: VK_EXT_shader_object
cmdBindShadersEXT
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> V.Vector (Flags ShaderStageFlagBits)
  -> Maybe (V.Vector ShaderEXT)
  -> m ()
cmdBindShadersEXT commandBuffer stages shaders =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_stageCount = fromIntegral (V.length stages) :: Word32
    p_stages <- do
      pv_ <- arenaBytes (4 * max 1 (V.length stages)) 4
      liftIO (V.imapM_ (\i v_e -> pokeByteOff pv_ (i * 4) ((\(Flags w) -> w) v_e)) stages)
      pure (castPtr pv_ :: Ptr ())
    p_shaders <-
      maybe
        (pure nullPtr)
        ( \v_ -> do
            unless
              (V.length v_ == fromIntegral n_stageCount)
              (error "lithon: vkCmdBindShadersEXT: shaders length must match the count source")
            pv_ <- arenaBytes (8 * max 1 (V.length v_)) 8
            liftIO (V.imapM_ (\i v_e -> pokeByteOff pv_ (i * 8) (case v_e of ShaderEXT w_ -> w_)) v_)
            pure (castPtr pv_ :: Ptr ())
        )
        shaders
    liftIO
      ( mkCmdBindShadersEXT
          (checkCommandPtr "vkCmdBindShadersEXT" commandBuffer.cmds.pCmdBindShadersEXT)
          (castPtr commandBuffer.handle)
          n_stageCount
          p_stages
          p_shaders
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdSetLogicOpEXT.html vkCmdSetLogicOpEXT>
-- Available: VK_EXT_extended_dynamic_state2,VK_EXT_shader_object
cmdSetLogicOpEXT
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> LogicOp
  -> m ()
cmdSetLogicOpEXT commandBuffer logicOp =
  liftIO do
    liftIO
      ( mkCmdSetLogicOpEXT
          (checkCommandPtr "vkCmdSetLogicOpEXT" commandBuffer.cmds.pCmdSetLogicOpEXT)
          (castPtr commandBuffer.handle)
          (toWire logicOp)
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdSetPatchControlPointsEXT.html vkCmdSetPatchControlPointsEXT>
-- Available: VK_EXT_extended_dynamic_state2,VK_EXT_shader_object
cmdSetPatchControlPointsEXT
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> Word32
  -> m ()
cmdSetPatchControlPointsEXT commandBuffer patchControlPoints =
  liftIO do
    liftIO
      ( mkCmdSetPatchControlPointsEXT
          (checkCommandPtr "vkCmdSetPatchControlPointsEXT" commandBuffer.cmds.pCmdSetPatchControlPointsEXT)
          (castPtr commandBuffer.handle)
          patchControlPoints
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCreateShadersEXT.html vkCreateShadersEXT>
-- Available: VK_EXT_shader_object
createShadersEXT
  :: forall m
   . (MonadIO m)
  => Device
  -> V.Vector (SomeStruct ShaderCreateInfoEXT)
  -> Maybe AllocationCallbacks
  -> m (Outcome (V.Vector ShaderEXT))
createShadersEXT device createInfos allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_createInfoCount = fromIntegral (V.length createInfos) :: Word32
        n_createInfoCount' = n_createInfoCount
    p_createInfos <- do
      pv_ <- arenaBytes (96 * max 1 (V.length createInfos)) 8
      V.imapM_ (\i e_ -> withSomeStruct e_ (pokeCStruct (castPtr (pv_ `plusPtr` (i * 96))))) createInfos
      pure (castPtr pv_ :: Ptr ())
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    p_o_shaders <- arenaBytes (8 * max 1 (fromIntegral n_createInfoCount)) 8
    r_ <-
      liftIO
        ( mkCreateShadersEXT
            (checkCommandPtr "vkCreateShadersEXT" device.cmds.pCreateShadersEXT)
            (castPtr device.handle)
            n_createInfoCount
            p_createInfos
            p_allocator
            (castPtr p_o_shaders)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      o_shaders <-
        liftIO
          ( V.generateM
              (fromIntegral n_createInfoCount')
              (\i -> ShaderEXT <$> peekElemOff (castPtr p_o_shaders :: Ptr Word64) i)
          )
      pure (Ok (openFromWire r_) o_shaders)

withShadersEXT
  :: forall m r
   . (MonadUnliftIO m)
  => Device
  -> V.Vector (SomeStruct ShaderCreateInfoEXT)
  -> Maybe AllocationCallbacks
  -> (Outcome (V.Vector ShaderEXT) -> m r)
  -> m r
withShadersEXT device createInfos allocator cont_ =
  withRunInIO \run_ ->
    bracket
      (run_ (createShadersEXT device createInfos allocator))
      ( \o_ -> case o_ of
          Ok _ h_ -> run_ (V.mapM_ (\h1_ -> destroyShaderEXT device h1_ allocator) h_)
          Err _ -> pure ()
      )
      (run_ . cont_)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkDestroyShaderEXT.html vkDestroyShaderEXT>
-- Available: VK_EXT_shader_object
destroyShaderEXT
  :: forall m
   . (MonadIO m)
  => Device
  -> ShaderEXT
  -> Maybe AllocationCallbacks
  -> m ()
destroyShaderEXT device shader allocator =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_allocator :: Ptr () <- maybe (pure nullPtr) (fmap castPtr . new) allocator
    liftIO
      ( mkDestroyShaderEXT
          (checkCommandPtr "vkDestroyShaderEXT" device.cmds.pDestroyShaderEXT)
          (castPtr device.handle)
          (case shader of ShaderEXT w_ -> w_)
          p_allocator
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetShaderBinaryDataEXT.html vkGetShaderBinaryDataEXT>
-- Available: VK_EXT_shader_object
getShaderBinaryDataEXT
  :: forall m
   . (MonadIO m)
  => Device
  -> ShaderEXT
  -> m (Outcome ByteString)
getShaderBinaryDataEXT device shader =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 8 8
    liftIO (pokeByteOff p_n_ 0 (0 :: CSize))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetShaderBinaryDataEXT
                  (checkCommandPtr "vkGetShaderBinaryDataEXT" device.cmds.pGetShaderBinaryDataEXT)
                  (castPtr device.handle)
                  (case shader of ShaderEXT w_ -> w_)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pDataSize <- liftIO (peekByteOff p_n_ 0 :: IO CSize)
            p_o_data' <- arenaBytes (max 1 (fromIntegral n_pDataSize)) 8
            r2_ <-
              liftIO
                ( mkGetShaderBinaryDataEXT
                    (checkCommandPtr "vkGetShaderBinaryDataEXT" device.cmds.pGetShaderBinaryDataEXT)
                    (castPtr device.handle)
                    (case shader of ShaderEXT w_ -> w_)
                    (castPtr p_n_)
                    (castPtr p_o_data')
                )
            n_pDataSize' <- liftIO (peekByteOff p_n_ 0 :: IO CSize)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_data' <- liftIO (BS.packCStringLen (castPtr p_o_data', fromIntegral n_pDataSize'))
                pure (Ok (openFromWire r2_) o_data')
    goDual_
