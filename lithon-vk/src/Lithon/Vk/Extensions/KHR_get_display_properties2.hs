-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_get_display_properties2 (
  DisplayModeProperties2KHR (..),
  DisplayPlaneCapabilities2KHR (..),
  DisplayPlaneInfo2KHR (..),
  DisplayPlaneProperties2KHR (..),
  DisplayProperties2KHR (..),
  getDisplayModeProperties2KHR,
  getDisplayPlaneCapabilities2KHR,
  getPhysicalDeviceDisplayPlaneProperties2KHR,
  getPhysicalDeviceDisplayProperties2KHR,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Extensions.KHR_display
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayModeProperties2KHR.html VkDisplayModeProperties2KHR>
-- Available: VK_KHR_get_display_properties2
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DisplayModeProperties2KHR = DisplayModeProperties2KHR
  { displayModeProperties :: DisplayModePropertiesKHR
  }

instance CStruct DisplayModeProperties2KHR where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000121002 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.displayModeProperties)
    pure ()

instance FromCStruct DisplayModeProperties2KHR where
  peekCStruct p = do
    v_displayModeProperties <- (peekByteOff p 16)
    pure DisplayModeProperties2KHR{displayModeProperties = v_displayModeProperties}

instance Nil DisplayModeProperties2KHR where
  nil =
    DisplayModeProperties2KHR
      { displayModeProperties = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayPlaneCapabilities2KHR.html VkDisplayPlaneCapabilities2KHR>
-- Available: VK_KHR_get_display_properties2
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DisplayPlaneCapabilities2KHR = DisplayPlaneCapabilities2KHR
  { capabilities :: DisplayPlaneCapabilitiesKHR
  }

instance CStruct DisplayPlaneCapabilities2KHR where
  cSize = 88
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000121004 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.capabilities)
    pure ()

instance FromCStruct DisplayPlaneCapabilities2KHR where
  peekCStruct p = do
    v_capabilities <- (peekByteOff p 16)
    pure DisplayPlaneCapabilities2KHR{capabilities = v_capabilities}

instance Nil DisplayPlaneCapabilities2KHR where
  nil =
    DisplayPlaneCapabilities2KHR
      { capabilities = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayPlaneInfo2KHR.html VkDisplayPlaneInfo2KHR>
-- Available: VK_KHR_get_display_properties2
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DisplayPlaneInfo2KHR = DisplayPlaneInfo2KHR
  { mode :: DisplayModeKHR
  , planeIndex :: Word32
  }

instance CStruct DisplayPlaneInfo2KHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000121003 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.mode)
    liftIO (pokeByteOff p 24 x.planeIndex)
    pure ()

instance FromCStruct DisplayPlaneInfo2KHR where
  peekCStruct p = do
    v_mode <- (peekByteOff p 16)
    v_planeIndex <- (peekByteOff p 24)
    pure DisplayPlaneInfo2KHR{mode = v_mode, planeIndex = v_planeIndex}

instance Nil DisplayPlaneInfo2KHR where
  nil =
    DisplayPlaneInfo2KHR
      { mode = nil
      , planeIndex = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayPlaneProperties2KHR.html VkDisplayPlaneProperties2KHR>
-- Available: VK_KHR_get_display_properties2
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DisplayPlaneProperties2KHR = DisplayPlaneProperties2KHR
  { displayPlaneProperties :: DisplayPlanePropertiesKHR
  }

instance CStruct DisplayPlaneProperties2KHR where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000121001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.displayPlaneProperties)
    pure ()

instance FromCStruct DisplayPlaneProperties2KHR where
  peekCStruct p = do
    v_displayPlaneProperties <- (peekByteOff p 16)
    pure DisplayPlaneProperties2KHR{displayPlaneProperties = v_displayPlaneProperties}

instance Nil DisplayPlaneProperties2KHR where
  nil =
    DisplayPlaneProperties2KHR
      { displayPlaneProperties = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDisplayProperties2KHR.html VkDisplayProperties2KHR>
-- Available: VK_KHR_get_display_properties2
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data DisplayProperties2KHR = DisplayProperties2KHR
  { displayProperties :: DisplayPropertiesKHR
  }

instance CStruct DisplayProperties2KHR where
  cSize = 64
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000121000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    pokeCStruct (castPtr (p `plusPtr` 16)) x.displayProperties
    pure ()

instance FromCStruct DisplayProperties2KHR where
  peekCStruct p = do
    v_displayProperties <- peekCStruct (castPtr (p `plusPtr` 16))
    pure DisplayProperties2KHR{displayProperties = v_displayProperties}

instance Nil DisplayProperties2KHR where
  nil =
    DisplayProperties2KHR
      { displayProperties = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetDisplayModeProperties2KHR.html vkGetDisplayModeProperties2KHR>
-- Available: VK_KHR_get_display_properties2
getDisplayModeProperties2KHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> DisplayKHR
  -> m (Outcome (V.Vector DisplayModeProperties2KHR))
getDisplayModeProperties2KHR physicalDevice display =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetDisplayModeProperties2KHR
                  (checkCommandPtr "vkGetDisplayModeProperties2KHR" physicalDevice.cmds.pGetDisplayModeProperties2KHR)
                  (castPtr physicalDevice.handle)
                  (case display of DisplayKHR w_ -> w_)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pPropertyCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_properties <- arenaBytes (40 * max 1 (fromIntegral n_pPropertyCount)) 8
            forM_
              ([0 .. fromIntegral n_pPropertyCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_properties `plusPtr` (i * 40))) (nil :: DisplayModeProperties2KHR))
            r2_ <-
              liftIO
                ( mkGetDisplayModeProperties2KHR
                    (checkCommandPtr "vkGetDisplayModeProperties2KHR" physicalDevice.cmds.pGetDisplayModeProperties2KHR)
                    (castPtr physicalDevice.handle)
                    (case display of DisplayKHR w_ -> w_)
                    (castPtr p_n_)
                    (castPtr p_o_properties)
                )
            n_pPropertyCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_properties <- liftIO (peekElems (fromIntegral n_pPropertyCount') (castPtr p_o_properties))
                pure (Ok (openFromWire r2_) o_properties)
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetDisplayPlaneCapabilities2KHR.html vkGetDisplayPlaneCapabilities2KHR>
-- Available: VK_KHR_get_display_properties2
getDisplayPlaneCapabilities2KHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> DisplayPlaneInfo2KHR
  -> m (Outcome DisplayPlaneCapabilities2KHR)
getDisplayPlaneCapabilities2KHR physicalDevice displayPlaneInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_displayPlaneInfo :: Ptr () <- castPtr <$> new displayPlaneInfo
    p_o_capabilities :: Ptr () <- castPtr <$> new (nil :: DisplayPlaneCapabilities2KHR)
    r_ <-
      liftIO
        ( mkGetDisplayPlaneCapabilities2KHR
            ( checkCommandPtr
                "vkGetDisplayPlaneCapabilities2KHR"
                physicalDevice.cmds.pGetDisplayPlaneCapabilities2KHR
            )
            (castPtr physicalDevice.handle)
            p_displayPlaneInfo
            p_o_capabilities
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      o_capabilities <- liftIO (peekCStruct (castPtr p_o_capabilities))
      pure (Ok (openFromWire r_) o_capabilities)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetPhysicalDeviceDisplayPlaneProperties2KHR.html vkGetPhysicalDeviceDisplayPlaneProperties2KHR>
-- Available: VK_KHR_get_display_properties2
getPhysicalDeviceDisplayPlaneProperties2KHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> m (Outcome (V.Vector DisplayPlaneProperties2KHR))
getPhysicalDeviceDisplayPlaneProperties2KHR physicalDevice =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetPhysicalDeviceDisplayPlaneProperties2KHR
                  ( checkCommandPtr
                      "vkGetPhysicalDeviceDisplayPlaneProperties2KHR"
                      physicalDevice.cmds.pGetPhysicalDeviceDisplayPlaneProperties2KHR
                  )
                  (castPtr physicalDevice.handle)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pPropertyCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_properties <- arenaBytes (32 * max 1 (fromIntegral n_pPropertyCount)) 8
            forM_
              ([0 .. fromIntegral n_pPropertyCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_properties `plusPtr` (i * 32))) (nil :: DisplayPlaneProperties2KHR))
            r2_ <-
              liftIO
                ( mkGetPhysicalDeviceDisplayPlaneProperties2KHR
                    ( checkCommandPtr
                        "vkGetPhysicalDeviceDisplayPlaneProperties2KHR"
                        physicalDevice.cmds.pGetPhysicalDeviceDisplayPlaneProperties2KHR
                    )
                    (castPtr physicalDevice.handle)
                    (castPtr p_n_)
                    (castPtr p_o_properties)
                )
            n_pPropertyCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_properties <- liftIO (peekElems (fromIntegral n_pPropertyCount') (castPtr p_o_properties))
                pure (Ok (openFromWire r2_) o_properties)
    goDual_

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetPhysicalDeviceDisplayProperties2KHR.html vkGetPhysicalDeviceDisplayProperties2KHR>
-- Available: VK_KHR_get_display_properties2
getPhysicalDeviceDisplayProperties2KHR
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> m (Outcome (V.Vector DisplayProperties2KHR))
getPhysicalDeviceDisplayProperties2KHR physicalDevice =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_n_ <- arenaBytes 4 4
    liftIO (pokeByteOff p_n_ 0 (0 :: Word32))
    let goDual_ = do
          r1_ <-
            liftIO
              ( mkGetPhysicalDeviceDisplayProperties2KHR
                  ( checkCommandPtr
                      "vkGetPhysicalDeviceDisplayProperties2KHR"
                      physicalDevice.cmds.pGetPhysicalDeviceDisplayProperties2KHR
                  )
                  (castPtr physicalDevice.handle)
                  (castPtr p_n_)
                  nullPtr
              )
          if r1_ < 0 then
            pure (Err (openFromWire r1_))
          else do
            n_pPropertyCount <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            p_o_properties <- arenaBytes (64 * max 1 (fromIntegral n_pPropertyCount)) 8
            forM_
              ([0 .. fromIntegral n_pPropertyCount - 1] :: [Int])
              (\i -> pokeCStruct (castPtr (p_o_properties `plusPtr` (i * 64))) (nil :: DisplayProperties2KHR))
            r2_ <-
              liftIO
                ( mkGetPhysicalDeviceDisplayProperties2KHR
                    ( checkCommandPtr
                        "vkGetPhysicalDeviceDisplayProperties2KHR"
                        physicalDevice.cmds.pGetPhysicalDeviceDisplayProperties2KHR
                    )
                    (castPtr physicalDevice.handle)
                    (castPtr p_n_)
                    (castPtr p_o_properties)
                )
            n_pPropertyCount' <- liftIO (peekByteOff p_n_ 0 :: IO Word32)
            if r2_ == 5 then
              goDual_
            else
              if r2_ < 0 then
                pure (Err (openFromWire r2_))
              else do
                o_properties <- liftIO (peekElems (fromIntegral n_pPropertyCount') (castPtr p_o_properties))
                pure (Ok (openFromWire r2_) o_properties)
    goDual_
