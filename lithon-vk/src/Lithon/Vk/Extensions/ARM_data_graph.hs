-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.ARM_data_graph (
  DataGraphPipelineConstantARM (..),
  DataGraphPipelineCreateInfoARM (..),
  DataGraphPipelineResourceInfoARM (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core14.Enums.PipelineCreateFlagBits2

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDataGraphPipelineConstantARM.html VkDataGraphPipelineConstantARM>
-- Available: VK_ARM_data_graph
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @pConstantData@ is deliberately un-modelled (raw pointer); see the module notes.
data DataGraphPipelineConstantARM (es :: [Type]) = DataGraphPipelineConstantARM
  { next :: Chain es
  , id :: Word32
  , pConstantData :: Ptr ()
  }

instance
  (AllExtends DataGraphPipelineConstantARM es, PokeChain es)
  => CStruct (DataGraphPipelineConstantARM es)
  where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000507003 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.id)
    liftIO (pokeByteOff p 24 x.pConstantData)
    pure ()

instance
  (AllExtends DataGraphPipelineConstantARM es, PokeChain es, PeekChain es)
  => FromCStruct (DataGraphPipelineConstantARM es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_id <- (peekByteOff p 16)
    v_pConstantData <- peekByteOff p 24
    pure DataGraphPipelineConstantARM{next = v_next, id = v_id, pConstantData = v_pConstantData}

instance (NilChain es) => Nil (DataGraphPipelineConstantARM es) where
  nil =
    DataGraphPipelineConstantARM
      { next = nilChain
      , id = 0
      , pConstantData = nullPtr
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDataGraphPipelineCreateInfoARM.html VkDataGraphPipelineCreateInfoARM>
-- Available: VK_ARM_data_graph
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @resourceInfoCount@ is derived from @resourceInfos@'s length.
data DataGraphPipelineCreateInfoARM (es :: [Type]) = DataGraphPipelineCreateInfoARM
  { next :: Chain es
  , flags :: PipelineCreateFlags2
  , layout :: PipelineLayout
  , resourceInfos :: V.Vector (SomeStruct DataGraphPipelineResourceInfoARM)
  }

instance
  (AllExtends DataGraphPipelineCreateInfoARM es, PokeChain es)
  => CStruct (DataGraphPipelineCreateInfoARM es)
  where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000507000 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.layout)
    p_resourceInfos <- arenaBytes (32 * max 1 (V.length x.resourceInfos)) 8
    V.imapM_
      (\i e -> withSomeStruct e (pokeCStruct (castPtr (p_resourceInfos `plusPtr` (i * 32)))))
      x.resourceInfos
    liftIO
      (pokeByteOff p 40 (if V.null x.resourceInfos then nullPtr else (castPtr p_resourceInfos :: Ptr ())))
    liftIO (pokeByteOff p 32 (fromIntegral (V.length x.resourceInfos) :: Word32))
    pure ()

instance (NilChain es) => Nil (DataGraphPipelineCreateInfoARM es) where
  nil =
    DataGraphPipelineCreateInfoARM
      { next = nilChain
      , flags = mempty
      , layout = nil
      , resourceInfos = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkDataGraphPipelineResourceInfoARM.html VkDataGraphPipelineResourceInfoARM>
-- Available: VK_ARM_data_graph
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
data DataGraphPipelineResourceInfoARM (es :: [Type]) = DataGraphPipelineResourceInfoARM
  { next :: Chain es
  , descriptorSet :: Word32
  , binding :: Word32
  , arrayElement :: Word32
  }

instance
  (AllExtends DataGraphPipelineResourceInfoARM es, PokeChain es)
  => CStruct (DataGraphPipelineResourceInfoARM es)
  where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000507002 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.descriptorSet)
    liftIO (pokeByteOff p 20 x.binding)
    liftIO (pokeByteOff p 24 x.arrayElement)
    pure ()

instance
  (AllExtends DataGraphPipelineResourceInfoARM es, PokeChain es, PeekChain es)
  => FromCStruct (DataGraphPipelineResourceInfoARM es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_descriptorSet <- (peekByteOff p 16)
    v_binding <- (peekByteOff p 20)
    v_arrayElement <- (peekByteOff p 24)
    pure
      DataGraphPipelineResourceInfoARM
        { next = v_next
        , descriptorSet = v_descriptorSet
        , binding = v_binding
        , arrayElement = v_arrayElement
        }

instance (NilChain es) => Nil (DataGraphPipelineResourceInfoARM es) where
  nil =
    DataGraphPipelineResourceInfoARM
      { next = nilChain
      , descriptorSet = 0
      , binding = 0
      , arrayElement = 0
      }
