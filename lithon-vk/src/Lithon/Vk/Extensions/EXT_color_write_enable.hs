-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_color_write_enable (
  PhysicalDeviceColorWriteEnableFeaturesEXT (..),
  PipelineColorWriteCreateInfoEXT (..),
  cmdSetColorWriteEnableEXT,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceColorWriteEnableFeaturesEXT.html VkPhysicalDeviceColorWriteEnableFeaturesEXT>
-- Available: VK_EXT_color_write_enable
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceColorWriteEnableFeaturesEXT = PhysicalDeviceColorWriteEnableFeaturesEXT
  { colorWriteEnable :: Bool
  }

instance CStruct PhysicalDeviceColorWriteEnableFeaturesEXT where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000381000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.colorWriteEnable then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceColorWriteEnableFeaturesEXT where
  peekCStruct p = do
    v_colorWriteEnable <- (toBool <$> peekByteOff p 16)
    pure PhysicalDeviceColorWriteEnableFeaturesEXT{colorWriteEnable = v_colorWriteEnable}

instance Nil PhysicalDeviceColorWriteEnableFeaturesEXT where
  nil =
    PhysicalDeviceColorWriteEnableFeaturesEXT
      { colorWriteEnable = False
      }

instance Chainable PhysicalDeviceColorWriteEnableFeaturesEXT where
  sTypeWire = 1000381000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPipelineColorWriteCreateInfoEXT.html VkPipelineColorWriteCreateInfoEXT>
-- Available: VK_EXT_color_write_enable
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @attachmentCount@ is derived from @colorWriteEnables@'s length.
data PipelineColorWriteCreateInfoEXT = PipelineColorWriteCreateInfoEXT
  { colorWriteEnables :: V.Vector Bool
  }

instance CStruct PipelineColorWriteCreateInfoEXT where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000381001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    p_colorWriteEnables <- arenaBytes (4 * max 1 (V.length x.colorWriteEnables)) 4
    liftIO
      ( V.imapM_
          (\i v -> pokeByteOff p_colorWriteEnables (i * 4) (if v then TRUE else FALSE))
          x.colorWriteEnables
      )
    liftIO
      ( pokeByteOff
          p
          24
          (if V.null x.colorWriteEnables then nullPtr else (castPtr p_colorWriteEnables :: Ptr ()))
      )
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.colorWriteEnables) :: Word32))
    pure ()

instance FromCStruct PipelineColorWriteCreateInfoEXT where
  peekCStruct p = do
    v_colorWriteEnables <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then
        pure V.empty
      else
        V.generateM (fromIntegral n) (\i -> toBool <$> peekByteOff q (i * 4))
    pure PipelineColorWriteCreateInfoEXT{colorWriteEnables = v_colorWriteEnables}

instance Nil PipelineColorWriteCreateInfoEXT where
  nil =
    PipelineColorWriteCreateInfoEXT
      { colorWriteEnables = V.empty
      }

instance Chainable PipelineColorWriteCreateInfoEXT where
  sTypeWire = 1000381001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdSetColorWriteEnableEXT.html vkCmdSetColorWriteEnableEXT>
-- Available: VK_EXT_color_write_enable
cmdSetColorWriteEnableEXT
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> V.Vector Bool
  -> m ()
cmdSetColorWriteEnableEXT commandBuffer colorWriteEnables =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_attachmentCount = fromIntegral (V.length colorWriteEnables) :: Word32
    p_colorWriteEnables <- do
      pv_ <- arenaBytes (4 * max 1 (V.length colorWriteEnables)) 4
      liftIO
        (V.imapM_ (\i v_e -> pokeByteOff pv_ (i * 4) (if v_e then 1 else 0 :: Word32)) colorWriteEnables)
      pure (castPtr pv_ :: Ptr ())
    liftIO
      ( mkCmdSetColorWriteEnableEXT
          (checkCommandPtr "vkCmdSetColorWriteEnableEXT" commandBuffer.cmds.pCmdSetColorWriteEnableEXT)
          (castPtr commandBuffer.handle)
          n_attachmentCount
          p_colorWriteEnables
      )
    pure ()
