-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.KHR_maintenance6 (
  BindDescriptorBufferEmbeddedSamplersInfoEXT (..),
  SetDescriptorBufferOffsetsInfoEXT (..),
  cmdBindDescriptorBufferEmbeddedSamplers2EXT,
  cmdSetDescriptorBufferOffsets2EXT,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Enums.ShaderStageFlagBits
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkBindDescriptorBufferEmbeddedSamplersInfoEXT.html VkBindDescriptorBufferEmbeddedSamplersInfoEXT>
-- Available: VK_EXT_descriptor_buffer+VK_KHR_maintenance6
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
data BindDescriptorBufferEmbeddedSamplersInfoEXT (es :: [Type]) = BindDescriptorBufferEmbeddedSamplersInfoEXT
  { next :: Chain es
  , stageFlags :: ShaderStageFlags
  , layout :: PipelineLayout
  , set :: Word32
  }

instance
  (AllExtends BindDescriptorBufferEmbeddedSamplersInfoEXT es, PokeChain es)
  => CStruct (BindDescriptorBufferEmbeddedSamplersInfoEXT es)
  where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545008 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.stageFlags)
    liftIO (pokeByteOff p 24 x.layout)
    liftIO (pokeByteOff p 32 x.set)
    pure ()

instance
  (AllExtends BindDescriptorBufferEmbeddedSamplersInfoEXT es, PokeChain es, PeekChain es)
  => FromCStruct (BindDescriptorBufferEmbeddedSamplersInfoEXT es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_stageFlags <- (peekByteOff p 16)
    v_layout <- (peekByteOff p 24)
    v_set <- (peekByteOff p 32)
    pure
      BindDescriptorBufferEmbeddedSamplersInfoEXT
        { next = v_next
        , stageFlags = v_stageFlags
        , layout = v_layout
        , set = v_set
        }

instance (NilChain es) => Nil (BindDescriptorBufferEmbeddedSamplersInfoEXT es) where
  nil =
    BindDescriptorBufferEmbeddedSamplersInfoEXT
      { next = nilChain
      , stageFlags = mempty
      , layout = nil
      , set = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSetDescriptorBufferOffsetsInfoEXT.html VkSetDescriptorBufferOffsetsInfoEXT>
-- Available: VK_EXT_descriptor_buffer+VK_KHR_maintenance6
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @setCount@ is derived from @bufferIndices@'s length.
data SetDescriptorBufferOffsetsInfoEXT (es :: [Type]) = SetDescriptorBufferOffsetsInfoEXT
  { next :: Chain es
  , stageFlags :: ShaderStageFlags
  , layout :: PipelineLayout
  , firstSet :: Word32
  , bufferIndices :: V.Vector Word32
  , offsets :: V.Vector Word64
  }

instance
  (AllExtends SetDescriptorBufferOffsetsInfoEXT es, PokeChain es)
  => CStruct (SetDescriptorBufferOffsetsInfoEXT es)
  where
  cSize = 56
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545007 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.stageFlags)
    liftIO (pokeByteOff p 24 x.layout)
    liftIO (pokeByteOff p 32 x.firstSet)
    p_bufferIndices <- arenaBytes (4 * max 1 (V.length x.bufferIndices)) 4
    liftIO (V.imapM_ (\i v -> pokeByteOff p_bufferIndices (i * 4) v) x.bufferIndices)
    liftIO
      (pokeByteOff p 40 (if V.null x.bufferIndices then nullPtr else (castPtr p_bufferIndices :: Ptr ())))
    liftIO (pokeByteOff p 36 (fromIntegral (V.length x.bufferIndices) :: Word32))
    p_offsets <- arenaBytes (8 * max 1 (V.length x.offsets)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_offsets (i * 8) v) x.offsets)
    liftIO (pokeByteOff p 48 (if V.null x.offsets then nullPtr else (castPtr p_offsets :: Ptr ())))
    unless
      (V.length x.offsets == V.length x.bufferIndices)
      (error "lithon: offsets length must match bufferIndices")
    pure ()

instance
  (AllExtends SetDescriptorBufferOffsetsInfoEXT es, PokeChain es, PeekChain es)
  => FromCStruct (SetDescriptorBufferOffsetsInfoEXT es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_stageFlags <- (peekByteOff p 16)
    v_layout <- (peekByteOff p 24)
    v_firstSet <- (peekByteOff p 32)
    v_bufferIndices <- do
      n <- (peekByteOff p 36 :: IO Word32)
      q <- peekByteOff p 40 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 4))
    v_offsets <- do
      n <- (peekByteOff p 36 :: IO Word32)
      q <- peekByteOff p 48 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    pure
      SetDescriptorBufferOffsetsInfoEXT
        { next = v_next
        , stageFlags = v_stageFlags
        , layout = v_layout
        , firstSet = v_firstSet
        , bufferIndices = v_bufferIndices
        , offsets = v_offsets
        }

instance (NilChain es) => Nil (SetDescriptorBufferOffsetsInfoEXT es) where
  nil =
    SetDescriptorBufferOffsetsInfoEXT
      { next = nilChain
      , stageFlags = mempty
      , layout = nil
      , firstSet = 0
      , bufferIndices = V.empty
      , offsets = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdBindDescriptorBufferEmbeddedSamplers2EXT.html vkCmdBindDescriptorBufferEmbeddedSamplers2EXT>
-- Available: VK_EXT_descriptor_buffer+VK_KHR_maintenance6
-- __Deprecated__.
cmdBindDescriptorBufferEmbeddedSamplers2EXT
  :: forall m es1
   . (MonadIO m, AllExtends BindDescriptorBufferEmbeddedSamplersInfoEXT es1, PokeChain es1)
  => CommandBuffer
  -> BindDescriptorBufferEmbeddedSamplersInfoEXT es1
  -> m ()
cmdBindDescriptorBufferEmbeddedSamplers2EXT commandBuffer bindDescriptorBufferEmbeddedSamplersInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_bindDescriptorBufferEmbeddedSamplersInfo :: Ptr () <-
      castPtr <$> new bindDescriptorBufferEmbeddedSamplersInfo
    liftIO
      ( mkCmdBindDescriptorBufferEmbeddedSamplers2EXT
          ( checkCommandPtr
              "vkCmdBindDescriptorBufferEmbeddedSamplers2EXT"
              commandBuffer.cmds.pCmdBindDescriptorBufferEmbeddedSamplers2EXT
          )
          (castPtr commandBuffer.handle)
          p_bindDescriptorBufferEmbeddedSamplersInfo
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdSetDescriptorBufferOffsets2EXT.html vkCmdSetDescriptorBufferOffsets2EXT>
-- Available: VK_EXT_descriptor_buffer+VK_KHR_maintenance6
-- __Deprecated__.
cmdSetDescriptorBufferOffsets2EXT
  :: forall m es1
   . (MonadIO m, AllExtends SetDescriptorBufferOffsetsInfoEXT es1, PokeChain es1)
  => CommandBuffer
  -> SetDescriptorBufferOffsetsInfoEXT es1
  -> m ()
cmdSetDescriptorBufferOffsets2EXT commandBuffer setDescriptorBufferOffsetsInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_setDescriptorBufferOffsetsInfo :: Ptr () <- castPtr <$> new setDescriptorBufferOffsetsInfo
    liftIO
      ( mkCmdSetDescriptorBufferOffsets2EXT
          ( checkCommandPtr
              "vkCmdSetDescriptorBufferOffsets2EXT"
              commandBuffer.cmds.pCmdSetDescriptorBufferOffsets2EXT
          )
          (castPtr commandBuffer.handle)
          p_setDescriptorBufferOffsetsInfo
      )
    pure ()
