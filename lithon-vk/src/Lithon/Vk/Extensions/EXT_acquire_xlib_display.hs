-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Extensions.EXT_acquire_xlib_display (
  acquireXlibDisplayEXT,
  getRandROutputDisplayEXT,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Extensions.KHR_display
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkAcquireXlibDisplayEXT.html vkAcquireXlibDisplayEXT>
-- Available: VK_EXT_acquire_xlib_display
-- Platform-specific (guarded by @VK_USE_PLATFORM_XLIB_XRANDR_EXT@ in C; emitted unconditionally here).
acquireXlibDisplayEXT
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> Ptr ()
  -> DisplayKHR
  -> m (Outcome ())
acquireXlibDisplayEXT physicalDevice dpy display =
  liftIO do
    r_ <-
      liftIO
        ( mkAcquireXlibDisplayEXT
            (checkCommandPtr "vkAcquireXlibDisplayEXT" physicalDevice.cmds.pAcquireXlibDisplayEXT)
            (castPtr physicalDevice.handle)
            dpy
            (case display of DisplayKHR w_ -> w_)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetRandROutputDisplayEXT.html vkGetRandROutputDisplayEXT>
-- Available: VK_EXT_acquire_xlib_display
-- Platform-specific (guarded by @VK_USE_PLATFORM_XLIB_XRANDR_EXT@ in C; emitted unconditionally here).
getRandROutputDisplayEXT
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> Ptr ()
  -> Word64
  -> m (Outcome DisplayKHR)
getRandROutputDisplayEXT physicalDevice dpy rrOutput =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_o_display <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkGetRandROutputDisplayEXT
            (checkCommandPtr "vkGetRandROutputDisplayEXT" physicalDevice.cmds.pGetRandROutputDisplayEXT)
            (castPtr physicalDevice.handle)
            dpy
            rrOutput
            (castPtr p_o_display)
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      w_o_display <- liftIO (peekByteOff p_o_display 0 :: IO Word64)
      let o_display = DisplayKHR w_o_display
      pure (Ok (openFromWire r_) o_display)
