-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core11.Promoted_From_KHR_external_fence_capabilities (
  ExternalFenceProperties (..),
  PhysicalDeviceExternalFenceInfo (..),
  getPhysicalDeviceExternalFenceProperties,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core11.Enums.ExternalFenceFeatureFlagBits
import Lithon.Vk.Core11.Enums.ExternalFenceHandleTypeFlagBits
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkExternalFenceProperties.html VkExternalFenceProperties>
-- Available: VK_VERSION_1_1
-- Alias spellings: VkExternalFencePropertiesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data ExternalFenceProperties = ExternalFenceProperties
  { exportFromImportedHandleTypes :: ExternalFenceHandleTypeFlags
  , compatibleHandleTypes :: ExternalFenceHandleTypeFlags
  , externalFenceFeatures :: ExternalFenceFeatureFlags
  }

instance CStruct ExternalFenceProperties where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000112001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.exportFromImportedHandleTypes)
    liftIO (pokeByteOff p 20 x.compatibleHandleTypes)
    liftIO (pokeByteOff p 24 x.externalFenceFeatures)
    pure ()

instance FromCStruct ExternalFenceProperties where
  peekCStruct p = do
    v_exportFromImportedHandleTypes <- (peekByteOff p 16)
    v_compatibleHandleTypes <- (peekByteOff p 20)
    v_externalFenceFeatures <- (peekByteOff p 24)
    pure
      ExternalFenceProperties
        { exportFromImportedHandleTypes = v_exportFromImportedHandleTypes
        , compatibleHandleTypes = v_compatibleHandleTypes
        , externalFenceFeatures = v_externalFenceFeatures
        }

instance Nil ExternalFenceProperties where
  nil =
    ExternalFenceProperties
      { exportFromImportedHandleTypes = mempty
      , compatibleHandleTypes = mempty
      , externalFenceFeatures = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceExternalFenceInfo.html VkPhysicalDeviceExternalFenceInfo>
-- Available: VK_VERSION_1_1
-- Alias spellings: VkPhysicalDeviceExternalFenceInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceExternalFenceInfo = PhysicalDeviceExternalFenceInfo
  { handleType :: Flags ExternalFenceHandleTypeFlagBits
  }

instance CStruct PhysicalDeviceExternalFenceInfo where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000112000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.handleType)
    pure ()

instance FromCStruct PhysicalDeviceExternalFenceInfo where
  peekCStruct p = do
    v_handleType <- (peekByteOff p 16)
    pure PhysicalDeviceExternalFenceInfo{handleType = v_handleType}

instance Nil PhysicalDeviceExternalFenceInfo where
  nil =
    PhysicalDeviceExternalFenceInfo
      { handleType = mempty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetPhysicalDeviceExternalFenceProperties.html vkGetPhysicalDeviceExternalFenceProperties>
-- Available: VK_VERSION_1_1
-- Alias spellings: vkGetPhysicalDeviceExternalFencePropertiesKHR
getPhysicalDeviceExternalFenceProperties
  :: forall m
   . (MonadIO m)
  => PhysicalDevice
  -> PhysicalDeviceExternalFenceInfo
  -> m ExternalFenceProperties
getPhysicalDeviceExternalFenceProperties physicalDevice externalFenceInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_externalFenceInfo :: Ptr () <- castPtr <$> new externalFenceInfo
    p_o_externalFenceProperties :: Ptr () <- castPtr <$> new (nil :: ExternalFenceProperties)
    liftIO
      ( mkGetPhysicalDeviceExternalFenceProperties
          ( checkCommandPtr
              "vkGetPhysicalDeviceExternalFenceProperties"
              physicalDevice.cmds.pGetPhysicalDeviceExternalFenceProperties
          )
          (castPtr physicalDevice.handle)
          p_externalFenceInfo
          p_o_externalFenceProperties
      )
    o_externalFenceProperties <- liftIO (peekCStruct (castPtr p_o_externalFenceProperties))
    pure o_externalFenceProperties
