-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core14.Promoted_From_EXT_host_image_copy (
  CopyImageToImageInfo (..),
  CopyImageToMemoryInfo (..),
  CopyMemoryToImageInfo (..),
  HostImageCopyDevicePerformanceQuery (..),
  HostImageLayoutTransitionInfo (..),
  ImageSubresource2 (..),
  ImageToMemoryCopy (..),
  MemoryToImageCopy (..),
  PhysicalDeviceHostImageCopyFeatures (..),
  PhysicalDeviceHostImageCopyProperties (..),
  SubresourceHostMemcpySize (..),
  SubresourceLayout2 (..),
  copyImageToImage,
  copyImageToMemory,
  copyMemoryToImage,
  getImageSubresourceLayout2,
  transitionImageLayout,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems, peekFixedBytes, pokeFixedBytes)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.CommandBufferBuildingCommands
import Lithon.Vk.Core10.Enums.ImageLayout
import Lithon.Vk.Core10.FundamentalTypesUsedByManyCommandsAndStructures
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core10.ImageCommands
import Lithon.Vk.Core10.ImageViewCommands
import Lithon.Vk.Core10.SparseResourceMemoryManagementAPICommandsOptional
import Lithon.Vk.Core13.Other
import Lithon.Vk.Core14.Enums.HostImageCopyFlagBits
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkCopyImageToImageInfo.html VkCopyImageToImageInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkCopyImageToImageInfoEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @regionCount@ is derived from @regions@'s length.
data CopyImageToImageInfo = CopyImageToImageInfo
  { flags :: HostImageCopyFlags
  , srcImage :: Image
  , srcImageLayout :: Open ImageLayout
  , dstImage :: Image
  , dstImageLayout :: Open ImageLayout
  , regions :: V.Vector ImageCopy2
  }

instance CStruct CopyImageToImageInfo where
  cSize = 64
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270007 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.srcImage)
    liftIO (pokeByteOff p 32 (openToWire x.srcImageLayout))
    liftIO (pokeByteOff p 40 x.dstImage)
    liftIO (pokeByteOff p 48 (openToWire x.dstImageLayout))
    p_regions <- if V.null x.regions then pure nullPtr else newVector x.regions
    liftIO (pokeByteOff p 56 p_regions)
    liftIO (pokeByteOff p 52 (fromIntegral (V.length x.regions) :: Word32))
    pure ()

instance FromCStruct CopyImageToImageInfo where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_srcImage <- (peekByteOff p 24)
    v_srcImageLayout <- (openFromWire <$> peekByteOff p 32)
    v_dstImage <- (peekByteOff p 40)
    v_dstImageLayout <- (openFromWire <$> peekByteOff p 48)
    v_regions <- do
      n <- (peekByteOff p 52 :: IO Word32)
      q <- peekByteOff p 56
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure
      CopyImageToImageInfo
        { flags = v_flags
        , srcImage = v_srcImage
        , srcImageLayout = v_srcImageLayout
        , dstImage = v_dstImage
        , dstImageLayout = v_dstImageLayout
        , regions = v_regions
        }

instance Nil CopyImageToImageInfo where
  nil =
    CopyImageToImageInfo
      { flags = mempty
      , srcImage = nil
      , srcImageLayout = Known ImageLayoutUndefined
      , dstImage = nil
      , dstImageLayout = Known ImageLayoutUndefined
      , regions = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkCopyImageToMemoryInfo.html VkCopyImageToMemoryInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkCopyImageToMemoryInfoEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @regionCount@ is derived from @regions@'s length.
data CopyImageToMemoryInfo = CopyImageToMemoryInfo
  { flags :: HostImageCopyFlags
  , srcImage :: Image
  , srcImageLayout :: Open ImageLayout
  , regions :: V.Vector ImageToMemoryCopy
  }

instance CStruct CopyImageToMemoryInfo where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270004 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.srcImage)
    liftIO (pokeByteOff p 32 (openToWire x.srcImageLayout))
    p_regions <- if V.null x.regions then pure nullPtr else newVector x.regions
    liftIO (pokeByteOff p 40 p_regions)
    liftIO (pokeByteOff p 36 (fromIntegral (V.length x.regions) :: Word32))
    pure ()

instance FromCStruct CopyImageToMemoryInfo where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_srcImage <- (peekByteOff p 24)
    v_srcImageLayout <- (openFromWire <$> peekByteOff p 32)
    v_regions <- do
      n <- (peekByteOff p 36 :: IO Word32)
      q <- peekByteOff p 40
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure
      CopyImageToMemoryInfo
        { flags = v_flags
        , srcImage = v_srcImage
        , srcImageLayout = v_srcImageLayout
        , regions = v_regions
        }

instance Nil CopyImageToMemoryInfo where
  nil =
    CopyImageToMemoryInfo
      { flags = mempty
      , srcImage = nil
      , srcImageLayout = Known ImageLayoutUndefined
      , regions = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkCopyMemoryToImageInfo.html VkCopyMemoryToImageInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkCopyMemoryToImageInfoEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @regionCount@ is derived from @regions@'s length.
data CopyMemoryToImageInfo = CopyMemoryToImageInfo
  { flags :: HostImageCopyFlags
  , dstImage :: Image
  , dstImageLayout :: Open ImageLayout
  , regions :: V.Vector MemoryToImageCopy
  }

instance CStruct CopyMemoryToImageInfo where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270005 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.dstImage)
    liftIO (pokeByteOff p 32 (openToWire x.dstImageLayout))
    p_regions <- if V.null x.regions then pure nullPtr else newVector x.regions
    liftIO (pokeByteOff p 40 p_regions)
    liftIO (pokeByteOff p 36 (fromIntegral (V.length x.regions) :: Word32))
    pure ()

instance FromCStruct CopyMemoryToImageInfo where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_dstImage <- (peekByteOff p 24)
    v_dstImageLayout <- (openFromWire <$> peekByteOff p 32)
    v_regions <- do
      n <- (peekByteOff p 36 :: IO Word32)
      q <- peekByteOff p 40
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure
      CopyMemoryToImageInfo
        { flags = v_flags
        , dstImage = v_dstImage
        , dstImageLayout = v_dstImageLayout
        , regions = v_regions
        }

instance Nil CopyMemoryToImageInfo where
  nil =
    CopyMemoryToImageInfo
      { flags = mempty
      , dstImage = nil
      , dstImageLayout = Known ImageLayoutUndefined
      , regions = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkHostImageCopyDevicePerformanceQuery.html VkHostImageCopyDevicePerformanceQuery>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkHostImageCopyDevicePerformanceQueryEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data HostImageCopyDevicePerformanceQuery = HostImageCopyDevicePerformanceQuery
  { optimalDeviceAccess :: Bool
  , identicalMemoryLayout :: Bool
  }

instance CStruct HostImageCopyDevicePerformanceQuery where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270009 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.optimalDeviceAccess then TRUE else FALSE))
    liftIO (pokeByteOff p 20 (if x.identicalMemoryLayout then TRUE else FALSE))
    pure ()

instance FromCStruct HostImageCopyDevicePerformanceQuery where
  peekCStruct p = do
    v_optimalDeviceAccess <- (toBool <$> peekByteOff p 16)
    v_identicalMemoryLayout <- (toBool <$> peekByteOff p 20)
    pure
      HostImageCopyDevicePerformanceQuery
        { optimalDeviceAccess = v_optimalDeviceAccess
        , identicalMemoryLayout = v_identicalMemoryLayout
        }

instance Nil HostImageCopyDevicePerformanceQuery where
  nil =
    HostImageCopyDevicePerformanceQuery
      { optimalDeviceAccess = False
      , identicalMemoryLayout = False
      }

instance Chainable HostImageCopyDevicePerformanceQuery where
  sTypeWire = 1000270009
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkHostImageLayoutTransitionInfo.html VkHostImageLayoutTransitionInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkHostImageLayoutTransitionInfoEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data HostImageLayoutTransitionInfo = HostImageLayoutTransitionInfo
  { image :: Image
  , oldLayout :: Open ImageLayout
  , newLayout :: Open ImageLayout
  , subresourceRange :: ImageSubresourceRange
  }

instance CStruct HostImageLayoutTransitionInfo where
  cSize = 56
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270006 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.image)
    liftIO (pokeByteOff p 24 (openToWire x.oldLayout))
    liftIO (pokeByteOff p 28 (openToWire x.newLayout))
    liftIO (pokeByteOff p 32 x.subresourceRange)
    pure ()

instance FromCStruct HostImageLayoutTransitionInfo where
  peekCStruct p = do
    v_image <- (peekByteOff p 16)
    v_oldLayout <- (openFromWire <$> peekByteOff p 24)
    v_newLayout <- (openFromWire <$> peekByteOff p 28)
    v_subresourceRange <- (peekByteOff p 32)
    pure
      HostImageLayoutTransitionInfo
        { image = v_image
        , oldLayout = v_oldLayout
        , newLayout = v_newLayout
        , subresourceRange = v_subresourceRange
        }

instance Nil HostImageLayoutTransitionInfo where
  nil =
    HostImageLayoutTransitionInfo
      { image = nil
      , oldLayout = Known ImageLayoutUndefined
      , newLayout = Known ImageLayoutUndefined
      , subresourceRange = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkImageSubresource2.html VkImageSubresource2>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkImageSubresource2EXT, VkImageSubresource2KHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data ImageSubresource2 = ImageSubresource2
  { imageSubresource :: ImageSubresource
  }

instance CStruct ImageSubresource2 where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000338003 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.imageSubresource)
    pure ()

instance FromCStruct ImageSubresource2 where
  peekCStruct p = do
    v_imageSubresource <- (peekByteOff p 16)
    pure ImageSubresource2{imageSubresource = v_imageSubresource}

instance Nil ImageSubresource2 where
  nil =
    ImageSubresource2
      { imageSubresource = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkImageToMemoryCopy.html VkImageToMemoryCopy>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkImageToMemoryCopyEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @pHostPointer@ is deliberately un-modelled (raw pointer); see the module notes.
data ImageToMemoryCopy = ImageToMemoryCopy
  { pHostPointer :: Ptr ()
  , memoryRowLength :: Word32
  , memoryImageHeight :: Word32
  , imageSubresource :: ImageSubresourceLayers
  , imageOffset :: Offset3D
  , imageExtent :: Extent3D
  }

instance CStruct ImageToMemoryCopy where
  cSize = 72
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270003 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.pHostPointer)
    liftIO (pokeByteOff p 24 x.memoryRowLength)
    liftIO (pokeByteOff p 28 x.memoryImageHeight)
    liftIO (pokeByteOff p 32 x.imageSubresource)
    liftIO (pokeByteOff p 48 x.imageOffset)
    liftIO (pokeByteOff p 60 x.imageExtent)
    pure ()

instance FromCStruct ImageToMemoryCopy where
  peekCStruct p = do
    v_pHostPointer <- peekByteOff p 16
    v_memoryRowLength <- (peekByteOff p 24)
    v_memoryImageHeight <- (peekByteOff p 28)
    v_imageSubresource <- (peekByteOff p 32)
    v_imageOffset <- (peekByteOff p 48)
    v_imageExtent <- (peekByteOff p 60)
    pure
      ImageToMemoryCopy
        { pHostPointer = v_pHostPointer
        , memoryRowLength = v_memoryRowLength
        , memoryImageHeight = v_memoryImageHeight
        , imageSubresource = v_imageSubresource
        , imageOffset = v_imageOffset
        , imageExtent = v_imageExtent
        }

instance Nil ImageToMemoryCopy where
  nil =
    ImageToMemoryCopy
      { pHostPointer = nullPtr
      , memoryRowLength = 0
      , memoryImageHeight = 0
      , imageSubresource = nil
      , imageOffset = nil
      , imageExtent = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMemoryToImageCopy.html VkMemoryToImageCopy>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkMemoryToImageCopyEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @pHostPointer@ is deliberately un-modelled (raw pointer); see the module notes.
data MemoryToImageCopy = MemoryToImageCopy
  { pHostPointer :: Ptr ()
  , memoryRowLength :: Word32
  , memoryImageHeight :: Word32
  , imageSubresource :: ImageSubresourceLayers
  , imageOffset :: Offset3D
  , imageExtent :: Extent3D
  }

instance CStruct MemoryToImageCopy where
  cSize = 72
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270002 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.pHostPointer)
    liftIO (pokeByteOff p 24 x.memoryRowLength)
    liftIO (pokeByteOff p 28 x.memoryImageHeight)
    liftIO (pokeByteOff p 32 x.imageSubresource)
    liftIO (pokeByteOff p 48 x.imageOffset)
    liftIO (pokeByteOff p 60 x.imageExtent)
    pure ()

instance FromCStruct MemoryToImageCopy where
  peekCStruct p = do
    v_pHostPointer <- peekByteOff p 16
    v_memoryRowLength <- (peekByteOff p 24)
    v_memoryImageHeight <- (peekByteOff p 28)
    v_imageSubresource <- (peekByteOff p 32)
    v_imageOffset <- (peekByteOff p 48)
    v_imageExtent <- (peekByteOff p 60)
    pure
      MemoryToImageCopy
        { pHostPointer = v_pHostPointer
        , memoryRowLength = v_memoryRowLength
        , memoryImageHeight = v_memoryImageHeight
        , imageSubresource = v_imageSubresource
        , imageOffset = v_imageOffset
        , imageExtent = v_imageExtent
        }

instance Nil MemoryToImageCopy where
  nil =
    MemoryToImageCopy
      { pHostPointer = nullPtr
      , memoryRowLength = 0
      , memoryImageHeight = 0
      , imageSubresource = nil
      , imageOffset = nil
      , imageExtent = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceHostImageCopyFeatures.html VkPhysicalDeviceHostImageCopyFeatures>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceHostImageCopyFeaturesEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceHostImageCopyFeatures = PhysicalDeviceHostImageCopyFeatures
  { hostImageCopy :: Bool
  }

instance CStruct PhysicalDeviceHostImageCopyFeatures where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.hostImageCopy then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceHostImageCopyFeatures where
  peekCStruct p = do
    v_hostImageCopy <- (toBool <$> peekByteOff p 16)
    pure PhysicalDeviceHostImageCopyFeatures{hostImageCopy = v_hostImageCopy}

instance Nil PhysicalDeviceHostImageCopyFeatures where
  nil =
    PhysicalDeviceHostImageCopyFeatures
      { hostImageCopy = False
      }

instance Chainable PhysicalDeviceHostImageCopyFeatures where
  sTypeWire = 1000270000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceHostImageCopyProperties.html VkPhysicalDeviceHostImageCopyProperties>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceHostImageCopyPropertiesEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @copySrcLayoutCount@ is derived from @copySrcLayouts@'s length.
--   * @copyDstLayoutCount@ is derived from @copyDstLayouts@'s length.
data PhysicalDeviceHostImageCopyProperties = PhysicalDeviceHostImageCopyProperties
  { copySrcLayouts :: V.Vector (Open ImageLayout)
  , copyDstLayouts :: V.Vector (Open ImageLayout)
  , optimalTilingLayoutUUID :: ByteString
  , identicalMemoryTypeRequirements :: Bool
  }

instance CStruct PhysicalDeviceHostImageCopyProperties where
  cSize = 72
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    p_copySrcLayouts <- arenaBytes (4 * max 1 (V.length x.copySrcLayouts)) 4
    liftIO (V.imapM_ (\i v -> pokeByteOff p_copySrcLayouts (i * 4) (openToWire v)) x.copySrcLayouts)
    liftIO
      (pokeByteOff p 24 (if V.null x.copySrcLayouts then nullPtr else (castPtr p_copySrcLayouts :: Ptr ())))
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.copySrcLayouts) :: Word32))
    p_copyDstLayouts <- arenaBytes (4 * max 1 (V.length x.copyDstLayouts)) 4
    liftIO (V.imapM_ (\i v -> pokeByteOff p_copyDstLayouts (i * 4) (openToWire v)) x.copyDstLayouts)
    liftIO
      (pokeByteOff p 40 (if V.null x.copyDstLayouts then nullPtr else (castPtr p_copyDstLayouts :: Ptr ())))
    liftIO (pokeByteOff p 32 (fromIntegral (V.length x.copyDstLayouts) :: Word32))
    liftIO (pokeFixedBytes 16 p 48 x.optimalTilingLayoutUUID)
    liftIO (pokeByteOff p 64 (if x.identicalMemoryTypeRequirements then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceHostImageCopyProperties where
  peekCStruct p = do
    v_copySrcLayouts <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then
        pure V.empty
      else
        V.generateM (fromIntegral n) (\i -> openFromWire <$> peekByteOff q (i * 4))
    v_copyDstLayouts <- do
      n <- (peekByteOff p 32 :: IO Word32)
      q <- peekByteOff p 40 :: IO (Ptr ())
      if q == nullPtr then
        pure V.empty
      else
        V.generateM (fromIntegral n) (\i -> openFromWire <$> peekByteOff q (i * 4))
    v_optimalTilingLayoutUUID <- (peekFixedBytes 16 p 48)
    v_identicalMemoryTypeRequirements <- (toBool <$> peekByteOff p 64)
    pure
      PhysicalDeviceHostImageCopyProperties
        { copySrcLayouts = v_copySrcLayouts
        , copyDstLayouts = v_copyDstLayouts
        , optimalTilingLayoutUUID = v_optimalTilingLayoutUUID
        , identicalMemoryTypeRequirements = v_identicalMemoryTypeRequirements
        }

instance Nil PhysicalDeviceHostImageCopyProperties where
  nil =
    PhysicalDeviceHostImageCopyProperties
      { copySrcLayouts = V.empty
      , copyDstLayouts = V.empty
      , optimalTilingLayoutUUID = ""
      , identicalMemoryTypeRequirements = False
      }

instance Chainable PhysicalDeviceHostImageCopyProperties where
  sTypeWire = 1000270001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSubresourceHostMemcpySize.html VkSubresourceHostMemcpySize>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkSubresourceHostMemcpySizeEXT
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data SubresourceHostMemcpySize = SubresourceHostMemcpySize
  { size :: Word64
  }

instance CStruct SubresourceHostMemcpySize where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000270008 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.size)
    pure ()

instance FromCStruct SubresourceHostMemcpySize where
  peekCStruct p = do
    v_size <- (peekByteOff p 16)
    pure SubresourceHostMemcpySize{size = v_size}

instance Nil SubresourceHostMemcpySize where
  nil =
    SubresourceHostMemcpySize
      { size = 0
      }

instance Chainable SubresourceHostMemcpySize where
  sTypeWire = 1000270008
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkSubresourceLayout2.html VkSubresourceLayout2>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkSubresourceLayout2EXT, VkSubresourceLayout2KHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
data SubresourceLayout2 (es :: [Type]) = SubresourceLayout2
  { next :: Chain es
  , subresourceLayout :: SubresourceLayout
  }

instance (AllExtends SubresourceLayout2 es, PokeChain es) => CStruct (SubresourceLayout2 es) where
  cSize = 56
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000338002 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.subresourceLayout)
    pure ()

instance
  (AllExtends SubresourceLayout2 es, PokeChain es, PeekChain es)
  => FromCStruct (SubresourceLayout2 es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_subresourceLayout <- (peekByteOff p 16)
    pure SubresourceLayout2{next = v_next, subresourceLayout = v_subresourceLayout}

instance (NilChain es) => Nil (SubresourceLayout2 es) where
  nil =
    SubresourceLayout2
      { next = nilChain
      , subresourceLayout = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCopyImageToImage.html vkCopyImageToImage>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCopyImageToImageEXT
copyImageToImage
  :: forall m
   . (MonadIO m)
  => Device
  -> CopyImageToImageInfo
  -> m (Outcome ())
copyImageToImage device copyImageToImageInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_copyImageToImageInfo :: Ptr () <- castPtr <$> new copyImageToImageInfo
    r_ <-
      liftIO
        ( mkCopyImageToImage
            (checkCommandPtr "vkCopyImageToImage" device.cmds.pCopyImageToImage)
            (castPtr device.handle)
            p_copyImageToImageInfo
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCopyImageToMemory.html vkCopyImageToMemory>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCopyImageToMemoryEXT
copyImageToMemory
  :: forall m
   . (MonadIO m)
  => Device
  -> CopyImageToMemoryInfo
  -> m (Outcome ())
copyImageToMemory device copyImageToMemoryInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_copyImageToMemoryInfo :: Ptr () <- castPtr <$> new copyImageToMemoryInfo
    r_ <-
      liftIO
        ( mkCopyImageToMemory
            (checkCommandPtr "vkCopyImageToMemory" device.cmds.pCopyImageToMemory)
            (castPtr device.handle)
            p_copyImageToMemoryInfo
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCopyMemoryToImage.html vkCopyMemoryToImage>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCopyMemoryToImageEXT
copyMemoryToImage
  :: forall m
   . (MonadIO m)
  => Device
  -> CopyMemoryToImageInfo
  -> m (Outcome ())
copyMemoryToImage device copyMemoryToImageInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_copyMemoryToImageInfo :: Ptr () <- castPtr <$> new copyMemoryToImageInfo
    r_ <-
      liftIO
        ( mkCopyMemoryToImage
            (checkCommandPtr "vkCopyMemoryToImage" device.cmds.pCopyMemoryToImage)
            (castPtr device.handle)
            p_copyMemoryToImageInfo
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkGetImageSubresourceLayout2.html vkGetImageSubresourceLayout2>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkGetImageSubresourceLayout2EXT, vkGetImageSubresourceLayout2KHR
getImageSubresourceLayout2
  :: forall m es3
   . (MonadIO m, AllExtends SubresourceLayout2 es3, PokeChain es3, PeekChain es3, NilChain es3)
  => Device
  -> Image
  -> ImageSubresource2
  -> m (SubresourceLayout2 es3)
getImageSubresourceLayout2 device image subresource =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_subresource :: Ptr () <- castPtr <$> new subresource
    p_o_layout :: Ptr () <- castPtr <$> new (nil :: SubresourceLayout2 es3)
    liftIO
      ( mkGetImageSubresourceLayout2
          (checkCommandPtr "vkGetImageSubresourceLayout2" device.cmds.pGetImageSubresourceLayout2)
          (castPtr device.handle)
          (case image of Image w_ -> w_)
          p_subresource
          p_o_layout
      )
    o_layout <- liftIO (peekCStruct (castPtr p_o_layout))
    pure o_layout

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkTransitionImageLayout.html vkTransitionImageLayout>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkTransitionImageLayoutEXT
transitionImageLayout
  :: forall m
   . (MonadIO m)
  => Device
  -> V.Vector HostImageLayoutTransitionInfo
  -> m (Outcome ())
transitionImageLayout device transitions =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_transitionCount = fromIntegral (V.length transitions) :: Word32
    p_transitions <- do pv_ <- newVector transitions; pure (castPtr pv_ :: Ptr ())
    r_ <-
      liftIO
        ( mkTransitionImageLayout
            (checkCommandPtr "vkTransitionImageLayout" device.cmds.pTransitionImageLayout)
            (castPtr device.handle)
            n_transitionCount
            p_transitions
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
