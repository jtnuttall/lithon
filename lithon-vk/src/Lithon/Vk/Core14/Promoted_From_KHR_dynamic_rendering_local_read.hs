-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core14.Promoted_From_KHR_dynamic_rendering_local_read (
  PhysicalDeviceDynamicRenderingLocalReadFeatures (..),
  RenderingAttachmentLocationInfo (..),
  RenderingInputAttachmentIndexInfo (..),
  cmdSetRenderingAttachmentLocations,
  cmdSetRenderingInputAttachmentIndices,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceDynamicRenderingLocalReadFeatures.html VkPhysicalDeviceDynamicRenderingLocalReadFeatures>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceDynamicRenderingLocalReadFeaturesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceDynamicRenderingLocalReadFeatures = PhysicalDeviceDynamicRenderingLocalReadFeatures
  { dynamicRenderingLocalRead :: Bool
  }

instance CStruct PhysicalDeviceDynamicRenderingLocalReadFeatures where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000232000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.dynamicRenderingLocalRead then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceDynamicRenderingLocalReadFeatures where
  peekCStruct p = do
    v_dynamicRenderingLocalRead <- (toBool <$> peekByteOff p 16)
    pure
      PhysicalDeviceDynamicRenderingLocalReadFeatures
        { dynamicRenderingLocalRead = v_dynamicRenderingLocalRead
        }

instance Nil PhysicalDeviceDynamicRenderingLocalReadFeatures where
  nil =
    PhysicalDeviceDynamicRenderingLocalReadFeatures
      { dynamicRenderingLocalRead = False
      }

instance Chainable PhysicalDeviceDynamicRenderingLocalReadFeatures where
  sTypeWire = 1000232000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkRenderingAttachmentLocationInfo.html VkRenderingAttachmentLocationInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkRenderingAttachmentLocationInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @colorAttachmentCount@ is derived from @colorAttachmentLocations@'s length.
data RenderingAttachmentLocationInfo = RenderingAttachmentLocationInfo
  { colorAttachmentLocations :: V.Vector Word32
  }

instance CStruct RenderingAttachmentLocationInfo where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000232001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    p_colorAttachmentLocations <- arenaBytes (4 * max 1 (V.length x.colorAttachmentLocations)) 4
    liftIO
      (V.imapM_ (\i v -> pokeByteOff p_colorAttachmentLocations (i * 4) v) x.colorAttachmentLocations)
    liftIO
      ( pokeByteOff
          p
          24
          ( if V.null x.colorAttachmentLocations then
              nullPtr
            else
              (castPtr p_colorAttachmentLocations :: Ptr ())
          )
      )
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.colorAttachmentLocations) :: Word32))
    pure ()

instance FromCStruct RenderingAttachmentLocationInfo where
  peekCStruct p = do
    v_colorAttachmentLocations <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 4))
    pure RenderingAttachmentLocationInfo{colorAttachmentLocations = v_colorAttachmentLocations}

instance Nil RenderingAttachmentLocationInfo where
  nil =
    RenderingAttachmentLocationInfo
      { colorAttachmentLocations = V.empty
      }

instance Chainable RenderingAttachmentLocationInfo where
  sTypeWire = 1000232001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkRenderingInputAttachmentIndexInfo.html VkRenderingInputAttachmentIndexInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkRenderingInputAttachmentIndexInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @colorAttachmentCount@ is derived from @colorAttachmentInputIndices@'s length.
--   * @pDepthInputAttachmentIndex@ is deliberately un-modelled (raw pointer); see the module notes.
--   * @pStencilInputAttachmentIndex@ is deliberately un-modelled (raw pointer); see the module notes.
data RenderingInputAttachmentIndexInfo = RenderingInputAttachmentIndexInfo
  { colorAttachmentInputIndices :: V.Vector Word32
  , pDepthInputAttachmentIndex :: Ptr ()
  , pStencilInputAttachmentIndex :: Ptr ()
  }

instance CStruct RenderingInputAttachmentIndexInfo where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000232002 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    p_colorAttachmentInputIndices <- arenaBytes (4 * max 1 (V.length x.colorAttachmentInputIndices)) 4
    liftIO
      (V.imapM_ (\i v -> pokeByteOff p_colorAttachmentInputIndices (i * 4) v) x.colorAttachmentInputIndices)
    liftIO
      ( pokeByteOff
          p
          24
          ( if V.null x.colorAttachmentInputIndices then
              nullPtr
            else
              (castPtr p_colorAttachmentInputIndices :: Ptr ())
          )
      )
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.colorAttachmentInputIndices) :: Word32))
    liftIO (pokeByteOff p 32 x.pDepthInputAttachmentIndex)
    liftIO (pokeByteOff p 40 x.pStencilInputAttachmentIndex)
    pure ()

instance FromCStruct RenderingInputAttachmentIndexInfo where
  peekCStruct p = do
    v_colorAttachmentInputIndices <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 4))
    v_pDepthInputAttachmentIndex <- peekByteOff p 32
    v_pStencilInputAttachmentIndex <- peekByteOff p 40
    pure
      RenderingInputAttachmentIndexInfo
        { colorAttachmentInputIndices = v_colorAttachmentInputIndices
        , pDepthInputAttachmentIndex = v_pDepthInputAttachmentIndex
        , pStencilInputAttachmentIndex = v_pStencilInputAttachmentIndex
        }

instance Nil RenderingInputAttachmentIndexInfo where
  nil =
    RenderingInputAttachmentIndexInfo
      { colorAttachmentInputIndices = V.empty
      , pDepthInputAttachmentIndex = nullPtr
      , pStencilInputAttachmentIndex = nullPtr
      }

instance Chainable RenderingInputAttachmentIndexInfo where
  sTypeWire = 1000232002
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdSetRenderingAttachmentLocations.html vkCmdSetRenderingAttachmentLocations>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdSetRenderingAttachmentLocationsKHR
cmdSetRenderingAttachmentLocations
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> RenderingAttachmentLocationInfo
  -> m ()
cmdSetRenderingAttachmentLocations commandBuffer locationInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_locationInfo :: Ptr () <- castPtr <$> new locationInfo
    liftIO
      ( mkCmdSetRenderingAttachmentLocations
          ( checkCommandPtr
              "vkCmdSetRenderingAttachmentLocations"
              commandBuffer.cmds.pCmdSetRenderingAttachmentLocations
          )
          (castPtr commandBuffer.handle)
          p_locationInfo
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdSetRenderingInputAttachmentIndices.html vkCmdSetRenderingInputAttachmentIndices>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdSetRenderingInputAttachmentIndicesKHR
cmdSetRenderingInputAttachmentIndices
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> RenderingInputAttachmentIndexInfo
  -> m ()
cmdSetRenderingInputAttachmentIndices commandBuffer inputAttachmentIndexInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_inputAttachmentIndexInfo :: Ptr () <- castPtr <$> new inputAttachmentIndexInfo
    liftIO
      ( mkCmdSetRenderingInputAttachmentIndices
          ( checkCommandPtr
              "vkCmdSetRenderingInputAttachmentIndices"
              commandBuffer.cmds.pCmdSetRenderingInputAttachmentIndices
          )
          (castPtr commandBuffer.handle)
          p_inputAttachmentIndexInfo
      )
    pure ()
