-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core14.Promoted_From_KHR_line_rasterization (
  PhysicalDeviceLineRasterizationFeatures (..),
  PhysicalDeviceLineRasterizationProperties (..),
  PipelineRasterizationLineStateCreateInfo (..),
  cmdSetLineStipple,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core14.Enums.LineRasterizationMode
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceLineRasterizationFeatures.html VkPhysicalDeviceLineRasterizationFeatures>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceLineRasterizationFeaturesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceLineRasterizationFeatures = PhysicalDeviceLineRasterizationFeatures
  { rectangularLines :: Bool
  , bresenhamLines :: Bool
  , smoothLines :: Bool
  , stippledRectangularLines :: Bool
  , stippledBresenhamLines :: Bool
  , stippledSmoothLines :: Bool
  }

instance CStruct PhysicalDeviceLineRasterizationFeatures where
  cSize = 40
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000259000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.rectangularLines then TRUE else FALSE))
    liftIO (pokeByteOff p 20 (if x.bresenhamLines then TRUE else FALSE))
    liftIO (pokeByteOff p 24 (if x.smoothLines then TRUE else FALSE))
    liftIO (pokeByteOff p 28 (if x.stippledRectangularLines then TRUE else FALSE))
    liftIO (pokeByteOff p 32 (if x.stippledBresenhamLines then TRUE else FALSE))
    liftIO (pokeByteOff p 36 (if x.stippledSmoothLines then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceLineRasterizationFeatures where
  peekCStruct p = do
    v_rectangularLines <- (toBool <$> peekByteOff p 16)
    v_bresenhamLines <- (toBool <$> peekByteOff p 20)
    v_smoothLines <- (toBool <$> peekByteOff p 24)
    v_stippledRectangularLines <- (toBool <$> peekByteOff p 28)
    v_stippledBresenhamLines <- (toBool <$> peekByteOff p 32)
    v_stippledSmoothLines <- (toBool <$> peekByteOff p 36)
    pure
      PhysicalDeviceLineRasterizationFeatures
        { rectangularLines = v_rectangularLines
        , bresenhamLines = v_bresenhamLines
        , smoothLines = v_smoothLines
        , stippledRectangularLines = v_stippledRectangularLines
        , stippledBresenhamLines = v_stippledBresenhamLines
        , stippledSmoothLines = v_stippledSmoothLines
        }

instance Nil PhysicalDeviceLineRasterizationFeatures where
  nil =
    PhysicalDeviceLineRasterizationFeatures
      { rectangularLines = False
      , bresenhamLines = False
      , smoothLines = False
      , stippledRectangularLines = False
      , stippledBresenhamLines = False
      , stippledSmoothLines = False
      }

instance Chainable PhysicalDeviceLineRasterizationFeatures where
  sTypeWire = 1000259000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceLineRasterizationProperties.html VkPhysicalDeviceLineRasterizationProperties>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceLineRasterizationPropertiesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceLineRasterizationProperties = PhysicalDeviceLineRasterizationProperties
  { lineSubPixelPrecisionBits :: Word32
  }

instance CStruct PhysicalDeviceLineRasterizationProperties where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000259002 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.lineSubPixelPrecisionBits)
    pure ()

instance FromCStruct PhysicalDeviceLineRasterizationProperties where
  peekCStruct p = do
    v_lineSubPixelPrecisionBits <- (peekByteOff p 16)
    pure
      PhysicalDeviceLineRasterizationProperties{lineSubPixelPrecisionBits = v_lineSubPixelPrecisionBits}

instance Nil PhysicalDeviceLineRasterizationProperties where
  nil =
    PhysicalDeviceLineRasterizationProperties
      { lineSubPixelPrecisionBits = 0
      }

instance Chainable PhysicalDeviceLineRasterizationProperties where
  sTypeWire = 1000259002
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPipelineRasterizationLineStateCreateInfo.html VkPipelineRasterizationLineStateCreateInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPipelineRasterizationLineStateCreateInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PipelineRasterizationLineStateCreateInfo = PipelineRasterizationLineStateCreateInfo
  { lineRasterizationMode :: LineRasterizationMode
  , stippledLineEnable :: Bool
  , lineStippleFactor :: Word32
  , lineStipplePattern :: Word16
  }

instance CStruct PipelineRasterizationLineStateCreateInfo where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000259001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (toWire x.lineRasterizationMode))
    liftIO (pokeByteOff p 20 (if x.stippledLineEnable then TRUE else FALSE))
    liftIO (pokeByteOff p 24 x.lineStippleFactor)
    liftIO (pokeByteOff p 28 x.lineStipplePattern)
    pure ()

instance FromCStruct PipelineRasterizationLineStateCreateInfo where
  peekCStruct p = do
    v_lineRasterizationMode <- (unsafeFromWire <$> peekByteOff p 16)
    v_stippledLineEnable <- (toBool <$> peekByteOff p 20)
    v_lineStippleFactor <- (peekByteOff p 24)
    v_lineStipplePattern <- (peekByteOff p 28)
    pure
      PipelineRasterizationLineStateCreateInfo
        { lineRasterizationMode = v_lineRasterizationMode
        , stippledLineEnable = v_stippledLineEnable
        , lineStippleFactor = v_lineStippleFactor
        , lineStipplePattern = v_lineStipplePattern
        }

instance Nil PipelineRasterizationLineStateCreateInfo where
  nil =
    PipelineRasterizationLineStateCreateInfo
      { lineRasterizationMode = LineRasterizationModeDefault
      , stippledLineEnable = False
      , lineStippleFactor = 0
      , lineStipplePattern = 0
      }

instance Chainable PipelineRasterizationLineStateCreateInfo where
  sTypeWire = 1000259001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdSetLineStipple.html vkCmdSetLineStipple>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdSetLineStippleKHR
cmdSetLineStipple
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> Word32
  -> Word16
  -> m ()
cmdSetLineStipple commandBuffer lineStippleFactor lineStipplePattern =
  liftIO do
    liftIO
      ( mkCmdSetLineStipple
          (checkCommandPtr "vkCmdSetLineStipple" commandBuffer.cmds.pCmdSetLineStipple)
          (castPtr commandBuffer.handle)
          lineStippleFactor
          lineStipplePattern
      )
    pure ()
