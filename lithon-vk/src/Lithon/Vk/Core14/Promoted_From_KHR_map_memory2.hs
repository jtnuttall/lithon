-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core14.Promoted_From_KHR_map_memory2 (
  MemoryMapInfo (..),
  MemoryUnmapInfo (..),
  mapMemory2,
  unmapMemory2,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.Enums.MemoryMapFlagBits
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core14.Enums.MemoryUnmapFlagBits
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMemoryMapInfo.html VkMemoryMapInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkMemoryMapInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data MemoryMapInfo = MemoryMapInfo
  { flags :: MemoryMapFlags
  , memory :: DeviceMemory
  , offset :: Word64
  , size :: Word64
  }

instance CStruct MemoryMapInfo where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000271000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.memory)
    liftIO (pokeByteOff p 32 x.offset)
    liftIO (pokeByteOff p 40 x.size)
    pure ()

instance FromCStruct MemoryMapInfo where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_memory <- (peekByteOff p 24)
    v_offset <- (peekByteOff p 32)
    v_size <- (peekByteOff p 40)
    pure MemoryMapInfo{flags = v_flags, memory = v_memory, offset = v_offset, size = v_size}

instance Nil MemoryMapInfo where
  nil =
    MemoryMapInfo
      { flags = mempty
      , memory = nil
      , offset = 0
      , size = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkMemoryUnmapInfo.html VkMemoryUnmapInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkMemoryUnmapInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data MemoryUnmapInfo = MemoryUnmapInfo
  { flags :: MemoryUnmapFlags
  , memory :: DeviceMemory
  }

instance CStruct MemoryUnmapInfo where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000271001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.flags)
    liftIO (pokeByteOff p 24 x.memory)
    pure ()

instance FromCStruct MemoryUnmapInfo where
  peekCStruct p = do
    v_flags <- (peekByteOff p 16)
    v_memory <- (peekByteOff p 24)
    pure MemoryUnmapInfo{flags = v_flags, memory = v_memory}

instance Nil MemoryUnmapInfo where
  nil =
    MemoryUnmapInfo
      { flags = mempty
      , memory = nil
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkMapMemory2.html vkMapMemory2>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkMapMemory2KHR
mapMemory2
  :: forall m
   . (MonadIO m)
  => Device
  -> MemoryMapInfo
  -> m (Outcome (Ptr ()))
mapMemory2 device memoryMapInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_memoryMapInfo :: Ptr () <- castPtr <$> new memoryMapInfo
    p_o_data' <- arenaBytes 8 8
    r_ <-
      liftIO
        ( mkMapMemory2
            (checkCommandPtr "vkMapMemory2" device.cmds.pMapMemory2)
            (castPtr device.handle)
            p_memoryMapInfo
            (castPtr p_o_data')
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      o_data' <- liftIO (peekByteOff p_o_data' 0)
      pure (Ok (openFromWire r_) o_data')

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkUnmapMemory2.html vkUnmapMemory2>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkUnmapMemory2KHR
unmapMemory2
  :: forall m
   . (MonadIO m)
  => Device
  -> MemoryUnmapInfo
  -> m (Outcome ())
unmapMemory2 device memoryUnmapInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_memoryUnmapInfo :: Ptr () <- castPtr <$> new memoryUnmapInfo
    r_ <-
      liftIO
        ( mkUnmapMemory2
            (checkCommandPtr "vkUnmapMemory2" device.cmds.pUnmapMemory2)
            (castPtr device.handle)
            p_memoryUnmapInfo
        )
    if r_ < 0 then
      pure (Err (openFromWire r_))
    else do
      pure (Ok (openFromWire r_) ())
