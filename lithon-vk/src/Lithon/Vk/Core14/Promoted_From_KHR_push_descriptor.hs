-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core14.Promoted_From_KHR_push_descriptor (
  PhysicalDevicePushDescriptorProperties (..),
  cmdPushDescriptorSet,
  cmdPushDescriptorSetWithTemplate,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.DescriptorSetCommands
import Lithon.Vk.Core10.Enums.PipelineBindPoint
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core11.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDevicePushDescriptorProperties.html VkPhysicalDevicePushDescriptorProperties>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDevicePushDescriptorPropertiesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDevicePushDescriptorProperties = PhysicalDevicePushDescriptorProperties
  { maxPushDescriptors :: Word32
  }

instance CStruct PhysicalDevicePushDescriptorProperties where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000080000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.maxPushDescriptors)
    pure ()

instance FromCStruct PhysicalDevicePushDescriptorProperties where
  peekCStruct p = do
    v_maxPushDescriptors <- (peekByteOff p 16)
    pure PhysicalDevicePushDescriptorProperties{maxPushDescriptors = v_maxPushDescriptors}

instance Nil PhysicalDevicePushDescriptorProperties where
  nil =
    PhysicalDevicePushDescriptorProperties
      { maxPushDescriptors = 0
      }

instance Chainable PhysicalDevicePushDescriptorProperties where
  sTypeWire = 1000080000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdPushDescriptorSet.html vkCmdPushDescriptorSet>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdPushDescriptorSetKHR
cmdPushDescriptorSet
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> PipelineBindPoint
  -> PipelineLayout
  -> Word32
  -> V.Vector (SomeStruct WriteDescriptorSet)
  -> m ()
cmdPushDescriptorSet commandBuffer pipelineBindPoint layout set descriptorWrites =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    let n_descriptorWriteCount = fromIntegral (V.length descriptorWrites) :: Word32
    p_descriptorWrites <- do
      pv_ <- arenaBytes (64 * max 1 (V.length descriptorWrites)) 8
      V.imapM_
        (\i e_ -> withSomeStruct e_ (pokeCStruct (castPtr (pv_ `plusPtr` (i * 64)))))
        descriptorWrites
      pure (castPtr pv_ :: Ptr ())
    liftIO
      ( mkCmdPushDescriptorSet
          (checkCommandPtr "vkCmdPushDescriptorSet" commandBuffer.cmds.pCmdPushDescriptorSet)
          (castPtr commandBuffer.handle)
          (toWire pipelineBindPoint)
          (case layout of PipelineLayout w_ -> w_)
          set
          n_descriptorWriteCount
          p_descriptorWrites
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdPushDescriptorSetWithTemplate.html vkCmdPushDescriptorSetWithTemplate>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdPushDescriptorSetWithTemplateKHR
cmdPushDescriptorSetWithTemplate
  :: forall m
   . (MonadIO m)
  => CommandBuffer
  -> DescriptorUpdateTemplate
  -> PipelineLayout
  -> Word32
  -> Ptr ()
  -> m ()
cmdPushDescriptorSetWithTemplate commandBuffer descriptorUpdateTemplate layout set data' =
  liftIO do
    liftIO
      ( mkCmdPushDescriptorSetWithTemplate
          ( checkCommandPtr
              "vkCmdPushDescriptorSetWithTemplate"
              commandBuffer.cmds.pCmdPushDescriptorSetWithTemplate
          )
          (castPtr commandBuffer.handle)
          (case descriptorUpdateTemplate of DescriptorUpdateTemplate w_ -> w_)
          (case layout of PipelineLayout w_ -> w_)
          set
          data'
      )
    pure ()
