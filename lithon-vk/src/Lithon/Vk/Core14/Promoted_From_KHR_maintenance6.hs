-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core14.Promoted_From_KHR_maintenance6 (
  BindDescriptorSetsInfo (..),
  BindMemoryStatus (..),
  PhysicalDeviceMaintenance6Features (..),
  PhysicalDeviceMaintenance6Properties (..),
  PushConstantsInfo (..),
  PushDescriptorSetInfo (..),
  PushDescriptorSetWithTemplateInfo (..),
  cmdBindDescriptorSets2,
  cmdPushConstants2,
  cmdPushDescriptorSet2,
  cmdPushDescriptorSetWithTemplate2,
) where

import Control.Monad (forM_, unless, when)
import Control.Monad.IO.Class (MonadIO, liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word16, Word32, Word64, Word8)
import Foreign.C.Types (CSize (..))
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings, runAlloc, withArena)
import Lithon.Core.BaseTypes (Bool32 (..), toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (
  CStruct (..),
  Chainable (..),
  FromCStruct (..),
  Nil (..),
  new,
  newVector,
  peekElems,
 )
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags (..))
import Lithon.Core.Loader (checkCommandPtr)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

import Lithon.Vk.Core10.DescriptorSetCommands
import Lithon.Vk.Core10.Enums.ShaderStageFlagBits
import Lithon.Vk.Core10.Handles
import Lithon.Vk.Core11.Handles
import Lithon.Vk.Dispatch
import Lithon.Vk.Result (Outcome (..))

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkBindDescriptorSetsInfo.html VkBindDescriptorSetsInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkBindDescriptorSetsInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @descriptorSetCount@ is derived from @descriptorSets@'s length.
--   * @dynamicOffsetCount@ is derived from @dynamicOffsets@'s length.
data BindDescriptorSetsInfo (es :: [Type]) = BindDescriptorSetsInfo
  { next :: Chain es
  , stageFlags :: ShaderStageFlags
  , layout :: PipelineLayout
  , firstSet :: Word32
  , descriptorSets :: V.Vector DescriptorSet
  , dynamicOffsets :: V.Vector Word32
  }

instance (AllExtends BindDescriptorSetsInfo es, PokeChain es) => CStruct (BindDescriptorSetsInfo es) where
  cSize = 64
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545003 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.stageFlags)
    liftIO (pokeByteOff p 24 x.layout)
    liftIO (pokeByteOff p 32 x.firstSet)
    p_descriptorSets <- arenaBytes (8 * max 1 (V.length x.descriptorSets)) 8
    liftIO (V.imapM_ (\i v -> pokeByteOff p_descriptorSets (i * 8) v) x.descriptorSets)
    liftIO
      (pokeByteOff p 40 (if V.null x.descriptorSets then nullPtr else (castPtr p_descriptorSets :: Ptr ())))
    liftIO (pokeByteOff p 36 (fromIntegral (V.length x.descriptorSets) :: Word32))
    p_dynamicOffsets <- arenaBytes (4 * max 1 (V.length x.dynamicOffsets)) 4
    liftIO (V.imapM_ (\i v -> pokeByteOff p_dynamicOffsets (i * 4) v) x.dynamicOffsets)
    liftIO
      (pokeByteOff p 56 (if V.null x.dynamicOffsets then nullPtr else (castPtr p_dynamicOffsets :: Ptr ())))
    liftIO (pokeByteOff p 48 (fromIntegral (V.length x.dynamicOffsets) :: Word32))
    pure ()

instance
  (AllExtends BindDescriptorSetsInfo es, PokeChain es, PeekChain es)
  => FromCStruct (BindDescriptorSetsInfo es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_stageFlags <- (peekByteOff p 16)
    v_layout <- (peekByteOff p 24)
    v_firstSet <- (peekByteOff p 32)
    v_descriptorSets <- do
      n <- (peekByteOff p 36 :: IO Word32)
      q <- peekByteOff p 40 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 8))
    v_dynamicOffsets <- do
      n <- (peekByteOff p 48 :: IO Word32)
      q <- peekByteOff p 56 :: IO (Ptr ())
      if q == nullPtr then pure V.empty else V.generateM (fromIntegral n) (\i -> peekByteOff q (i * 4))
    pure
      BindDescriptorSetsInfo
        { next = v_next
        , stageFlags = v_stageFlags
        , layout = v_layout
        , firstSet = v_firstSet
        , descriptorSets = v_descriptorSets
        , dynamicOffsets = v_dynamicOffsets
        }

instance (NilChain es) => Nil (BindDescriptorSetsInfo es) where
  nil =
    BindDescriptorSetsInfo
      { next = nilChain
      , stageFlags = mempty
      , layout = nil
      , firstSet = 0
      , descriptorSets = V.empty
      , dynamicOffsets = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkBindMemoryStatus.html VkBindMemoryStatus>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkBindMemoryStatusKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @pResult@ is deliberately un-modelled (raw pointer); see the module notes.
data BindMemoryStatus = BindMemoryStatus
  { pResult :: Ptr ()
  }

instance CStruct BindMemoryStatus where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545002 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.pResult)
    pure ()

instance FromCStruct BindMemoryStatus where
  peekCStruct p = do
    v_pResult <- peekByteOff p 16
    pure BindMemoryStatus{pResult = v_pResult}

instance Nil BindMemoryStatus where
  nil =
    BindMemoryStatus
      { pResult = nullPtr
      }

instance Chainable BindMemoryStatus where
  sTypeWire = 1000545002
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceMaintenance6Features.html VkPhysicalDeviceMaintenance6Features>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceMaintenance6FeaturesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceMaintenance6Features = PhysicalDeviceMaintenance6Features
  { maintenance6 :: Bool
  }

instance CStruct PhysicalDeviceMaintenance6Features where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.maintenance6 then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceMaintenance6Features where
  peekCStruct p = do
    v_maintenance6 <- (toBool <$> peekByteOff p 16)
    pure PhysicalDeviceMaintenance6Features{maintenance6 = v_maintenance6}

instance Nil PhysicalDeviceMaintenance6Features where
  nil =
    PhysicalDeviceMaintenance6Features
      { maintenance6 = False
      }

instance Chainable PhysicalDeviceMaintenance6Features where
  sTypeWire = 1000545000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceMaintenance6Properties.html VkPhysicalDeviceMaintenance6Properties>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceMaintenance6PropertiesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceMaintenance6Properties = PhysicalDeviceMaintenance6Properties
  { blockTexelViewCompatibleMultipleLayers :: Bool
  , maxCombinedImageSamplerDescriptorCount :: Word32
  , fragmentShadingRateClampCombinerInputs :: Bool
  }

instance CStruct PhysicalDeviceMaintenance6Properties where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.blockTexelViewCompatibleMultipleLayers then TRUE else FALSE))
    liftIO (pokeByteOff p 20 x.maxCombinedImageSamplerDescriptorCount)
    liftIO (pokeByteOff p 24 (if x.fragmentShadingRateClampCombinerInputs then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceMaintenance6Properties where
  peekCStruct p = do
    v_blockTexelViewCompatibleMultipleLayers <- (toBool <$> peekByteOff p 16)
    v_maxCombinedImageSamplerDescriptorCount <- (peekByteOff p 20)
    v_fragmentShadingRateClampCombinerInputs <- (toBool <$> peekByteOff p 24)
    pure
      PhysicalDeviceMaintenance6Properties
        { blockTexelViewCompatibleMultipleLayers = v_blockTexelViewCompatibleMultipleLayers
        , maxCombinedImageSamplerDescriptorCount = v_maxCombinedImageSamplerDescriptorCount
        , fragmentShadingRateClampCombinerInputs = v_fragmentShadingRateClampCombinerInputs
        }

instance Nil PhysicalDeviceMaintenance6Properties where
  nil =
    PhysicalDeviceMaintenance6Properties
      { blockTexelViewCompatibleMultipleLayers = False
      , maxCombinedImageSamplerDescriptorCount = 0
      , fragmentShadingRateClampCombinerInputs = False
      }

instance Chainable PhysicalDeviceMaintenance6Properties where
  sTypeWire = 1000545001
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPushConstantsInfo.html VkPushConstantsInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPushConstantsInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @size@ is derived from @values@'s length.
data PushConstantsInfo (es :: [Type]) = PushConstantsInfo
  { next :: Chain es
  , layout :: PipelineLayout
  , stageFlags :: ShaderStageFlags
  , offset :: Word32
  , values :: ByteString
  }

instance (AllExtends PushConstantsInfo es, PokeChain es) => CStruct (PushConstantsInfo es) where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545004 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.layout)
    liftIO (pokeByteOff p 24 x.stageFlags)
    liftIO (pokeByteOff p 28 x.offset)
    p_values <- newBytes x.values
    liftIO (pokeByteOff p 40 (if BS.null x.values then nullPtr else (castPtr p_values :: Ptr ())))
    liftIO (pokeByteOff p 32 (fromIntegral (BS.length x.values) :: Word32))
    pure ()

instance (AllExtends PushConstantsInfo es, PokeChain es, PeekChain es) => FromCStruct (PushConstantsInfo es) where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_layout <- (peekByteOff p 16)
    v_stageFlags <- (peekByteOff p 24)
    v_offset <- (peekByteOff p 28)
    v_values <- do
      n <- (peekByteOff p 32 :: IO Word32)
      q <- peekByteOff p 40
      if q == nullPtr then pure BS.empty else BS.packCStringLen (q, fromIntegral n)
    pure
      PushConstantsInfo
        { next = v_next
        , layout = v_layout
        , stageFlags = v_stageFlags
        , offset = v_offset
        , values = v_values
        }

instance (NilChain es) => Nil (PushConstantsInfo es) where
  nil =
    PushConstantsInfo
      { next = nilChain
      , layout = nil
      , stageFlags = mempty
      , offset = 0
      , values = ""
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPushDescriptorSetInfo.html VkPushDescriptorSetInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPushDescriptorSetInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @descriptorWriteCount@ is derived from @descriptorWrites@'s length.
data PushDescriptorSetInfo (es :: [Type]) = PushDescriptorSetInfo
  { next :: Chain es
  , stageFlags :: ShaderStageFlags
  , layout :: PipelineLayout
  , set :: Word32
  , descriptorWrites :: V.Vector (SomeStruct WriteDescriptorSet)
  }

instance (AllExtends PushDescriptorSetInfo es, PokeChain es) => CStruct (PushDescriptorSetInfo es) where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545005 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.stageFlags)
    liftIO (pokeByteOff p 24 x.layout)
    liftIO (pokeByteOff p 32 x.set)
    p_descriptorWrites <- arenaBytes (64 * max 1 (V.length x.descriptorWrites)) 8
    V.imapM_
      (\i e -> withSomeStruct e (pokeCStruct (castPtr (p_descriptorWrites `plusPtr` (i * 64)))))
      x.descriptorWrites
    liftIO
      ( pokeByteOff
          p
          40
          (if V.null x.descriptorWrites then nullPtr else (castPtr p_descriptorWrites :: Ptr ()))
      )
    liftIO (pokeByteOff p 36 (fromIntegral (V.length x.descriptorWrites) :: Word32))
    pure ()

instance (NilChain es) => Nil (PushDescriptorSetInfo es) where
  nil =
    PushDescriptorSetInfo
      { next = nilChain
      , stageFlags = mempty
      , layout = nil
      , set = 0
      , descriptorWrites = V.empty
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPushDescriptorSetWithTemplateInfo.html VkPushDescriptorSetWithTemplateInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPushDescriptorSetWithTemplateInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is the typed @next :: Chain es@ field.
--   * @pData@ is deliberately un-modelled (raw pointer); see the module notes.
data PushDescriptorSetWithTemplateInfo (es :: [Type]) = PushDescriptorSetWithTemplateInfo
  { next :: Chain es
  , descriptorUpdateTemplate :: DescriptorUpdateTemplate
  , layout :: PipelineLayout
  , set :: Word32
  , pData :: Ptr ()
  }

instance
  (AllExtends PushDescriptorSetWithTemplateInfo es, PokeChain es)
  => CStruct (PushDescriptorSetWithTemplateInfo es)
  where
  cSize = 48
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000545006 :: Int32))
    pokeChain (castPtr (p `plusPtr` 8)) x.next
    liftIO (pokeByteOff p 16 x.descriptorUpdateTemplate)
    liftIO (pokeByteOff p 24 x.layout)
    liftIO (pokeByteOff p 32 x.set)
    liftIO (pokeByteOff p 40 x.pData)
    pure ()

instance
  (AllExtends PushDescriptorSetWithTemplateInfo es, PokeChain es, PeekChain es)
  => FromCStruct (PushDescriptorSetWithTemplateInfo es)
  where
  peekCStruct p = do
    v_next <- peekChain =<< peekByteOff p 8
    v_descriptorUpdateTemplate <- (peekByteOff p 16)
    v_layout <- (peekByteOff p 24)
    v_set <- (peekByteOff p 32)
    v_pData <- peekByteOff p 40
    pure
      PushDescriptorSetWithTemplateInfo
        { next = v_next
        , descriptorUpdateTemplate = v_descriptorUpdateTemplate
        , layout = v_layout
        , set = v_set
        , pData = v_pData
        }

instance (NilChain es) => Nil (PushDescriptorSetWithTemplateInfo es) where
  nil =
    PushDescriptorSetWithTemplateInfo
      { next = nilChain
      , descriptorUpdateTemplate = nil
      , layout = nil
      , set = 0
      , pData = nullPtr
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdBindDescriptorSets2.html vkCmdBindDescriptorSets2>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdBindDescriptorSets2KHR
-- __Deprecated__.
cmdBindDescriptorSets2
  :: forall m es1
   . (MonadIO m, AllExtends BindDescriptorSetsInfo es1, PokeChain es1)
  => CommandBuffer
  -> BindDescriptorSetsInfo es1
  -> m ()
cmdBindDescriptorSets2 commandBuffer bindDescriptorSetsInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_bindDescriptorSetsInfo :: Ptr () <- castPtr <$> new bindDescriptorSetsInfo
    liftIO
      ( mkCmdBindDescriptorSets2
          (checkCommandPtr "vkCmdBindDescriptorSets2" commandBuffer.cmds.pCmdBindDescriptorSets2)
          (castPtr commandBuffer.handle)
          p_bindDescriptorSetsInfo
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdPushConstants2.html vkCmdPushConstants2>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdPushConstants2KHR
-- __Deprecated__.
cmdPushConstants2
  :: forall m es1
   . (MonadIO m, AllExtends PushConstantsInfo es1, PokeChain es1)
  => CommandBuffer
  -> PushConstantsInfo es1
  -> m ()
cmdPushConstants2 commandBuffer pushConstantsInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_pushConstantsInfo :: Ptr () <- castPtr <$> new pushConstantsInfo
    liftIO
      ( mkCmdPushConstants2
          (checkCommandPtr "vkCmdPushConstants2" commandBuffer.cmds.pCmdPushConstants2)
          (castPtr commandBuffer.handle)
          p_pushConstantsInfo
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdPushDescriptorSet2.html vkCmdPushDescriptorSet2>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdPushDescriptorSet2KHR
cmdPushDescriptorSet2
  :: forall m es1
   . (MonadIO m, AllExtends PushDescriptorSetInfo es1, PokeChain es1)
  => CommandBuffer
  -> PushDescriptorSetInfo es1
  -> m ()
cmdPushDescriptorSet2 commandBuffer pushDescriptorSetInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_pushDescriptorSetInfo :: Ptr () <- castPtr <$> new pushDescriptorSetInfo
    liftIO
      ( mkCmdPushDescriptorSet2
          (checkCommandPtr "vkCmdPushDescriptorSet2" commandBuffer.cmds.pCmdPushDescriptorSet2)
          (castPtr commandBuffer.handle)
          p_pushDescriptorSetInfo
      )
    pure ()

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/vkCmdPushDescriptorSetWithTemplate2.html vkCmdPushDescriptorSetWithTemplate2>
-- Available: VK_VERSION_1_4
-- Alias spellings: vkCmdPushDescriptorSetWithTemplate2KHR
cmdPushDescriptorSetWithTemplate2
  :: forall m es1
   . (MonadIO m, AllExtends PushDescriptorSetWithTemplateInfo es1, PokeChain es1)
  => CommandBuffer
  -> PushDescriptorSetWithTemplateInfo es1
  -> m ()
cmdPushDescriptorSetWithTemplate2 commandBuffer pushDescriptorSetWithTemplateInfo =
  liftIO $ withArena \arena_ -> runAlloc arena_ do
    p_pushDescriptorSetWithTemplateInfo :: Ptr () <- castPtr <$> new pushDescriptorSetWithTemplateInfo
    liftIO
      ( mkCmdPushDescriptorSetWithTemplate2
          ( checkCommandPtr
              "vkCmdPushDescriptorSetWithTemplate2"
              commandBuffer.cmds.pCmdPushDescriptorSetWithTemplate2
          )
          (castPtr commandBuffer.handle)
          p_pushDescriptorSetWithTemplateInfo
      )
    pure ()
