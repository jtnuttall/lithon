-- Generated by lithon-codegen. DO NOT EDIT.
-- Regenerate: cabal run lithon-codegen -- generate --profile profiles/lithon-core.json
{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE DerivingVia #-}
{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE ImportQualifiedPost #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE PatternSynonyms #-}
{-# LANGUAGE TypeFamilies #-}
{-# LANGUAGE UndecidableInstances #-}
{-# LANGUAGE NoFieldSelectors #-}

module Lithon.Vk.Core14.Promoted_From_KHR_vertex_attribute_divisor (
  VertexInputBindingDivisorDescription (..),
  PhysicalDeviceVertexAttributeDivisorFeatures (..),
  PhysicalDeviceVertexAttributeDivisorProperties (..),
  PipelineVertexInputDivisorStateCreateInfo (..),
) where

import Control.Monad (unless)
import Control.Monad.IO.Class (liftIO)
import Data.ByteString (ByteString)
import Data.ByteString qualified as BS
import Data.Int (Int32, Int64)
import Data.Kind (Type)
import Data.Vector qualified as V
import Data.Word (Word32, Word64)
import Foreign.C.Types (CSize)
import Foreign.Ptr (FunPtr, Ptr, castPtr, nullFunPtr, nullPtr, plusPtr)
import Foreign.Storable (Storable (..), peekByteOff, peekElemOff, pokeByteOff)
import Lithon.Core.Alloc (arenaBytes, newBytes, newCString, newCStrings)
import Lithon.Core.BaseTypes (Bool32, toBool, pattern FALSE, pattern TRUE)
import Lithon.Core.CStruct (CStruct (..), Chainable (..), FromCStruct (..), Nil (..), new, newVector, peekElems)
import Lithon.Core.Chain (
  AllExtends,
  Chain,
  Extends,
  NilChain (..),
  PeekChain (..),
  PokeChain (..),
  SomeStruct (..),
  withSomeStruct,
  pattern (:&),
 )
import Lithon.Core.Flags (Flags)
import Lithon.Core.Open (HasWire (..), Open (..), openFromWire, openToWire, unsafeFromWire)

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkVertexInputBindingDivisorDescription.html VkVertexInputBindingDivisorDescription>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkVertexInputBindingDivisorDescriptionKHR
data VertexInputBindingDivisorDescription = VertexInputBindingDivisorDescription
  { binding :: !Word32
  , divisor :: !Word32
  }
  deriving stock (Eq, Show)

instance Storable VertexInputBindingDivisorDescription where
  sizeOf _ = 8
  alignment _ = 4
  peek p =
    VertexInputBindingDivisorDescription
      <$> (peekByteOff p 0)
      <*> (peekByteOff p 4)
  poke p x = do
    pokeByteOff p 0 x.binding
    pokeByteOff p 4 x.divisor

instance CStruct VertexInputBindingDivisorDescription where
  cSize = 8
  cAlign = 4
  pokeCStruct p x = liftIO (poke p x)

instance FromCStruct VertexInputBindingDivisorDescription where
  peekCStruct = peek

instance Nil VertexInputBindingDivisorDescription where
  nil =
    VertexInputBindingDivisorDescription
      { binding = 0
      , divisor = 0
      }

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceVertexAttributeDivisorFeatures.html VkPhysicalDeviceVertexAttributeDivisorFeatures>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceVertexAttributeDivisorFeatures = PhysicalDeviceVertexAttributeDivisorFeatures
  { vertexAttributeInstanceRateDivisor :: Bool
  , vertexAttributeInstanceRateZeroDivisor :: Bool
  }

instance CStruct PhysicalDeviceVertexAttributeDivisorFeatures where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000190002 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 (if x.vertexAttributeInstanceRateDivisor then TRUE else FALSE))
    liftIO (pokeByteOff p 20 (if x.vertexAttributeInstanceRateZeroDivisor then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceVertexAttributeDivisorFeatures where
  peekCStruct p = do
    v_vertexAttributeInstanceRateDivisor <- (toBool <$> peekByteOff p 16)
    v_vertexAttributeInstanceRateZeroDivisor <- (toBool <$> peekByteOff p 20)
    pure
      PhysicalDeviceVertexAttributeDivisorFeatures
        { vertexAttributeInstanceRateDivisor = v_vertexAttributeInstanceRateDivisor
        , vertexAttributeInstanceRateZeroDivisor = v_vertexAttributeInstanceRateZeroDivisor
        }

instance Nil PhysicalDeviceVertexAttributeDivisorFeatures where
  nil =
    PhysicalDeviceVertexAttributeDivisorFeatures
      { vertexAttributeInstanceRateDivisor = False
      , vertexAttributeInstanceRateZeroDivisor = False
      }

instance Chainable PhysicalDeviceVertexAttributeDivisorFeatures where
  sTypeWire = 1000190002
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPhysicalDeviceVertexAttributeDivisorProperties.html VkPhysicalDeviceVertexAttributeDivisorProperties>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPhysicalDeviceVertexAttributeDivisorPropertiesKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
data PhysicalDeviceVertexAttributeDivisorProperties = PhysicalDeviceVertexAttributeDivisorProperties
  { maxVertexAttribDivisor :: Word32
  , supportsNonZeroFirstInstance :: Bool
  }

instance CStruct PhysicalDeviceVertexAttributeDivisorProperties where
  cSize = 24
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000525000 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    liftIO (pokeByteOff p 16 x.maxVertexAttribDivisor)
    liftIO (pokeByteOff p 20 (if x.supportsNonZeroFirstInstance then TRUE else FALSE))
    pure ()

instance FromCStruct PhysicalDeviceVertexAttributeDivisorProperties where
  peekCStruct p = do
    v_maxVertexAttribDivisor <- (peekByteOff p 16)
    v_supportsNonZeroFirstInstance <- (toBool <$> peekByteOff p 20)
    pure
      PhysicalDeviceVertexAttributeDivisorProperties
        { maxVertexAttribDivisor = v_maxVertexAttribDivisor
        , supportsNonZeroFirstInstance = v_supportsNonZeroFirstInstance
        }

instance Nil PhysicalDeviceVertexAttributeDivisorProperties where
  nil =
    PhysicalDeviceVertexAttributeDivisorProperties
      { maxVertexAttribDivisor = 0
      , supportsNonZeroFirstInstance = False
      }

instance Chainable PhysicalDeviceVertexAttributeDivisorProperties where
  sTypeWire = 1000525000
  chainOffset = 8

-- | Registry: <https://registry.khronos.org/vulkan/specs/latest/man/html/VkPipelineVertexInputDivisorStateCreateInfo.html VkPipelineVertexInputDivisorStateCreateInfo>
-- Available: VK_VERSION_1_4
-- Alias spellings: VkPipelineVertexInputDivisorStateCreateInfoKHR
--
-- Marshalling:
--   * @sType@ is written automatically.
--   * @pNext@ is reserved and written as null.
--   * @vertexBindingDivisorCount@ is derived from @vertexBindingDivisors@'s length.
data PipelineVertexInputDivisorStateCreateInfo = PipelineVertexInputDivisorStateCreateInfo
  { vertexBindingDivisors :: V.Vector VertexInputBindingDivisorDescription
  }

instance CStruct PipelineVertexInputDivisorStateCreateInfo where
  cSize = 32
  cAlign = 8
  pokeCStruct p x = do
    liftIO (pokeByteOff p 0 (1000190001 :: Int32))
    liftIO (pokeByteOff p 8 (nullPtr :: Ptr ()))
    p_vertexBindingDivisors <-
      if V.null x.vertexBindingDivisors then pure nullPtr else newVector x.vertexBindingDivisors
    liftIO (pokeByteOff p 24 p_vertexBindingDivisors)
    liftIO (pokeByteOff p 16 (fromIntegral (V.length x.vertexBindingDivisors) :: Word32))
    pure ()

instance FromCStruct PipelineVertexInputDivisorStateCreateInfo where
  peekCStruct p = do
    v_vertexBindingDivisors <- do
      n <- (peekByteOff p 16 :: IO Word32)
      q <- peekByteOff p 24
      if q == nullPtr then pure V.empty else peekElems (fromIntegral n) q
    pure PipelineVertexInputDivisorStateCreateInfo{vertexBindingDivisors = v_vertexBindingDivisors}

instance Nil PipelineVertexInputDivisorStateCreateInfo where
  nil =
    PipelineVertexInputDivisorStateCreateInfo
      { vertexBindingDivisors = V.empty
      }

instance Chainable PipelineVertexInputDivisorStateCreateInfo where
  sTypeWire = 1000190001
  chainOffset = 8
