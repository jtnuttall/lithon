-- | Placeholder for the shared package-emission glue (option parsers and the
-- emit entry point shared by every generated-package target). Filled in when
-- package emission is hoisted out of the per-target modules.
module Lithon.Codegen.Backend.Package.Emit () where
